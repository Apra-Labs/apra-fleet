{
  "sprint": "kb-trust-ops",
  "track": "single",
  "branch": "feat/code-intelligence-abstraction",
  "base": "main",
  "worktree": "C:\\ws_yash\\Repos\\apra-fleet-main\\apra-fleet",
  "transport": "remote",
  "epic": "yashr-bp2",
  "lastDispatchedPhase": 3,
  "tasks": [
    { "id": "T1.1", "phase": 1, "title": "F1 core: proposal-only capture + guard rekey", "model": "claude-opus-4-8", "type": "work", "status": "completed", "bead": "yashr-bp2.1", "commit": "9b81b423dc0ec5e47769e9de4fd3f2333c1a8447", "notes": "Exemption removed; capture() choke-point forces proposal (UNVERIFIED+flagged+directive:pending, scope=project) per advisory M-A1 (recorded deviation: proposal transform lives in SqliteProvider.capture(), not only the kb-capture handler, so the HTTP route cannot mint an active directive; general INFERRED clamp stays in kb-capture.ts -- broader HTTP clamp bypass tracked as yashr-f3g). H1 promote() gate refuses user-directive naming the CLI; approveDirective is dedicated (no delegate to promote). H2 query()/prime() defaults exclude non-CONFIRMED user-directive; kb_list + flagged_only still show pending. audn supersede guard + decay guard rekeyed to type+CONFIRMED. L1 tool description updated; L2 decay wording. Deviation (resolution 1): D1's 'assert rather than filter' superseded by the binding H2 hardening. kb-user-directive.test.ts rewritten through activation (26 tests green). NOTE: the full activation-flow rewrite was done here (not deferred to T1.3) so every commit stays green; T1.3 adds the dedicated fail-then-pass gate file. Pre-existing (not introduced by T1.1): 4 tests in kb-session-prime.test.ts fail on this machine because they read the real project kb.sqlite (environmental leak) -- identical fail on clean base; plus the 2 allowed timezone failures (yashr-302)." },
    { "id": "T1.2", "phase": 1, "title": "F1 CLI: human-terminal activation surface", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.2", "commit": "f6b46c96c0a1dad0e8ae6a07281aef96242701e7", "notes": "New src/cli/kb-directives.ts (directives/approve-directive/reject-directive/add-directive) wired through the src/index.ts kb subcommand dispatch (resolution 4: one kb group). Opens providers.project (same KB the MCP server uses, cwd-resolved scope). Handlers exported for direct unit testing (13 tests green) plus runKbDirectives top-level dispatch. Trust-rationale header per D1. Smoke-tested end-to-end via node dist/index.js kb directives (exit 0) and error path (exit 1). --help updated. Never exposed over MCP." },
    { "id": "T1.3", "phase": 1, "title": "F1 proof: fail-then-pass + directive test rewrite", "model": "claude-opus-4-8", "type": "work", "status": "completed", "bead": "yashr-bp2.3", "commit": "ebff004b72542cd36795f8f8844434bbfce1fe7c", "notes": "New tests/knowledge/kb-directive-gate.test.ts (8 tests) encodes the yashr-9ha attack through the real tool handlers (kbCapture/kbPromote/kbQuery/kbList) + provider.approveDirective as the CLI does. DOOR 1: forged capture -> pending (UNVERIFIED+flagged+directive:pending, scope forced project), absent from kb_query/prime defaults (H2), visible to kb_list+flagged_only. DOOR 2: two kb_promote -> both refused naming approve-directive, still inactive (promote-ladder attack, H1). CLOSED: approveDirective -> active with all four D6 semantics (top-tier retrieval, never decayed, agent capture only flags), proposal never supersedes active, supersede = human approve-new+reject-old (resolution 2). Fail-then-pass verified: 7/8 fail on pre-T1.1 source, 8/8 pass on patched. The kb-user-directive.test.ts rewrite through activation was done in T1.1 (26 tests) to keep every commit green -- boundary shift from the plan (which nominally put the rewrite in T1.3) recorded here. Full suite: 1878 passed; only pre-existing failures remain (4 env-leak session-prime + 2 allowed timezone yashr-302)." },
    { "id": "T1.4", "phase": 1, "title": "F2: auto-capture standing instructions (docs)", "model": "claude-haiku-4-5", "type": "work", "status": "completed", "bead": "yashr-bp2.4", "commit": "10e021173eed19a90bc449b5424f21db75814fd0", "notes": "DEVIATION (recorded, cosmetic only): executed after T1.6 instead of before it (dispatch order was T1.4/T1.5/T1.6; ran T1.6 first by mistake). No functional dependency between them, so no impact. Added a new 'Standing instructions' H2 section to skills/pm/SKILL.md (pending + exact CLI command apra-fleet kb approve-directive <id>); the pre-existing Lifecycle paragraph that claimed capture-time CONFIRMED (stale post-T1.1) now points to that new section instead of repeating the stale claim -- this is a necessary correctness fix (contradicting docs would be worse than a small in-place edit), not a restructure. Rewrote the 'User directives' section of skills/pm/tpl-kb-agent.md (was titled a D6 CONFIRMED-on-capture exception; now titled D1 proposal+CLI-activation) to state pending/CLI-activation and added the requested guidance that the KB Agent may itself propose a directive detected in the session record via the same pending flow; also fixed a Step 6b parenthetical referencing the removed capture-time-CONFIRMED path (same correctness-fix rationale). Added the one-line note to skills/pm/kb-review.md per the spec (pending directive proposals surfaced, CLI-only resolution, never kb_promote). Docs-only, no server code touched; verified ASCII-only via a byte scan of all three files." },
    { "id": "T1.5", "phase": 1, "title": "F3: polarity word-boundary matching", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.5", "commit": "f38bcdfdf1690718328d992c47f3aefe3c5d1bdf", "notes": "DEVIATION (recorded, cosmetic only): executed after T1.4/T1.6 rather than the dispatched T1.4/T1.5/T1.6 order (T1.6 ran first by mistake, T1.4 second); no functional dependency issue since T1.5 only needed T1.1 (already landed) ahead of it in audn.ts, which held. Replaced hasOppositePolarity's String.includes() substring checks with precompiled word-boundary regexes (\\b anchored at each whole phrase's outer edges, case-insensitive via the 'i' flag); POLARITY_NEGATIVE/POLARITY_POSITIVE lists unchanged; the guard-rekey/contradiction/dedup/supersede structure in makeAudnDecision (T1.1's territory) was not touched. Fail-then-pass VERIFIED BY ACTUAL RUN (not just reasoned): added 3 false-positive tests ('prefixed'/'unresolved'/'suffixed' vs genuine 'is broken' text) plus 4 regression-guard tests to tests/knowledge/audn.test.ts, ran them against the pre-fix source first -- all 3 false-positive tests failed exactly as predicted (asserted false, old includes()-based code returned true), the 4 regression tests already passed pre-fix -- then applied the word-boundary fix and reran: all 7 new tests plus the full audn.test.ts (26/26) pass. Verified apostrophe phrases (\"doesn't work\") still match since \\b anchors only at the phrase's outer edges, not per inner word. Full tests/knowledge suite: 236 passed, only the 4 pre-existing kb-session-prime.test.ts graph-neighbor-expansion failures (yashr-bwc) remain, unrelated to this change." },
    { "id": "T1.6", "phase": 1, "title": "F4: explicit validated repo path for export + cold-seed", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.6", "commit": "13a528cc0580f31f1519e04d1d351b223f228f51", "notes": "DEVIATION (recorded, no functional impact): executed before T1.4/T1.5 -- neither depends on this file pair, so order was safe, but the dispatch specified T1.4/T1.5/T1.6 sequential order and this ran first by mistake; T1.4 and T1.5 follow in this same dispatch. kb-export.ts: added resolveRepoPath(explicit) helper reused for both tiers -- explicit repo_path validated (existsSync+isDirectory) same as before; when omitted, falls back to process.cwd() but now validated through the identical check rather than a blind '|| process.cwd()' (no behavior change for valid inputs; new behavior only on the previously-unhandled edge of an invalid cwd, which now throws the same clear error instead of writing to a bad path). kb-session-prime.ts: added optional repo_path to kbSessionPrimeSchema (cold-seed block only), resolveRepoPath() returns null (not throw) preserving the non-fatal hard-skip contract; explicit invalid input does NOT fall through to cwd (verified by test). Tests added in tests/knowledge/kb-export.test.ts (3 new: cwd fallback, explicit-wins-over-cwd, refuse-when-neither-valid) and tests/knowledge/kb-session-prime.test.ts (4 new in a new 'repo path precedence' describe block: explicit wins, invalid explicit skips silently without falling back to cwd, omitted falls back to validated cwd, neither valid skips silently). The 4 pre-existing kb-session-prime.test.ts failures (graph-neighbor expansion describe block, unmocked process.cwd() reads this repo's real .fleet/kb-canonical.json -- yashr-bwc) reproduced identically after my edit (same 4 test names, same diff shape); git stash to diff against the pre-edit tree was denied by the sandbox policy, so equivalence was confirmed by code inspection (old code's unconditional `process.cwd()` read of that path is unchanged in shape by resolveRepoPath when cwd validates, which it does on this real machine) rather than a literal before/after test run." },
    { "id": "T1.7", "phase": 1, "title": "VERIFY Phase 1", "model": null, "type": "verify", "status": "completed", "bead": "yashr-bp2.7", "commit": "d6fdf82d08749ba2889fe9a7576daef12fe848fc", "notes": "npm run build: CLEAN (tsc, no errors/output). npm test: 130 files, 1912 tests -- 1892 passed, 6 failed, 14 skipped. All 6 failures are on the allowed list, no new/different failures: (a) 2 timezone failures in tests/time-utils.test.ts (toLocalISOString offset/minute-preservation, yashr-302); (b) 4 pre-existing failures in tests/knowledge/kb-session-prime.test.ts 'kb_session_prime graph-neighbor expansion' describe block (graceful-skip x2, isError-context, FTS-hostile-neighbor) -- these leak this repo's real .fleet/kb-canonical.json because that describe block never mocks process.cwd(); confirmed unaffected by T1.6's resolveRepoPath refactor since cwd validates successfully on this real machine (same code path shape as the pre-T1.6 unconditional process.cwd() call). npx gitnexus analyze: SUCCEEDED (21.1s, 4347 nodes, 11764 edges, 243 clusters, 300 flows, incremental changed=19 added=4); it modified AGENTS.md and CLAUDE.md (injected non-ASCII gitnexus:start/end markers) as documented -- reverted both via git checkout -- AGENTS.md CLAUDE.md immediately after, confirmed via git status. Pre-existing untracked .claude/skills/ and .claude/worktrees/ directories (dated before this dispatch -- other agents' worktree checkouts and a gitnexus skill cache) are left alone: they are orchestrator/environment scratch outside this sprint's scope, not sprint deliverables, and committing a nested worktree's raw files would be destructive; git status --porcelain shows only these two pre-existing untracked entries, nothing else. Pushing branch feat/code-intelligence-abstraction next; commit field will be updated to this VERIFY commit's real SHA once created." },
    { "id": "T2.1", "phase": 2, "title": "F5: kb_stats tool + provider stats read", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.8", "commit": "cfdd2873c823b7930149a93402f3bc5d8851717e", "notes": "New src/tools/kb-stats.ts + SqliteProvider.stats() (kb-list no-bump pattern, plain SELECT/COUNT/GROUP BY only). Sections: totals (GROUP BY confidence/type, whole-table counts); stale/flagged/superseded (whole-table counts); retrieval {entries_retrieved (use_count>0 AND live), total_uses (SUM(use_count), whole-table), hit_rate = entries_retrieved/total-live per resolution 6, null when total-live is 0}; promote_ratio = promoted_at-IS-NOT-NULL-count / CONFIRMED-count, null when no CONFIRMED yet; coverage (per-symbol EXACT-match boolean via json_each + fraction). 'Live' = superseded_at IS NULL AND stale=0 everywhere (matches list()/query() defaults). bible section computed in the TOOL layer (not the provider) via providers.project.list({confidence:'CONFIRMED'}) -- already the exact live-CONFIRMED set kb_export uses, so no new provider primitive was needed: drift = count of live-CONFIRMED entries whose (promoted_at||created_at) is newer than the bible's newest updated_at; absent/unreadable/malformed/non-array bible -> present=false, drift=ALL live CONFIRMED (D5), never thrown -- verified by a dedicated malformed-JSON test that initially caught the wrong branch (fixed by hoisting the liveConfirmed-count fallback outside the file-read try so a JSON.parse throw does not lose it). Repo path uses a local resolveRepoPath(explicit) matching the T1.6 precedence but returning null (non-fatal) rather than throwing, since kb_stats must never fail the whole tool over a bad repo path. Registered as kb_stats in src/index.ts. `stats()` added to the MemoryProvider interface (unlike list(), which stayed SqliteProvider-only) because D4 explicitly binds HttpKbProvider to implement it or return a documented not-supported result -- HttpKbProvider.stats() returns {supported:false, reason, ...all-zero/null shape} without any network call, never throws. 14 tests in tests/knowledge/kb-stats.test.ts cover every section (empty KB; mixed-confidence/type fixture; stale=2 from BOTH the invalidated context-cache entry AND evaluateAudn's update-path superseded row which also sets stale=1 -- corrected from an initial wrong expectation of 1; flagged; superseded; hit_rate denominator arithmetic; promote_ratio; coverage exact-vs-substring near-miss; the kb_list-pattern no-bump proof via the double-query({ids}) technique from kb-list.test.ts; bible absent/current/drift/malformed-JSON/invalid-repo-path; HttpKbProvider never-throw). npm run build clean. Full suite after this task: only the 4 pre-existing kb-session-prime.test.ts leaks (yashr-bwc) fail, no new/different failures." },
    { "id": "T2.2", "phase": 2, "title": "F5/F6: fleet_status KB health + bible drift", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.9", "commit": "5b7b4d1958c9f61e3085f4ea2d376b7ebc649983", "notes": "src/tools/check-status.ts gains kbHealthSummary()/kbHealthCompactLine()/KbHealth types, following the codeIntelligenceHealth degraded-safe precedent exactly (KB 4e11460c): kbHealthSummary() calls kbStats({}) (T2.1) and JSON.parses the result inside try/catch -> null on ANY failure (reject or bad JSON); fleetStatus() adds a belt-and-suspenders outer try/catch around the call too (mirrors the existing codeIntelligence call site) so the section can never throw into fleet_status. Compact line format: 'kb: N entries (confirmed:C stale:S flagged:F) | hit-rate:H% | promote-ratio:P%' with hit-rate/promote-ratio rendered 'n/a' (not 0% or NaN) when their kb_stats denominator is null (empty KB, resolution 6 semantics). JSON payload gains a kbHealth key that is OMITTED (not present as null) when the health read fails, per the literal 'section omitted' wording in the dispatch spec -- this differs deliberately from codeIntelligence's own null-safe-default-object pattern, which always includes the key. Bible drift wording uses the AMENDED D5 text (auto-commit lands in T2.3, not yet landed at this task boundary but the wording anticipates it per the dispatch): nonzero drift -> 'bible: N promotions behind (auto-commit may have failed -- run apra-fleet kb commit)', appended to the compact line only when drift>0 (also present inside the bible sub-object in JSON regardless). 12 tests in tests/fleet-status-kb-health.test.ts: kbHealthCompactLine pure-function rendering (healthy no-drift, drift>0 exact wording, null-rate n/a, bible-absent-with-drift), kbHealthSummary degraded-safe (reject -> null, malformed JSON -> null), and full fleetStatus() integration (compact + json success, drift line, error-path section omission in both formats, proof that member/codeIntelligence sections are untouched when KB fails) -- kb-stats.js is vi.mock'd entirely so none of this touches a real kb.sqlite (the same real-KB-leak class of bug flagged as yashr-bwc in kb-session-prime.test.ts). npm run build clean; ran tests/fleet-status-kb-health.test.ts + tests/fleet-status-branch.test.ts + tests/fleet-status-code-intelligence.test.ts together (40/40 green, no regressions)." },
    { "id": "T2.3", "phase": 2, "title": "F6 docs: export-then-commit checklist", "model": "claude-haiku-4-5", "type": "work", "status": "completed", "bead": "yashr-bp2.10", "commit": "10974284996b8f635bb080b7b8bc625c96fc9a08", "notes": "T2.3 upgraded per user directive 2026-07-07: PLAN.md dispatched this as a docs-only 'export-then-commit checklist' task (claude-haiku-4-5), but the user directive 2026-07-07 ('at time of harvesting we should commit our learning -- we should not run this manually') amended design.md D5 and requirements.md to F6a, turning this into a CODE change -- auto-commit inside kb_export itself. Implemented exactly as the amended D5 spec: after writing .fleet/kb-canonical.json, when repoPath has a .git dir AND `git status --porcelain -- <bible-path>` shows the file actually changed (covers both 'modified since last commit' and 'brand-new untracked on first export'), runs `git add <bible-path>` then `git -c user.name=pm-kb -c user.email=kb@pm.local commit -m \"chore(kb): update knowledge bible -- <N> confirmed entries\" -- <bible-path>` (pathspec-only on both the add and the commit's own scope -- verified by a dedicated test that a staged unrelated file is never swept in). Any git failure (execFileSync throw) is caught, logged via logWarn('kb-export', ...), and non-fatal -- export's own JSON.stringify return already happened by construction (committed:false is just appended). Config off-switch read from FLEET_DIR/knowledge/config.json -> {bible:{autoCommit}}, default true, with missing-file/missing-section/malformed-JSON all degrading to true (never silently disables the feature on a config problem). Push intentionally NOT automatic (D5: rides existing sprint push cadence). kb_export's JSON result gains `committed: boolean`. Tool description in index.ts updated to describe the auto-commit + off-switch (no longer says 'the PM commits the resulting file', since that's now false). skills/pm/tpl-kb-agent.md updated per the upgrade spec: Role section's no-git-ops rule gets an explicit carve-out note (the commit is the TOOL's code, not agent discretion); Step 6b retitled as a promote->export->auto-commit checklist item with the full D5-amended explanation and the config off-switch; Step 8 report template gains a 'Bible: committed=.../drift=...' line noting nonzero drift is now an ANOMALY signal (auto-commit likely failed), not a routine reminder; Rules section's 'NEVER commit files' line gets the same carve-out. skills/pm/SKILL.md's PM-completion-flow checklist bullet (named in the ORIGINAL plan's docs-only version) was deliberately NOT touched -- the amended dispatch instructions name only tpl-kb-agent.md, and a manual-commit checklist step there would now be actively wrong (the step is automatic, not something to remind a human to do). 5 tests in tests/knowledge/kb-export-autocommit.test.ts using a real git-init temp-repo fixture confirm all five D5-mandated behaviors: content-unchanged -> no commit (verified via re-export with identical entries, commit count stays 1); changed -> exactly one pathspec commit with pm-kb identity (author/email/subject asserted via git log, plus git show --stat proves only the bible path was touched); git failure -> export success + logged warning (simulated via a fake non-repo .git file, warnSpy asserts logWarn called, export still returns exported:1); autoCommit:false -> no git call at all (config written to the shared FLEET_DIR/knowledge/config.json test path with a snapshot/restore around it to avoid polluting kb-setup.test.ts's shared state, verified via `git log` throwing -- zero commits ever created); dirty unrelated staged file never committed (git show --stat HEAD excludes it, git status still shows it staged afterward). npm run build clean. Byte-scanned all touched files for non-ASCII -- the only non-ASCII bytes found are pre-existing lines in src/index.ts (the --help text's em-dash bullets), confirmed via git diff to be outside every line this task touched. Full tests/knowledge/ suite after this task: 259 tests, only the 4 pre-existing kb-session-prime.test.ts leaks (yashr-bwc) fail, no new/different failures." },
    { "id": "T2.4", "phase": 2, "title": "F7: server version handshake", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.11", "commit": "e9f54b491c531d3eac606fff282f99c2e214c26a", "notes": "New src/services/version-check.ts (small helper module, per the plan's 'either in check-status.ts or a new src/services/version-check.ts' choice). readDiskVersion(startDir?) does a FRESH read every call (not cached at process start like serverVersion itself) via a local findProjectRootFrom() walk-up (mirrors findProjectRoot in install.ts:88-95, capped at 5 levels, same shape); for SEA mode it reads the embedded manifest.json asset instead (same 'node:sea' getAsset mechanism install.ts's own isSea()/getSeaAsset use) since SEA binaries carry no bare version.json on disk relative to the running process. Reused isSea() directly from src/cli/install.ts (already an established cross-import, see src/delivery-mode.ts's identical import) rather than duplicating SEA detection. checkVersionMismatch(running, startDir?) strips a dev git-hash suffix ('_abcdef') from the running side before the string compare, since disk reads are always a bare 'vX.Y.Z' and a hash-suffix artifact of dev mode must not read as a version bump. Both functions degrade to null on ANY failure (missing file, malformed JSON, non-string version field, SEA asset unavailable) -- never throw, per D6's 'omit silently' contract. Wired into check-status.ts's fleetStatus() strictly after T2.2's KB section (shared-file sequencing per PLAN.md), same try/catch-and-default-to-null shape: mismatch -> compact line 'server running vX, disk has vY -- restart your MCP client' (versionMismatchCompactLine()) + a versionMismatch json field; match or any failure -> both omitted entirely (no auto-restart, surface only). 13 tests: tests/version-check.test.ts (10, mocking src/cli/install.js's isSea() the same way tests/delivery-mode.test.ts does -- disk read match, nested subdirectory walk-up, missing/malformed/non-string version.json, SEA-mode-with-unavailable-asset never-throws, mismatch compare with hash-suffix stripping, unreadable-disk omission; caught and fixed a real test bug during this task: vi.clearAllMocks() in afterEach does NOT reset a prior mockReturnValue(true) on isSea(), so the SEA-mode test's mock was leaking into later tests -- fixed by setting isSea's default explicitly in every beforeEach rather than relying on clearAllMocks) and tests/fleet-status-version-check.test.ts (3, full fleetStatus() integration -- match/mismatch/throw across both compact and json formats, mocking version-check.js and kb-stats.js entirely so it never touches real version.json or kb.sqlite state). npm run build clean; byte-scanned changed files for non-ASCII -- check-status.ts's 9 hits are the pre-existing cloud-cost-warning unicode symbol, confirmed via git diff to be outside every line this task touched." },
    { "id": "T2.5", "phase": 2, "title": "VERIFY Phase 2", "model": null, "type": "verify", "status": "completed", "bead": "yashr-bp2.12", "commit": "200f7a41fdb870bac2219f2ed3ea41e6533f1000", "notes": "npm run build: CLEAN (tsc, no errors/output). npm test (npx vitest run, full suite): 135 test files, 1956 tests -- 1936 passed, 6 failed, 14 skipped. All 6 failures are on the allowed list, no new/different failures: (a) 2 timezone failures in tests/time-utils.test.ts (toLocalISOString offset/minute-preservation, yashr-302); (b) 4 pre-existing failures in tests/knowledge/kb-session-prime.test.ts 'kb_session_prime graph-neighbor expansion' describe block (graceful-skip x2, isError-context, FTS-hostile-neighbor) -- unmocked process.cwd() leaks this repo's real .fleet/kb-canonical.json into that describe block (yashr-bwc), unrelated to any Phase 2 change and unaffected by T2.1-T2.4 (none of which touch kb-session-prime.ts). npx gitnexus analyze: SUCCEEDED (20.2s, 4399 nodes, 11970 edges, 250 clusters, 300 flows, incremental changed=9 added=7); it modified AGENTS.md and CLAUDE.md (injected non-ASCII gitnexus:start/end markers) as documented in the KB runbook -- reverted both via git checkout -- AGENTS.md CLAUDE.md immediately after, confirmed clean via git status --porcelain (only the pre-existing untracked .claude/skills/ and .claude/worktrees/ orchestrator-scratch directories remain, same as noted in T1.7). Pushing branch feat/code-intelligence-abstraction next (never main, no PR); commit field updated to this VERIFY commit's real SHA once created." },
    { "id": "T3.1", "phase": 3, "title": "F8: kb_feedback downvote tool", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.13", "commit": "b8825c7626dfe1c0b5434077f50219b84bf05c56", "notes": "New src/tools/kb-feedback.ts + SqliteProvider.feedback(id, reason, author) in sqlite-provider.ts. Effect: stale=1 + flagged_for_review=1 + ASCII note '[feedback <ISO>] <role>: <reason>' appended via the existing truncateContent (CONTENT_CAP=4000) helper; never deletes, never touches confidence. Directive exception keyed off ACTIVE directives (type='user-directive' AND confidence='CONFIRMED', same rekey as T1.1's guards) -- flagged only, stale left untouched; a pending proposal (confidence != CONFIRMED) stales normally, proven by a dedicated test. Role validated via a duplicated validateAuthor/AUTHOR_VALUES pattern in kb-feedback.ts (not imported from kb-capture.ts, which per PLAN.md's shared-file sequencing is touched ONLY by T1.1); invalid/absent role stamps 'unknown'. Registered as kb_feedback in src/index.ts with a description stating the never-deletes/never-demotes contract and that the human resolves in kb-review. 10 tests in tests/knowledge/kb-feedback.test.ts: normal entry (stale+flagged+note, confidence untouched), never-deletes, active-directive flag-only, pending-directive stales normally, CONTENT_CAP respected, unknown-id error (provider + tool layer), plus 4 role-validation cases (valid/invalid/absent/unknown-id). npm run build clean; targeted run (kb-feedback + kb-user-directive + kb-directive-gate) 44/44 green." },
    { "id": "T3.2", "phase": 3, "title": "F8 docs: feedback one-liners in templates", "model": "claude-haiku-4-5", "type": "work", "status": "completed", "bead": "yashr-bp2.14", "commit": "7a980729b2ed41a45b70292384f48109c9a944a2", "notes": "One line added to all four locations per PLAN.md: skills/pm/tpl-doer.md (end of Knowledge Bank section), skills/pm/tpl-reviewer.md (end of Knowledge Bank section, before the KB Agent handoff paragraph), and both the doer + reviewer dispatch prompt blocks inside skills/pm/doer-reviewer-loop.md. Exact wording: 'if a KB entry you retrieved proves wrong in practice, call kb_feedback with the entry id and what was wrong.' Docs-only, no other template edits. Byte-scanned all three touched files for non-ASCII (node charCode scan) -- clean. npm run build clean (no code touched)." },
    { "id": "T3.3", "phase": 3, "title": "F9a: kb_export global scope", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.15", "commit": "ccea4be355b05bb05b4476cbfb120d2c30a1d779", "notes": "src/tools/kb-export.ts (AFTER T1.6, same file): kbExportSchema gains scope: 'project' (default) | 'global'. kbExport() selects providers.project vs providers.global via source.list({confidence:'CONFIRMED'}), writes to .fleet/kb-canonical.json (project) or .fleet/kb-canonical-global.json (global) -- same CanonicalEntry field set, same asciiSafeStringify + id-sorted determinism. maybeAutoCommitBible() gained a scope param purely for the commit message wording ('global knowledge bible' vs 'knowledge bible'); the auto-commit mechanism itself (pathspec-only add+commit, pm-kb identity, content-gated via git status --porcelain, config off-switch, non-fatal) is unchanged and applies identically to the global file. kb_export's JSON result gains a scope field. Tool description in index.ts updated to describe both scopes. 6 new tests in tests/knowledge/kb-export.test.ts describe('scope param'): default-omitted stays project-only (global KB populated but ignored, proving isolation), global scope writes kb-canonical-global.json with the exact stable field set and leaves kb-canonical.json absent, empty global KB writes a valid empty array, ASCII-safe output for global scope. Existing 8 project-scope tests untouched and still pass (test fixture now uses a SEPARATE global SqliteProvider instance instead of aliasing project, so global-scope behavior is exercisable -- default-scope tests are unaffected since they never touch providers.global). npm run build clean. npx vitest run tests/knowledge/kb-export.test.ts + kb-export-autocommit.test.ts: 19/19 green. Full kb-session-prime.test.ts 4 pre-existing yashr-bwc leaks reproduced identically (unrelated to this task, confirmed by running it standalone before touching any other file)." },
    { "id": "T3.4", "phase": 3, "title": "F9b: installer copies global bible", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.16", "commit": "2b9114a9017de3b7cca1cca021600f297898bdb1", "notes": "New copyGlobalBible(repoCwd) helper in src/cli/install.ts, called from Step 9 (KB + code intelligence setup) right after the existing .git cleanup block, independent of the .git check (the source file's own presence is the only gate). Copies <repoCwd>/.fleet/kb-canonical-global.json (when present) to FLEET_DIR/knowledge/global/kb-canonical-global.json (FLEET_DIR = ~/.apra-fleet/data by default, src/paths.ts) via fs.copyFileSync, creating the target dir with mkdirSync({recursive:true}) first. Fully non-fatal: absent source -> silent skip (early return before any dir creation); any error (permission, disk, etc.) -> caught, warned via console.warn('[WARN] Global knowledge bible copy skipped: ...') without throwing -- used [WARN] not the pre-existing file's unicode warning-sign convention to keep this task's own added lines ASCII-only (verified by a diff-scoped byte scan, 0 hits) even though other lines in this file pre-date the ASCII-only convention. 4 tests in tests/install.test.ts describe('install step 9 -- global bible copy'): file present -> copied (asserted via the exact copyFileSync(src, dest) call args), absent -> no copy attempted and install still resolves, target dir auto-created (mkdirSync call args), copy failure -> non-fatal + warns. Learned FLEET_DIR resolves from process.env.APRA_FLEET_DATA_DIR (set to a real tmp dir by tests/setup.ts) rather than the mocked os.homedir() in this test file -- test destDir expectations use the env var directly rather than the mockHome constant used by sibling describe blocks. npm run build clean; full install-test family (install.test.ts + install-force/multi-provider/npm/permissions + uninstall.test.ts): 106/106 green, no regressions." },
    { "id": "T3.5", "phase": 3, "title": "F9c: cold-seed merges installed global bible", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.17", "commit": "ef12973e3fa0e40d1533501f2c6c7d832a707608", "notes": "LAST edit to src/tools/kb-session-prime.ts (per dispatch): a new block appended AFTER the existing project-bible cold-seed block (no restructuring of existing blocks -- only toCanonicalKBEntry() gained an optional `via` param, default 'canonical-bible', preserving the existing project call site byte-for-byte). The new block reads FLEET_DIR/knowledge/global/kb-canonical-global.json (src/paths.ts FLEET_DIR -- homedir-based via APRA_FLEET_DATA_DIR or ~/.apra-fleet/data, NOT the repo -- this is the T3.4 installer's copy target) when top_entries.length < COLD_KB_MAX (re-checked after every prior merge, shared threshold), marks entries via:'canonical-bible-global', dedupes by id against everything already in top_entries (live hits + project-bible), caps at ADDED_ENTRY_CAP, and hard-skips non-fatally on missing/malformed/non-array file (identical try/catch shape to the project block). 9 new tests appended to tests/knowledge/kb-session-prime.test.ts describe('kb_session_prime global-bible cold-seed'): cold KB + global fixture seeds via canonical-bible-global; ordering live > project-bible > global-bible; absent/malformed/bad-shape global file all degrade to today's output; warm session (>=COLD_KB_MAX) skips entirely; dedupe against live hits; dedupe against the project bible (cross-bible dedupe); ADDED_ENTRY_CAP respected. Real node:fs against FLEET_DIR (not mocked), mirroring the sibling project-bible describe block's approach; module-singleton pattern (vi.resetModules() + dynamic import) reusing the file's existing hoisted mocks. npm run build clean. npx vitest run tests/knowledge/kb-session-prime.test.ts: 35/39 pass -- the 4 failures are the pre-existing yashr-bwc graph-neighbor-expansion leaks (unrelated describe block, unmocked process.cwd() reads the real repo's .fleet/kb-canonical.json), reproduced identically before and after this task. Full tests/knowledge/ suite: 278/282 pass, same 4 allowed failures, no new/different failures. Byte-scanned the diff's added lines for non-ASCII -- 0 hits." },
    { "id": "T3.6", "phase": 3, "title": "F10: quantitative model assignment templates", "model": "claude-haiku-4-5", "type": "work", "status": "completed", "bead": "yashr-bp2.18", "commit": "8f3d17f7802dd9ad94bd380538e4f0d0bba4f604", "notes": "Template text only (D9: no code changes), both named files. skills/pm/tpl-planner.md: new 'Quantitative model assignment (F10, D9)' subsection under Knowledge Bank -- call kb_stats with the plan's key symbols after kb_session_prime; coverage >= 0.8 -> cheap/standard, < 0.3 -> premium + front-load, between -> judgment; PLAN.md's model rationale MUST cite the coverage number; fallback to a qualitative 'Planning context' section when kb_stats is unavailable (matches the trial protocol this very sprint's own PLAN.md exercised in T3.6's own dispatch note). Also added a coverage-quantification line to the existing Model assignment rules table section and a citation-check bullet to the Self-critique checklist. doer-reviewer-loop.md's planner dispatch block gained the same kb_stats-then-threshold instruction (call kb_stats, thresholds, citation requirement, kb_stats-unavailable fallback) inline after the existing kb_session_prime sentence. Byte-scanned both files for non-ASCII -- clean. npm run build clean (no code touched)." },
    { "id": "T3.7", "phase": 3, "title": "F11: flagged-pipeline e2e proof", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": "yashr-bp2.19", "commit": "091e6128cb49632d6512c6569d26c1fa9e4a3962", "notes": "New tests/knowledge/kb-flagged-pipeline.test.ts (1 comprehensive e2e test, real in-memory sqlite via SqliteProvider, exercised through the actual TOOL layer -- kbCapture/kbFeedback/kbQuery -- plus provider.promote()/capture() directly for resolution, matching kb-review.md's real primitives). All 5 stages green on first run (predictions verified empirically, not assumed): (1) capture A ('symbolFoo is broken', symbols overlap) then contradicting B ('symbolFoo is fixed') -> audn_decision 'flagged', A.flagged_for_review=true, B.contradiction_of=A.id, B.flagged_for_review=false; (2) kb_feedback downvotes unrelated entry C -> stale=1+flagged_for_review=1; (3) kbQuery({flagged_only:true}) returns all 3 (A, B, C) with full non-empty content -- the TOOL forces include_stale:true so C is not silently dropped; (4) resolve A/B: provider.promote(B) twice to CONFIRMED, then a corrective kb_capture with A's exact title/symbols/source_files but content free of contradiction keywords/polarity words (so AUDN takes the UPDATE path, not re-flagging) -> audn_decision 'update', superseding A; (5) verified actual post-resolution reality: A gets superseded_at+stale=1 (flagged_for_review column itself stays 1, untouched -- never reset by supersede) and DROPS OUT of flagged_only (tool excludes superseded); B's confidence reaches CONFIRMED but contradiction_of is NEVER cleared by kb_promote, so B REMAINS listed in flagged_only indefinitely; C (never resolved) also remains listed; final flagged_only total=2 (B, C), not 0. This finding (promoted/kept side of a resolved pair is not auto-delisted) is the real, non-obvious behavior resolution 7 asked to surface. Corrected skills/pm/kb-review.md: Step 4's A/B recipes now specify the exact supersede mechanics (same title/symbols/source_files, no contradiction/polarity words, or AUDN re-flags instead of updating) and gained a 'Verified actual behavior' block stating kb_promote never clears flagged_for_review/contradiction_of, only the superseded side drops from flagged_only, and a feedback-flagged entry stays listed until separately resolved; Step 5's report template gained a footnote that 'cleared' means the superseded side only (K is normally N, not 2N per resolved pair). Used NON-directive entries throughout per the spec (the directive gate refuses kb_promote on user-directive rows; directive-pending resolution is CLI-only, out of scope for this agent-resolvable flow). npm run build clean. Byte-scanned kb-review.md for non-ASCII -- clean. Full tests/knowledge/ suite: 279/283 pass, same 4 pre-existing yashr-bwc failures, no new/different failures." },
    { "id": "T3.7b", "phase": 3, "title": "PM-added: CLI `apra-fleet kb commit` (closes yashr-8wy + Phase 2 LOW-1 dangling reference f68e41ae)", "model": "claude-sonnet-4-6", "type": "work", "status": "completed", "bead": null, "commit": "cb53ae73fb9e62636e66c3e8f5d59985c6eeac34", "notes": "DEVIATION (PM-added, not in original PLAN.md task list): the amended-D5 fleet_status bible-drift anomaly message (src/tools/check-status.ts, T2.2) tells operators to 'run apra-fleet kb commit' when drift is nonzero, but that command did not exist -- kb-directives.ts (T1.2) only implements directives/approve-directive/reject-directive/add-directive. This task closes that dangling reference plus the standing yashr-8wy bead. New src/cli/kb-commit.ts: thin wrapper around kb_export (T2.3/T3.3), which already owns every commit/no-commit decision (content-gated via git status --porcelain, pathspec-only add+commit, pm-kb identity, non-fatal on git failure, config off-switch). parseKbCommitArgs mirrors kb-directives.ts's tolerant argv parsing (parseSymbols pattern); kbCommitCmd takes an injected KbExportFn (structural type: input in, JSON string out) so tests never import the full tool module graph; runKbCommit is the real top-level dispatch resolving the actual kb_export tool via dynamic import. Wired into src/index.ts's kb subcommand dispatch alongside T1.2's directives commands (same dynamic-import-then-exit-code shape) and into --help. Semantics: `apra-fleet kb commit [--repo <path>] [--global]` re-runs kbExport with scope=project (default) or scope=global (--global); repo path resolution stays inside kb_export's own resolveRepoPath (explicit --repo > validated cwd, T1.6 precedence) -- no new repo-path logic in this file. Prints exported count, output path, scope, and committed true/false with the exact reason category (no change / autoCommit disabled / not a git repo -- kb_export's own committed boolean does not distinguish these, so the CLI's message covers all three generically, matching kb_export's own contract). Picked up mid-stall from a previous doer run: src/index.ts wiring and src/cli/kb-commit.ts were already fully written (uncommitted) when this run started; verified both were complete and correct against kb-export.ts's actual scope/kbExportSchema/return-shape before proceeding, then added the missing test coverage and committed. Tests: tests/knowledge/kb-commit-cli.test.ts (11 tests, mocked KbExportFn) -- parseKbCommitArgs argument combinations (default/--repo/--global/both orders/trailing --repo with no value); kbCommitCmd covering default project scope, --global, --repo passthrough, committed=false wording, export-rejection (exit 1, error message surfaced), malformed-JSON-from-export (exit 1). Smoke tested against the real dist build (not an automated test, per the T1.2 precedent): `node dist/index.js kb commit` against this repo's real KB -- exit 0, exported 69 entries, actually committed via pm-kb identity (commit 8accf3b 'chore(kb): update knowledge bible -- 69 confirmed entries', ancestor of this task's own commit -- this is kb_export's real auto-commit behavior firing as designed, not a test artifact, and is included in the branch history); `node dist/index.js kb commit --repo /definitely/does/not/exist` -- exit 1, kb_export's existing resolveRepoPath error message surfaced verbatim. npm run build clean. Byte-scanned all added lines (new files + src/index.ts diff) for non-ASCII -- clean. Ran together with kb-directives-cli.test.ts + kb-export.test.ts + kb-export-autocommit.test.ts: 43/43 green." },
    { "id": "T3.8", "phase": 3, "title": "VERIFY Phase 3 (final)", "model": null, "type": "verify", "status": "completed", "bead": "yashr-bp2.20", "commit": "3c8f9918500c594c71ac8274f557488f7535c588", "notes": "Resumed after an interrupted doer run (previous doer completed T3.1-T3.7 and stalled mid-T3.7b with src/index.ts modified-but-uncommitted and a new untracked src/cli/kb-commit.ts). Inspected the in-flight diff first: src/index.ts already had the full `kb commit` dispatch wired (help text + dynamic-import dispatch mirroring T1.2's kb-directives.js pattern) and src/cli/kb-commit.ts was already a complete, correct thin wrapper around kb_export (parseKbCommitArgs, KbExportFn structural type, kbCommitCmd, runKbCommit) -- both verified against kb-export.ts's actual scope/schema/return-shape and found correct, so nothing was rewritten, only completed: added tests/knowledge/kb-commit-cli.test.ts (11 tests) and ran a manual smoke test against the real dist build (recorded in T3.7b's own entry). Committed that as T3.7b (bead: PM-added deviation, no pre-existing bead id) at cb53ae73fb9e62636e66c3e8f5d59985c6eeac34, then this VERIFY. 1) npm run build: CLEAN (tsc, no errors/output), run twice (once before the ASCII fix, once after) to confirm both revisions built clean. 2) npm test (npx vitest run, full suite, run twice for the same before/after reason): 138 test files, 1995 tests -- 1975 passed, 6 failed, 14 skipped, BOTH times, identical failure set: (a) 2 timezone failures in tests/time-utils.test.ts (offset/minute-preservation, yashr-302); (b) 4 pre-existing kb-session-prime.test.ts 'graph-neighbor expansion' failures (graceful-skip x2, isError-context, FTS-hostile-neighbor, yashr-bwc) -- same 4 test names as every prior VERIFY; the assertion diffs now show 5 extra live-KB ids because the smoke test's real `node dist/index.js kb commit` run (see T3.7b) legitimately updated this repo's own .fleet/kb-canonical.json content, but the failure is the same pre-existing unmocked-process.cwd()-leak class of bug, not a new regression. 3) npx gitnexus analyze: SUCCEEDED (31.6s, 4430 nodes, 12084 edges, 253 clusters, 300 flows, incremental changed=15 added=5); modified AGENTS.md and CLAUDE.md (injected non-ASCII gitnexus:start/end markers) as documented every prior VERIFY -- reverted both via git checkout -- AGENTS.md CLAUDE.md immediately after, confirmed clean via git status --short (only the pre-existing untracked .claude/skills/ and .claude/worktrees/ orchestrator-scratch dirs remain, same as every prior VERIFY). 4) ASCII sweep over the sprint's own added lines (git diff cb37341..HEAD, added-lines-only, codePointAt>127 byte scan): caught ONE violation NOT flagged by any prior task's own byte scan -- an em-dash in tests/install.test.ts's T3.4 describe() title ('install step 9 -- global bible copy'). Fixed in place (em-dash -> '--', ASCII-only, ran tests/install.test.ts alone first to confirm 11/11 still green), committed separately at 3c8f9918500c594c71ac8274f557488f7535c588, then reran the full sweep against the new HEAD: ALL ADDED LINES ASCII-CLEAN. 5) F1 fail-then-pass gate test confirmed present and passing: tests/knowledge/kb-directive-gate.test.ts, 8/8 tests green (per T1.3's notes: 7/8 failed on pre-T1.1 source, 8/8 pass on patched -- the file itself is unchanged since T1.3, this VERIFY only re-confirmed it still passes on the final tree). 6) Pushed: git push origin feat/code-intelligence-abstraction succeeded (7017bde..3c8f991), then git fetch + git rev-list --left-right --count origin/...HEAD returned '0 0' and git status -sb showed no [ahead]/[behind] marker -- branch fully in sync with origin, confirmed not ahead. Never touched main; no PR opened. Sprint kb-trust-ops (epic yashr-bp2) is now fully complete: all 3 phases + the T3.7b deviation task landed and verified." }
  ],
  "dispatches": [
    { "seq": 1, "role": "planner", "model": "claude-fable-5", "phase": 0, "tasks": [], "tokens": null, "toolUses": null, "ms": null },
    { "seq": 2, "role": "plan-reviewer", "model": "claude-fable-5", "phase": 0, "tasks": [], "tokens": null, "toolUses": null, "ms": null },
    { "seq": 3, "role": "planner", "model": "claude-fable-5", "phase": 0, "tasks": [], "tokens": null, "toolUses": null, "ms": null },
    { "seq": 4, "role": "plan-reviewer", "model": "claude-fable-5", "phase": 0, "tasks": [], "tokens": null, "toolUses": null, "ms": null }
  ]
}
