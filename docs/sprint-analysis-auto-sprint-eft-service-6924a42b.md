# Sprint Analysis: auto-sprint/eft-service

Scope issue id(s): apra-fleet-eft.
Base branch: feat/fleet-reorg.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [90, 96, 103, 109, 109].
High-water-mark closed count this sprint: 109.
Final closed count: 109.
Final open-at-goal-priority count: 14.

## Deploy/Integration outcomes

No deploy failures recorded this sprint.
Integration test failures (5): C1: This dispatch's .fleet-task.md carried no explicit feature-id list and no on-disk scope artifact for one was found, so no per-feature testing/closing was performed (featuresClosed: 0, per the missing-scope contract rather than a guess). Playbook Part 1 (real functional apra-fleet-se suite against real bd, driven via scripts/run-integ-suites.mjs) was already complete on disk from earlier in this run: 868 tests passed / 0 failed in the main run plus a resumed 6-test tail (fatal-diagnostics-guard.test.mjs), exitCode 0 both times -- no failures, no bugs needed. Playbook Part 2 (smoke test) required standing the sandbox up fresh (it was down): Setup, install, server start, and toy-repo clone all succeeded. bd list/show initially failed with 'no beads database found' in the fresh clone; the documented recovery 'bd bootstrap --yes' fixed that but reproduced the known apra-fleet-eft.25 hazard (reactivating a live Dolt sync.remote to the real fleet-e2e-toy repo), which I neutralized by removing the Dolt remote before continuing. The integ-canary tag lookup returned zero matches (known apra-fleet-eft.18), so I self-provisioned the minimal --version-flag canary locally per the Reset fallback. Running apra-fleet workflow auto-sprint against it (max_cycles 1) failed pre-sprint validation with 'No open/in-progress/blocked/deferred beads found for scope --parent gh-toy-xtr. Nothing to do' -- an exact reproduction of the already-filed, still-open apra-fleet-eft.24 (childless single-issue sprint targets get rejected), so I updated that bug's notes with this reproduction instead of filing a duplicate. Test scenario step 4's assertions (canary closed, branch has a commit) both failed as a direct consequence. Teardown (stop server, delete sandbox) ran regardless and completed cleanly. Because Part 2's Test scenario did not pass (known, reproduced, already-tracked blocker) and no scoped features were available to test, passed is false. (bugs filed: none) | C2: Ran integ-test-playbook.md end to end for apra-fleet-eft.10 and apra-fleet-eft.11. Part 1 (real-bd functional suite via scripts/run-integ-suites.mjs) passed: 76/76 files, 0 failures, elapsedWall=256s, cumFileTime=21989s (resumed 1 crashed/pending file from a prior run, no [integ] bug needed). Part 2 (smoke test) failed: Setup, member registration, and canary self-provisioning (gh-toy-4s4, no upstream integ-canary tag found) all succeeded, but 'apra-fleet workflow auto-sprint' hung indefinitely on the very first Planner dispatch ('Plan C1 R1') with zero further log activity for 6+ minutes before being killed -- the member's launch-time claude process died and a later dispatch attempt reused a dead interactive session with no timeout/watchdog surfacing an error, so the canary never closed. Filed apra-fleet-eft.28 (P1) for this, cross-referencing the related-but-distinct apra-fleet-eft.20 (later-phase stall) and noting apra-fleet-eft.24 (pre-sprint validation hard-fail) did not reproduce this run. Teardown was run regardless. For the two assigned features: apra-fleet-eft.10 (server-side member reservation) -- ran the existing test suite plus the new tests/member-reservation-e2e.test.ts, all 79 tests passed including the previously-missing force_release-then-new-sprint-dispatch assertion, so it was closed clean. apra-fleet-eft.11 (vendored agent-contract durability) remains blocked: its [impl] task eft.11.1 is still in_progress and its [test] task eft.11.3 is still open with no test code to run, so it was left open with an updated inconclusive note. (bugs filed: apra-fleet-eft.28) | C3: Playbook part 1 (real-bd apra-fleet-se suite, resumed via scripts/run-integ-suites.mjs from a crashed prior state with 5 pending files) completed pass COMPLETE: 81/81 files, 0 failures (elapsedWall=138s, cumFileTime=22129s). Playbook part 2 (smoke test) ran Setup, self-provisioned the integ-canary (no upstream tag match), registered member toy-doer, and launched apra-fleet workflow auto-sprint (max_cycles:1) against it, but the Test scenario's step 4 assertion failed: the canary (gh-toy-rnh) never closed and the sprint branch landed 0 commits. Root causes filed as new bugs under apra-fleet-eft: apra-fleet-eft.29 (every Planner dispatch to member 'toy-doer' was rejected as 'reserved by' its own current sprint, blocking all planning for the entire run) and apra-fleet-eft.30 (the per-cycle D-push step still targets the real fleet-e2e-toy Dolt remote even after the documented sync.remote neutralization and a passing scripts/check-sandbox-sync-remote.mjs -- it only failed here for lack of local git credentials, not because isolation held by design, revealing the prior eft.25 fix is incomplete). Teardown ran regardless (server stopped, sandbox removed). Only in-scope feature apra-fleet-eft.11 was checked: its [test] task (eft.11.3) is still open and blocked by [impl] eft.11.1, which remains in_progress, so there is no test code to run yet -- left open with an updated inconclusive note, no feature closed and no feature-specific bug filed. (bugs filed: apra-fleet-eft.29, apra-fleet-eft.30) | C4: Checked Step 0a permissions (all playbook-required Bash prefixes present in .claude/settings.json) then ran both parts of integ-test-playbook.md. Part 1 (real functional suite, node scripts/run-integ-suites.mjs against real bd) resumed a stale 81/82 run, completed cleanly: pass COMPLETE, 82/82 files, 0 failures, elapsedWall=134s cumFileTime=22262s. Part 2 (smoke test) brought the sandbox up fresh (install, server on 18700, cloned fleet-e2e-toy, bd bootstrap --yes recovery + both sync.remote/Dolt-remote neutralize steps, verified clean by check-sandbox-sync-remote.mjs), registered member toy-doer, self-provisioned the integ-canary (no upstream tag found), and launched auto-sprint (max_cycles=1) against it -- but the run failed: the Planner's first dispatch to toy-doer hung indefinitely (dashboard state.json 'updatedAt' frozen 100+s with zero further log output, killed after ~2:48 elapsed), and the pre-dispatch D-push step re-wired a Dolt remote from git's own origin and attempted a live push to the real fleet-e2e-toy remote, only blocked by this machine's missing GitHub credentials (confirmed 0 commits actually reached origin/main). Both symptoms exactly match existing open bugs apra-fleet-eft.28 (dispatch hang after member claude-process death) and apra-fleet-eft.30 (D-push escapes sandbox neutralization) whose child fixes show closed but did not hold -- updated both with this cycle's fresh repro evidence rather than filing duplicates. Teardown (stop server, rm -rf sandbox) ran regardless and is confirmed clean. For the single scoped feature apra-fleet-eft.11, its [test] task (eft.11.3) remains open and blocked by the still in_progress eft.11.1 impl task -- no test code exists yet to run, so the feature was left open with an inconclusive note, unchanged from the prior cycle. (bugs filed: none) | C5: Ran the full integ-test-playbook pass for one scoped open feature, apra-fleet-eft.11. Playbook part 1 (real-bd apra-fleet-se suite via scripts/run-integ-suites.mjs) was already complete and current for this tree: pass COMPLETE, 82/82 files passed, elapsedWall=134s cumFileTime=22262s, no failures. Playbook part 2 (smoke test): Setup succeeded (install, server on 18700, toy-repo clone, member registration); a fresh bd bootstrap --yes was needed to materialize the toy repo's beads DB, so both sync.remote neutralize steps (YAML comment-out and 'bd dolt remote remove origin') were re-run and verified clean via check-sandbox-sync-remote.mjs before proceeding. No upstream integ-canary tag was found, so the minimal '--version flag' canary (gh-toy-0pz) was self-provisioned locally per the playbook. The auto-sprint run against that canary (max_cycles:1) reproduced two already-open, previously-filed bugs rather than completing: apra-fleet-eft.30 (the pre-dispatch Dolt sync bracket re-wired the Dolt remote from git origin and attempted a live push to the real fleet-e2e-toy remote, blocked only by missing local credentials -- despite the just-verified neutralize state) and apra-fleet-eft.28 (the first Planner dispatch, 'Plan C1 R1', hung: the dashboard state.json froze for 2m15s+ with zero further log output and no watchdog firing, so it was killed manually). Both bugs were updated with this cycle's fresh evidence rather than re-filed as duplicates; no new bug beads were created. Teardown (stop server, delete sandbox) ran regardless. For the target feature apra-fleet-eft.11 itself: its sole [test] task (eft.11.3) remains open and blocked by its impl sibling (eft.11.1, still in_progress), so there was no test code to execute; the feature was left open with an inconclusive note, unchanged from the prior cycle's state. Overall pass result: false, due to the part 2 smoke-test failures recorded above. (bugs filed: none)

## Reviewer-proposed newTask rejections

3 newTask(s) rejected before reaching bd create: C1: description fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/-]+$/ (or is empty): "Review left the branch with a dirty working tree (Step-4 clean-tree gate violated). Either (a) if the eft.11.1 per-commit push-discipline vendored change is intended to land this sprint, commit agents/doer.md and agents/harvester.md WITHIN the packages/apra-fleet-se/apra-pm submodule to a feat/pm-reorg commit that contains them and bump the parent-repo submodule pointer to that commit (currently pinned to 29aba2900 on fix/e2e-uninstall-teardown-4-g29aba29, which lacks the change, so a fresh `git submodule update` would drop it); or (b) if eft.11.1 is being deferred, revert the uncommitted submodule edits. Also commit-or-revert the six uncommitted docs files (docs/architecture.md, docs/beads.md, packages/apra-fleet-se/auto-sprint/docs/auto-sprint-diagram.md, packages/apra-fleet-se/docs/architecture.md, packages/apra-fleet-se/docs/overview.md, packages/apra-fleet-workflow/docs/apra-fleet-workflow-architecture.md). End state: `git status --porcelain` empty." | C4: description fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/-]+$/ (or is empty): "eft.30.2 added isMemberSyncRemoteConfigured + a doltPushAfter benign-skip branch and exposed the checkSyncRemoteConfigured override hook 'for tests', but no automated test exercises it. Add coverage in the dolt-sync test suite for: (a) a non-diverged push failure with bd-level sync.remote neutralized/absent returns { ok:true, skipped:true, reason:'no-remote' } and does NOT throw DoltSyncError; (b) the same failure with an actively configured sync.remote still throws DoltSyncError (eft.16.1 no-regression); (c) fail-closed paths -- command() throwing, failSoft error result, unparseable output, and empty output all treat the member as configured (still throw). This guards the safety-critical fail-closed default that is currently only manually verified." | C5: description fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/-]+$/ (or is empty): "eft.28 (dispatch hang after member claude PID death), eft.29 (self-reservation rejects every dispatch) and eft.30 (D-push escapes sandbox neutralization) each landed fix+test commits on auto-sprint/eft-service (execute-prompt.ts liveness poll 8b28b757, reservation/dispatch identity token 53789ece, dolt benign-skip 88b3c8ff) but all three reproduced fresh in cycles C3-C5. Their unit/mock tests pass while the actual integ-test-playbook Part 2 smoke run still fails. Add a gating end-to-end assertion (in the apra-fleet-se integ suite driven by scripts/run-integ-suites.mjs) that runs auto-sprint against a self-provisioned canary and asserts the canary closes and the sprint branch has a commit, and do not allow eft.28/29/30 to be marked closed until that gate is green. Reference: tests/execute-prompt-interactive.test.ts, packages/apra-fleet-se/test/dolt-sync-brackets.test.mjs."

## Final verdict

FAIL -- Sprint apra-fleet-eft (base origin/feat/fleet-reorg -> auto-sprint/eft-service, 144 commits, 223 files, +34176/-3814). Local hygiene is fine: git status --porcelain empty, npm run build green, unit suite green (174 files / 2398 passed, 12 skipped). But the sprint FAILS its own acceptance gate. The epic requires the always-on service to be the single supported user-facing entry point with Phases 0-1 shipped before Phase 2, yet the integ-test-playbook Part 2 smoke test (auto-sprint driving a canary to closure) failed in ALL 5 cycles -- the canary never closed and the sprint branch landed 0 commits in every reproduction. 8 P1 bugs remain OPEN under the epic, most of them [integ] smoke-blockers: eft.13 (fixed-port EADDRINUSE cascade), eft.18 (no integ-canary tag in fleet-e2e-toy), eft.20 (silent post-Plan stall + corrupted state.json), eft.24 (pre-sprint validation rejects childless single-issue target), eft.25 (bd bootstrap re-activates real Dolt sync.remote), eft.28 (Planner dispatch hangs forever after member claude PID dies), eft.29 (member rejected as 'reserved by' its own sprint, blocking every dispatch), eft.30 (D-push still targets the real fleet-e2e-toy remote despite neutralization). Critically eft.28/29/30 carry fix+test commits ON this branch (8b28b757/f4720eb2, 53789ece/903d6459, 88b3c8ff/3c08e841) yet all three STILL reproduced fresh in C3/C4/C5 -- the landed fixes did not hold, and eft.30 was blocked from a live push to the real remote only by missing local git credentials, not by design. Two P2 bugs (eft.26 reservation interop gap, eft.27 viewer full-state polling) also remain open. Target feature eft.11 (Phase 3 vendored agent-contract durability) is incomplete: impl eft.11.1 still in_progress and test eft.11.3 still open with no test code; the packages/apra-fleet-se/apra-pm submodule is pinned back to the base pin 1a0e72de (commit 8791f902), so the per-commit push-discipline change is NOT actually vendored. Per-cycle integration verdicts were all 'passed:false'. This is not APPROVE-able while the service cannot complete a single end-to-end canary sprint.
