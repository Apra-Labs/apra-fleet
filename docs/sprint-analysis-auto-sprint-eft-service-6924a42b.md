# Sprint Analysis: auto-sprint/eft-service

Scope issue id(s): apra-fleet-eft.
Base branch: feat/fleet-reorg.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [75, 77, 79, 84, 84].
High-water-mark closed count this sprint: 84.
Final closed count: 84.
Final open-at-goal-priority count: 8.

## Deploy/Integration outcomes

No deploy failures recorded this sprint.
Integration test failures (5): C1: Ran the full integ-test-playbook.md pass. Part 1 (real-bd functional suite, node scripts/run-integ-suites.mjs): 74/74 files passed, 0 failures, exitCode=0 -- this verified the fix for pre-existing bug apra-fleet-eft.16 (dolt-pull 'no remote' error), which I closed, and revealed a widened performance regression (28/74 files now exceed the 5-minute single-file budget, full suite 3228s vs the documented ~6.5-8min target), for which I updated the existing apra-fleet-eft.17 bug rather than filing 28 duplicates. Part 2 (smoke test): Setup/Reset succeeded (server up on :18700, toy repo reset and bd-bootstrapped from the real Dolt remote), but the toy repo's fleet-e2e-toy tracker has no issue tagged 'integ-canary' (only 8 untagged/unrelated issues exist), which blocks Test scenario steps 2-5 entirely; per the playbook's own rule to never write to the real Dolt remote from a sandbox run, I did not re-seed the fixture myself and instead filed apra-fleet-eft.18 (P1) so a maintainer can. Teardown ran regardless (server stopped, sandbox directory removed, verified clean). Of the 11 assigned features, 8 (apra-fleet-eft.1, .2, .3, .4, .5, .6, .8, .9) had their dedicated [test] task's real-bd files pass in Part 1's clean run and were closed; the remaining 3 (apra-fleet-eft.10, .11, .12) are Phase-3/deferred work with no children or [test] tasks yet, so they were left open with an inconclusive note rather than closed or bugged. (bugs filed: apra-fleet-eft.18) | C2: Ran the full integ-test-playbook.md pass. Part 1 (real functional apra-fleet-se suite against real bd, via scripts/run-integ-suites.mjs) was already complete for this session with 868/868 assertions passing across 74/74 files (elapsedWall=3228s, cumFileTime=21733s, 0 failures) -- no regressions. Part 2 (smoke test): Setup/Reset brought the sandbox up cleanly at ~/temp/.apra-fleet-tests on port 18700 (status exited 0, server reachable), but Test scenario step 3 (workflow auto-sprint) crashed immediately with ERR_MODULE_NOT_FOUND for 'undici' -- a dev-mode install bug in src/cli/install.ts's buildDevManifest(), which bundles @apralabs/apra-fleet-client's package tree for the workflow runtime but omits its 'undici' dependency (added for the client's streamable-HTTP transport), so every dev-mode sandbox ships a broken client and auto-sprint can never run. Filed P0 bug apra-fleet-eft.19 for this and ran Teardown regardless (server stopped, sandbox directory removed, verified no lingering process). Checked all three assigned open features (apra-fleet-eft.10, .11, .12): none has a [test] task child yet (bd list --parent confirms zero children for each) -- no impl/test work has landed for them this sprint, so each was left open with an appended inconclusive note; none were closed or given a per-feature bug. (bugs filed: apra-fleet-eft.19) | C3: Continued and completed the integ-test-playbook.md pass for the apra-fleet-eft sprint. Part 1 (real functional apra-fleet-se suite against real bd, via scripts/run-integ-suites.mjs) had already finished before this turn with SUMMARY pass=868 fail=0, exit code 0 -- no new bugs needed there. Across this continued pass, 8 features under apra-fleet-eft (eft.1, .2, .3, .4, .5, .6, .8, .9) were tested and closed clean, and 6 [integ] bugs were filed in total: eft.16 (real-bd 'no remote' dolt pull failures, now fixed and closed), eft.17 (real-bd suite exceeds its 5-minute-per-file budget, still open, P3), eft.18 (toy repo missing integ-canary tag, still open despite its impl/test subtasks being closed -- not yet re-verified/closed), eft.19 (dev-mode install missing undici crashing auto-sprint, same partially-fixed-but-unverified state as eft.18), and two new bugs I filed this turn from the smoke test itself: eft.20 (P1) -- the Part 2 smoke test's Test-scenario auto-sprint against canary gh-toy-53y ran for ~10 minutes, completed only the Plan phase (approved), then silently stalled with no further commits, no error in the fleet server log, and a corrupted state.json (a duplicated quote breaking JSON.parse) in the per-activity state-persistence file that feature eft.2 implements -- the canary never closed, failing the playbook's Test-scenario step-4 assertion; and eft.21 (P2) -- the playbook's own Teardown block silently fails to delete the sandbox because 'rm -rf ~/temp/.apra-fleet-tests' re-resolves '~' against the already-HOME-overridden shell, so the sandbox directory survives untouched even though the command reports success; I worked around this by deleting the sandbox via its absolute path before returning, and confirmed the server is stopped and no sandbox processes remain. No features from this scope remain untested: the only other open/in-progress items under apra-fleet-eft (eft.7, .10, .11, .12) have no [test] task children yet, so they were correctly left untouched as not-yet-ready rather than tested or closed. (bugs filed: apra-fleet-eft.16, apra-fleet-eft.17, apra-fleet-eft.18, apra-fleet-eft.19, apra-fleet-eft.20, apra-fleet-eft.21) | C4: Verified all playbook permission prefixes were present in .claude/settings.json (Step 0a), then ran both playbook parts. Part 1 (real-bd functional suite via scripts/run-integ-suites.mjs, packages/apra-fleet-se/test/INTEG-SUITE.md procedure): resumed 1 crashed/pending file, then reached 'pass COMPLETE' with 75/75 discovered files done and 0 failures -- no [integ] bugs needed. Part 2 (smoke test): Setup succeeded cleanly (fresh install, server started and verified on port 18700, toy repo cloned), but the '## Test scenario' steps (member registration via the register_member MCP tool, and the auto-sprint run against the canary issue) could not be executed this run because this session's tool set was limited to Read/Bash -- no MCP tool-calling capability was available to invoke register_member, which the playbook explicitly requires as an MCP tool call, not a shell command. Per this repo's CLAUDE.md permission-block policy (never author a wrapper/workaround to route around a missing or blocked tool path), I did not script a direct HTTP/JSON-RPC call to the MCP server to fake this step, and did not fabricate a pass. Teardown (stop server, remove sandbox) was still run in full. For the two in-scope features, apra-fleet-eft.11 and apra-fleet-eft.10, bd dep list / bd list --parent showed no [test] task children for either -- no impl or test work has landed for them this sprint, unchanged since the prior check -- so both were left open with an updated 'inconclusive' note rather than closed, and no bug was filed against them. No new bugs were filed overall; passed is reported false because the smoke test's core member-registration/sprint-run verification could not be completed in this run, which should be re-run by a runner instance with MCP tool access. (bugs filed: none) | C5: Ran integ-test-playbook.md part 1 (real functional apra-fleet-se suite against real bd): status already showed 'pass COMPLETE' with 0 failures across all 75 discovered files (elapsedWall/cumFileTime confirmed via `node scripts/run-integ-suites.mjs --status`), so no regression bugs were filed there. Part 2 (smoke test): permissions were verified present, `## Setup` ran cleanly (fresh sandbox HOME, install, server start, `node dist/index.js status` confirmed listening on port 18700, toy repo cloned), but the `## Test scenario` itself could not be executed in this dispatch -- its first step explicitly requires calling the `register_member` MCP tool ('not a shell command'), and this agent instance was provisioned with only Read/Bash tools, no MCP tool-calling capability. Per this repo's CLAUDE.md policy, a missing/blocked capability is to be surfaced rather than routed around with a wrapper/workaround script, so no hand-rolled MCP-over-HTTP client was written to fake the call. `## Teardown` was still run (server stopped, sandbox removed) per the never-skip-teardown rule. No product defect was observed, so no bug bead was filed for this gap. For the two named features, apra-fleet-eft.10 and apra-fleet-eft.11, `bd dep list` confirmed (again, matching prior passes' notes) that neither has any `[test]` task child -- no impl/test work has landed yet -- so both were left open with a refreshed inconclusive note and were not closed. Overall pass is reported as not fully passed because the smoke test's Test scenario portion of part 2 could not be completed. (bugs filed: none)

## Reviewer-proposed newTask rejections

2 newTask(s) rejected before reaching bd create: C5: description fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/-]+$/ (or is empty): "The integ-test-playbook.md Part 2 'Test scenario' (member registration via register_member MCP tool + auto-sprint against the canary, expecting Plan->Develop->Review->Harvest and the canary closing) did not complete successfully in any of the 5 sprint cycles. Provide a dispatch with MCP tool-calling capability and a re-seeded fleet-e2e-toy integ-canary fixture (see apra-fleet-eft.18) and produce one green run whose artifacts (server log, sprint-logs/.state/*.state.json, closed canary issue) are attached. Do not close apra-fleet-eft until this passes. This is the acceptance gate the 5 cycles never cleared." | C5: description fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/-]+$/ (or is empty): "In the smoke sandbox, auto-sprint completed/approved Plan then died silently mid-Develop with no doer commits, no error, and no exit line in the fleet server log; the eft.2 persistence layer also emitted malformed JSON ('\"phase\"\":\"Develop\"') in sprint-logs/.state/*.state.json. The atomic-writer commits (3db79f29 packages/apra-fleet-workflow/src/viewer/index.mjs + debounced-writer.mjs, c5d93857) address the corruption symptom but not the silent doer death. Reproduce, root-cause the stall (doer session created then no activity), add a regression test, and only then close eft.20. Corruption fix must be re-verified against a real SIGKILL/interrupt path, not just unit round-trip (e249dc9d, apra-fleet-workflow-debounced-writer.test.mjs)."

## Final verdict

FAIL -- FAIL. Build is green and the root unit suite passes (169 files, 2337 passed / 12 skipped, exit 0), and 98 commits landed a large amount of real Phase-0/1/2 supervisor, dolt-sync, and installer work backed by substantial new test coverage. However the sprint cannot be signed off: (1) The integ-test-playbook smoke test (Part 2 Test scenario) FAILED to complete in all 5 cycles -- the headline deliverable (auto-sprint-as-a-service running a sprint to completion in a sandbox) was NEVER verified end-to-end. C1/C2/C3 hit hard blocks (missing integ-canary tag, undici ERR_MODULE_NOT_FOUND, silent stall after Plan phase + corrupted state.json); C4/C5 could not even attempt the Test scenario (no MCP tool access to call register_member) and correctly refused to fabricate a pass. (2) Multiple goal-priority beads are still OPEN/IN_PROGRESS, so the epic acceptance criterion 'all child feature beads closed or explicitly deferred with rationale' is unmet: apra-fleet-eft.19 (P0, dev-mode install omits undici -> workflow auto-sprint crashes) is IN_PROGRESS -- a fix commit landed (430a51f2 'bundle undici into dev-mode workflowRuntime manifest', regression test c4cbc788) but the bead is unverified/unclosed because the smoke test never re-ran to green; apra-fleet-eft.20 (P1) OPEN -- auto-sprint silently dies after the Plan phase and the eft.2 continuous-persistence feature (closed as done) wrote MALFORMED JSON to sprint-logs/.state/*.state.json ('"phase"":"Develop"'); an atomic-writer remediation was committed (3db79f29 'route all sprint-state JSON writes through a single atomic writer', c5d93857, debounced-writer.mjs) but the underlying silent-stall product defect is neither root-caused nor re-verified; apra-fleet-eft.13 (P1) OPEN -- fixed-port test servers cause EADDRINUSE cascade -> MCP transport timeout; apra-fleet-eft.18 (P1) OPEN -- fleet-e2e-toy toy repo still has no integ-canary-tagged issue, blocking the smoke Test scenario (docs commit 8d05a23a adds a self-provision fallback but the real fixture reseed requires a maintainer per the never-write-to-real-Dolt rule); apra-fleet-eft.21 (P2) OPEN -- playbook Teardown 'rm -rf ~/temp/...' silently no-ops under the overridden HOME (guarded by test cf0a11e2 / 74189ae1 but bead open); apra-fleet-eft.7 (P2) still IN_PROGRESS. The three below-goal items (eft.10, eft.11, eft.17, all P3) are acceptably left open. Net: the code and unit tests are healthy, but the service has never been demonstrated to run a sprint to completion and a P0 install-crash plus a P1 silent-stall/state-corruption defect remain unverified. Sign-off requires a green smoke-test Test-scenario run.
