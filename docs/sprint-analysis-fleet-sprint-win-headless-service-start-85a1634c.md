# Sprint Analysis: fleet-sprint/win-headless-service-start

Scope issue id(s): apra-fleet-i8qj, apra-fleet-xj7v, apra-fleet-5ti7.
Base branch: fix/dolt-settle-recovery.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [4, 8, 9, 12, 12].
High-water-mark closed count this sprint: 14.
Final closed count: 12.
Final open-at-goal-priority count: 3.

## Deploy/Integration outcomes

Deploy failures (5): C1: Deploy stopped at `npm ci` in C:\akhil\git\apra-fleet. Preflight (node scripts/preflight-clear-build-locks.mjs) ran first and succeeded, clearing 3 stale esbuild.exe PIDs (16276, 24544, 48332) holding node_modules open. Active-sprints check (curl -s http://localhost:8787/api/sprints) returned exit 7 (connection refused) -- nothing listening on 8787, so no active sprints to gate on; proceeded per runbook. `npm ci` then failed with exit code 127 and the following error:

npm error code EPERM
npm error syscall unlink
npm error path C:\akhil\git\apra-fleet\node_modules\@rollup\rollup-win32-x64-msvc\rollup.win32-x64-msvc.node
npm error errno -4048
npm error [Error: EPERM: operation not permitted, unlink 'C:\akhil\git\apra-fleet\node_modules\@rollup\rollup-win32-x64-msvc\rollup.win32-x64-msvc.node'] { errno: -4048, code: 'EPERM', syscall: 'unlink', path: 'C:\\akhil\\git\\apra-fleet\\node_modules\\@rollup\\rollup-win32-x64-msvc\\rollup.win32-x64-msvc.node' }
npm error The operation was rejected by your operating system. It's possible that the file was already in use (by a text editor or antivirus), or that you lack permissions to access it.
Log: C:\Users\akhil\AppData\Local\npm-cache\_logs\2026-08-13T23_40_12_863Z-debug-0.log

The ownership-scoped preflight cleared the esbuild.exe lock holders but did not catch whatever process/handle is locking the rollup native module -- likely another orphaned build/test process or an AV scanner holding that .node file. Per runbook rules I did not attempt to fix or work around this (no retry, no npm install fallback, no manual node_modules deletion). Note: npm ci deletes node_modules before reinstalling, so the repo's node_modules tree may now be in a partially-mutated/incomplete state -- do not assume the checkout is untouched. npm run build, npm run build:binary, the installer step, and the smoke test never ran. Someone with elevated access needs to identify and release the lock on rollup.win32-x64-msvc.node (check for orphaned node/npm/antivirus processes) before re-running deploy.md from the top. | C2: Permissions OK: all 9 required prefixes from deploy.md's Permissions section are covered by the merged allowlist (.claude/settings.json + .claude/settings.local.json). Note: .claude/settings.json has a UTF-8 BOM that breaks the canonical Step 0 JSON.parse snippet; its permissions.allow is empty in this repo anyway (all grants live in settings.local.json), so the merged conclusion is unaffected. Pre-deploy active-sprints check: curl -s http://localhost:8787/api/sprints returned connection refused (exit 7) -- no supervisor sprints at risk, so install --force was never reached. Deploy failed before that step. Ran node scripts/preflight-clear-build-locks.mjs -- it reported 'no stale processes found holding node_modules open'. Immediately after, npm ci (cwd C:/akhil/git/apra-fleet) failed: exit code 127, npm error EPERM: operation not permitted, unlink 'C:/akhil/git/apra-fleet/node_modules/@rollup/.rollup-win32-x64-msvc-AJBsWU3G/rollup.win32-x64-msvc.node'. The runbook's own stale-lock mitigation did not catch this holder (it found nothing to clear yet the file was still locked when npm tried to unlink it) -- likely a process outside this checkout's node_modules (e.g. antivirus scanning the file) rather than an orphaned build tool, but this is not confirmed. npm debug log: C:/Users/akhil/AppData/Local/npm-cache/_logs/2026-08-14T00_44_15_864Z-debug-0.log. Stopped per runbook: do not retry in a loop, do not hand-delete node_modules. Deploy did not proceed past npm ci; no build, no install --force, no service restart occurred. | C3: Deploy stopped at step 2 of ## Deploy. Command: `npm ci`. Failed non-zero with EPERM: npm error code EPERM / syscall unlink / errno -4048 / path C:\akhil\git\apra-fleet\node_modules\@rollup\.rollup-win32-x64-msvc-AJBsWU3G\rollup.win32-x64-msvc.node -- 'operation not permitted, unlink'. Step 1 (node scripts/preflight-clear-build-locks.mjs) ran first per runbook and reported 'no stale processes found holding node_modules open', so the lock holder was not caught by it; that script is scoped to processes whose own executable path is inside this checkout's node_modules, which would not match a process that merely has the .node file loaded, or antivirus. Step 0 permissions check passed: all 9 prefixes in ## Permissions are covered by the merged .claude/settings.json + .claude/settings.local.json allowlist. No workaround or retry attempted per deployer rules. Nothing destructive ran: npm run build, npm run build:binary, the GET /api/sprints active-sprints gate, and the installer's install --force were all NOT executed, so the singleton MCP server on localhost:7523 was not restarted and no running sprint was affected. Operator action needed: clear the process holding the rollup native module in this checkout, then re-trigger deploy. | C4: Deploy stopped at `npm ci` (exit code 127) with an unrecoverable failure. Step 0 permissions check passed: all 9 required prefixes in deploy.md's Permissions section are covered by the merged .claude/settings.json + .claude/settings.local.json allowlist. Ran `node scripts/preflight-clear-build-locks.mjs` first per the runbook, which cleared 5 stale esbuild.exe processes holding node_modules open (PIDs 37160, 23572, 23484, 12444, 42388). Then ran `npm ci`, which failed with: npm error code EPERM / npm error syscall unlink / npm error path C:\akhil\git\apra-fleet\node_modules\@rollup\rollup-win32-x64-msvc\rollup.win32-x64-msvc.node / npm error errno -4048 / npm error [Error: EPERM: operation not permitted, unlink '...rollup.win32-x64-msvc.node'] { errno: -4048, code: 'EPERM', syscall: 'unlink' }. Root cause (verified by reading scripts/preflight-clear-build-locks.mjs): preflight's Windows matcher only kills processes whose own ExecutablePath is literally inside this repo's node_modules directory (`Get-CimInstance Win32_Process -Filter "ExecutablePath LIKE '<repo>\node_modules%'"`), so it cannot detect or clear an out-of-tree node.exe (e.g. C:\Program Files\nodejs\node.exe) that has this native .node module mapped/open -- a coverage gap for loaded native modules vs. build-tool child executables. Re-running preflight would be a no-op against this holder, so per 'do not attempt to fix or work around failures,' I did not retry or hunt for the holder (tasklist/handle/powershell are also outside the merged Bash allowlist). node_modules is likely left in a partially-pruned/inconsistent state since npm ci prunes before installing -- checkout is not in its pre-deploy condition even though no source file was touched. Deploy stopped before the active-sprints check and before `install --force`, so the singleton on localhost:7523 was never restarted and no live supervisor sprint was disturbed -- blast radius zero. No build, install, or smoke test steps were run. Recommended operator action: identify and stop whatever out-of-tree process (likely a stray vitest/node worker or a running apra-fleet process launched from this checkout) is holding rollup.win32-x64-msvc.node open, then re-trigger deploy; separately, consider widening preflight-clear-build-locks.mjs to also detect processes with loaded native modules from node_modules, not just processes executing from inside it. | C5: Deploy stopped at step 2 of ## Deploy (npm ci); did not proceed further.

Step 0 (permissions): all required command prefixes from deploy.md's Permissions section were covered by the merged allowlist (.claude/settings.json + .claude/settings.local.json). Note: .claude/settings.json has a UTF-8 BOM that breaks naive JSON.parse (fixed only in my read logic by stripping \uFEFF, file itself untouched) -- worth fixing as repo hygiene, contributes no permissions.allow entries in this repo (all grants live in settings.local.json, consistent with repo convention).

Executed:
1. node scripts/preflight-clear-build-locks.mjs -> succeeded, output: 'no stale processes found holding node_modules open.'
2. npm ci (cwd C:/akhil/git/apra-fleet) -> FAILED, exit code 127.
   npm error code EPERM
   npm error syscall unlink
   npm error path C:\\akhil\\git\\apra-fleet\\node_modules\\@rollup\\rollup-win32-x64-msvc\\rollup.win32-x64-msvc.node
   npm error errno -4048
   npm error [Error: EPERM: operation not permitted, unlink 'C:\\akhil\\git\\apra-fleet\\node_modules\\@rollup\\rollup-win32-x64-msvc\\rollup.win32-x64-msvc.node'] { errno: -4048, code: 'EPERM', syscall: 'unlink', path: ... }
   npm error The operation was rejected by your operating system. Possible causes: file in use by a text editor/antivirus, or insufficient permissions.
   Debug log: C:\\Users\\akhil\\AppData\\Local\\npm-cache\\_logs\\2026-08-14T02_56_02_331Z-debug-0.log

Diagnostic note: the preflight lock-clear script ran clean and found nothing to kill -- it only targets processes whose own executable path/cmdline points inside this exact checkout's node_modules, so the file lock is held by something else (e.g. antivirus scanning, an editor, or an unrelated process with the native rollup module loaded), not a stale build process from this repo. Re-running the preflight script alone will not resolve this.

Per role rules, I did not attempt to fix/work around the failure and did not continue past this step. No commands from npm run build, npm run build:binary, the active-sprints curl check, or installer install --force were executed. The fleet singleton MCP server (port 7523) was never touched/stopped. Repo files were not modified. No git/bd actions taken.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

1 newTask(s) rejected before reaching bd create: C4: title fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/\[\]-]+$/ (or is empty): "Surface the Windows service registration mode in `apra-fleet status`"

## Final verdict

FAIL -- Reviewed the net diff fix/dolt-settle-recovery..fleet-sprint/win-headless-service-start (12 commits, 11 files, +1481/-68). Verdict is FAIL on four grounds; the underlying code that IS present is largely good.

WHAT IS GENUINELY DONE (verified line-by-line, not by bead count):
- apra-fleet-xj7v (P2) is fully implemented and tested. src/tools/compose-permissions.ts:109-141 replaces the three TODO'd POSIX-only strings with an agent.os branch: the win32 side emits wrapPowerShellEncoded() Test-Path/Get-ChildItem scripts and the settings.local.json read becomes 'if (Test-Path ...) { Get-Content -Raw ... } else { \'{}\' }'. tests/compose-permissions.test.ts:642-770 decodes the -EncodedCommand payload and asserts no 2>/dev/null, ' && ', ' || ', bare cat/ls for a windows member, keeps a byte-identical POSIX regression guard, covers the missing-dir/missing-file degrade path on BOTH branches, and covers a workFolder with an embedded space. This is the one scope bead I would have approved standalone.
- apra-fleet-i8qj implementation is substantive and matches the bead's stated fix shape: register() now attempts '/sc onstart /ru SYSTEM /rl highest' first and degrades to the historical '/sc onlogon /rl limited' (windows.ts register()), persisting the outcome to data/service-registration.json; start() replaced the fire-and-forget detached spawn with a bounded execFileSync (5s timeout) plus a polled Last-Run-Time-advanced check, throwing a typed NoInteractiveSessionError only when the resolved mode is actually onlogon-interactive AND hasInteractiveSession() is false; src/cli/start.ts:70-95 branches on isNoInteractiveSessionError() to print a cause-naming warning plus an explicit no-auto-restart warning before direct-spawning. Failure-path tracing: I verified the ETIMEDOUT claim in i8qj.8 with a standalone repro (execFileSync with timeout throws err.code==='ETIMEDOUT', signal SIGTERM, status null), so the 'treat timeout as submitted-but-unconfirmed and fall through to the poll' branch is correct and cannot hang start(). The SYSTEM-mode misclassification guard (resolveRegistrationModeForStartFailure) and the async-Last-Run-Time race (poll rather than single sample) are both handled. Tests at tests/service-manager.test.ts:59-... and tests/integration/start-no-interactive-session.test.ts cover SYSTEM-preferred, access-denied fallback, both-attempts-fail-with-existing-task, hard-throw-when-no-task, and the four runStart branches including a regression guard that a generic error still takes the old generic path.

BLOCKERS:
1. apra-fleet-5ti7 is materially incomplete. Only the helper (src/os/windows.ts buildDetachedHiddenLaunchCommand/launchDetachedHidden, 5ti7.1) and its unit tests (tests/windows-hidden-launch-helper.test.ts, 5ti7.3) are committed. AC1's 'both the MCP server and the fleet-sprint supervisor launched via a single standardized helper' is NOT met on the branch: apra-fleet-5ti7.2 is still IN_PROGRESS and its work exists only as UNCOMMITTED files in the working tree. AC2 is not met at all - deploy.md is untouched by this diff and still leaves the launch mechanics to be improvised. AC4 (verified live on a real Windows machine) could not be met because deploy never completed.
2. Working tree is dirty (Step 4 requires 'git status --porcelain' empty): untracked src/cli/launch-mcp-server-windows.ts, untracked src/cli/launch-fleet-supervisor-windows.ts, and an unstaged +49-line delta on tests/windows-hidden-launch-helper.test.ts (the 5ti7.2 call-site spy assertions). Mitigating: the COMMITTED branch is self-consistent - the committed test file does not import the untracked launchers - so this is a hygiene/work-loss risk, not a broken branch. A 'git clean -fd' would destroy a completed piece of 5ti7.2.
3. Build and tests could not be run at all. 'npm run build' fails at the fleet-api-contract workspace with "'tsc' is not recognized"; node_modules contains 13 entries and no .bin directory - it was left half-pruned by the deployer's failed 'npm ci'. I reproduced the root cause exactly once: npm ci fails EPERM unlink on node_modules/@rollup/rollup-win32-x64-msvc/rollup.win32-x64-msvc.node. This is ENVIRONMENTAL, not a code defect on this branch - do not dispatch a doer to chase it as a bug. Per repo policy I did not delete node_modules, retry in a loop, or fall back to 'npm install'. Consequence: no green build, lint, or test run backs any of the 12 closed beads this round.
4. Deploy FAILED in all 5 cycles at the same npm ci EPERM, so the two verify-routed beads (apra-fleet-i8qj, apra-fleet-xj7v) were never exercised against a deployed build. i8qj AC1 in particular ('apra-fleet start on a Windows machine with zero interactive sessions ...') is inherently a live-machine assertion and remains unverified; the unit/integration tests satisfy AC3 but not AC1.

CLOSED-BEAD DEFECT (reopen): apra-fleet-i8qj.7 shipped docs/transport-and-service-mode.md line ~38 telling users to 'Use apra-fleet status ... to verify which mode you have'. I grepped: readServiceRegistrationMode() has no consumer anywhere outside src/services/service-manager/windows.ts and tests/service-manager.test.ts, and src/cli/status.ts (lines 55-85) only ever prints 'Service: not installed | installed (enabled) | installed (disabled)'. src/cli/status.ts is not in this diff. The doc therefore instructs users to run a command that cannot answer the question.

OBSERVATION, not asserted as a defect: buildDetachedHiddenLaunchCommand wraps the child in 'cmd.exe /c "<quoted child> > "<log>" 2>&1"'. Nested-quote handling under cmd.exe's stripping rules is exactly the class of bug 5ti7 was filed over, and it cannot be settled by code review - it needs the live Windows verification 5ti7 AC4 asks for. Folding it into the AC4-unverified finding rather than claiming it is broken.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-gpjp.
Summary: Ran both parts of the regression pass and both were blocked by the same single environment defect, not by 169 separate product regressions: the checkout's node_modules is broken (no node_modules/@apralabs workspace symlinks, ajv missing its package.json/index.js, .bin/vitest absent), so the built CLI itself cannot even load (`node dist/index.js --version` fails with ERR_MODULE_NOT_FOUND for 'smol-toml'). Part 1 (real-bd suite): after reconfirming the pre-existing stale-status-file condition already tracked in apra-fleet-m1n (appended a note there, recovered via --fresh per the script's own guidance, left the resulting integ-suite-status.json in place unmodified) both npm run test:integration and npm run test:slow ran, but 169/185 files (and both slow-lane files) failed at elapsed=0s with ERR_MODULE_NOT_FOUND -- no test body actually executed, so this is one blocked environment, not 169 confirmed regressions. Part 2 (smoke test): Setup's `node dist/index.js install` failed with the identical ERR_MODULE_NOT_FOUND before any sandbox state was created, so per the playbook's Setup-verify-failure stop condition the Test scenario was correctly skipped; Teardown was still run (stop errored harmlessly since no server had started, and rm -rf of the sandbox path succeeded, verified absent afterward). One attempted self-repair (npm ci) failed with EPERM on a locked @rollup/rollup-win32-x64-msvc native binary, so no further repair was attempted per this role's non-destructive mandate. Filed apra-fleet-gpjp (P0, standalone/parent-less) covering both parts' shared root cause, with the operator action needed (release the lock on the rollup binary, then npm install && npm run build) in its description. This entire result is informational: it does not gate the current sprint's PASS/FAIL verdict, and apra-fleet-gpjp carries over to a future sprint to be picked up and fixed there.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
