# Sprint Analysis: fix/windows-powershell-shell-portability

Scope issue id(s): apra-fleet-ot2z, apra-fleet-dnri.
Base branch: main.
Cycles run: 3.

## Progress

Closed-bead count history (per cycle evaluation): [24, 28, 35].
High-water-mark closed count this sprint: 38.
Final closed count: 35.
Final open-at-goal-priority count: 0.

## Deploy/Integration outcomes

No deploy failures recorded this sprint.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

None.

## Final verdict

PASS -- Reviewed net diff main..fix/windows-powershell-shell-portability (23 files, +1778/-167) against apra-fleet-ot2z and apra-fleet-dnri.

GATES: npm run build clean; git status --porcelain empty; root npm test exit 0 -- vitest 296 files / 4094 passed / 0 failed, apra-fleet-se workspace 2368 passed / 0 failed. Separately ran packages/apra-fleet-workflow (not covered by the root gate -- see newTask 1): 304 passed / 0 failed. The live-PowerShell suite genuinely EXECUTED rather than skipping (tests/windows-powershell-error-handling.test.ts, 27 tests, 10154ms, real powershell on win32 via the hasPowerShell runIf gate) -- so the ot2z.15 assertions are real evidence, not a platform-skipped false green.

apra-fleet-dnri (closed, P1) -- SATISFIED IN THIS DIFF. packages/apra-fleet-workflow/src/workflow/index.mjs: buildRepairPrompt(errorsText, initialPrompt) now reattaches the original prompt+schema as BEGIN/END ORIGINAL REQUEST reference text (AC bullet 1); currentPrompt is always re-derived from initialPrompt (line ~1396), and test 'a second repair round still carries the original inputs and schema, without compounding' pins non-growth. Resume disposition (lines 1086-1145): repair passes the failed attempt's captured session-id STRING, never bare true, and degrades to an explicit boolean false with a loud [Agent Schema Repair] DEGRADED log when no id was reported -- matching the AC exactly. I traced the failure path the advisor flagged: failedAttemptSessionId is assigned unconditionally at line ~1273 and the ONLY path that bypasses it is the structured.isError branch, which throws AgentDispatchError and exits the loop, so a later repair round can never reuse an earlier round's id. The new session_not_found one-shot re-dispatch (resume:false, inside the existing maxRepairs budget) correctly compensates for execute_prompt's explicit-id resume being TERMINAL -- verified against src/tools/execute-prompt.ts:79 (resume: z.union([z.boolean(), z.string()])), :857-864 and the terminal gate at :874-887, and against known-sessions.ts. skills/fleet/SKILL.md's stale 'resume is boolean only' line is corrected in the same change and now matches the tool schema's actual describe() text. Tests dnri.2/dnri.4 cover happy targeting, per-round re-capture, no-session-id loud degrade, untouched initial dispatch, and session_not_found recovery.

apra-fleet-ot2z (P1) -- the dispatch summary claimed 0 open goal-priority beads, but bd show reports the ot2z PARENT still OPEN (all 22 children closed). I walked its UPDATED ACCEPTANCE CRITERIA 1-6 individually rather than trusting the count: AC1 runner.js credential read is OS-branched on main (runner.js:2477-2486, $env:USERPROFILE\.fleet-git-credential-<label>.bat vs POSIX $HOME); AC2 watch.ts remote tail branches on targetOs (watch.ts:412/450/511/535); AC3 execute-command.ts:244-270 now has a real Windows branch (generateTaskWrapperWindows + WMI detached spawn + $env:USERPROFILE task dir), exceeding the 'at minimum hard-fail' bar; AC4 monitor-task.ts:36 and remove-member.ts:92 both branch on getAgentOS with wrapPowerShellEncoded; AC5 regression tests present and passing; AC6 the rule is in CLAUDE.md:29 and synced to AGENTS.md/AGY.md. All six are covered (AC1-4 landed on main in earlier merged cycles; this branch carries the remainder), so the open parent is a bookkeeping state awaiting post-review closure, not uncovered work -- it is not a FAIL basis.

This diff's ot2z increment, each verified against specific lines: (a) vcs-providers/github.mjs curlBinary(os) emits curl.exe on Windows for both the PR and comment builders -- and I confirmed the PRODUCTION path actually supplies os, not just the test: runner.js:2601 buildCreatePrCommand({...os}) with os from resolveMemberOs at :2598, so the live Invoke-WebRequest alias failure is genuinely fixed, and the golden POSIX commands are asserted byte-unchanged; (b) src/os/windows.ts deepMergeJson -- $current starts $null (no Hashtable metadata pollution) and Merge-Objects converts a PSCustomObject target to a Hashtable via ConvertTo-HashtableDeep before recursing, both pinned by live-PowerShell on-disk assertions; (c) strategy.ts buildWindowsDeleteFilesScript extracted and exercised by the live test rather than hand-copied, preventing silent drift; (d) the test-fixture leak is fixed properly -- makeTempDir now pushes to tempDirsToClean drained by a single collection-time afterAll, instead of registering afterAll from inside it() bodies; (e) contracts.mjs resolveSchemasDir is freshness-first with an injectable seam and 8 branch tests including the in-place-edit case; (f) hygiene: commit faf6c29b deletes the stray all.json/.fleet-review-round2.json and widens the gitignore (I confirmed the deletions in git log --diff-filter=D and that the files are gone and untracked); the ot2z.11 check-*.ps1 scratch scripts are absent and untracked. No secrets, no injection surface (tokens still routed through shQuote/os and only logSafeCommand is logged), no unjustified files.

kb_promotions: NONE. 0e3ece89 (fixture leak) and 1288e360 (deepMergeJson broken two ways) describe defects this very branch FIXED -- promoting them would enshrine a description of a tree that no longer exists. 81b3cdf8 is now partly stale too: repair no longer forces resume:true, it names an explicit session id or falls back to strict false. Everything else in the candidate list I did not independently verify this round.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: none.
Summary: Ran regression-test-playbook.md Part 1 (real-bd suite + slow lane) and Part 2 (sandbox smoke test) against branch HEAD. Part 1 real-bd lane: 200/200 files completed, 8 failures -- all pre-existing and updated on existing carry-over beads (no new beads filed, per the dedupe-first rule): apra-fleet-mmxx (f34-concurrent-launch-engagement-integration.test.mjs, VCSModule/member_detail mismatch), apra-fleet-x7oh (the 'bd create --silent did not return an epic id' setup-failure cluster, now confirmed across five files: finalization-pr-injection, lane-metadata-grouping, planner-auth-failure-no-retry, worklist-failure-isolation, vcs-auth-preflight), apra-fleet-qk6q (kb-remote-scope timeout) and apra-fleet-5i08 (publish-push-failure timeout). Also updated apra-fleet-m1n (stale integ-suite-status.json required --fresh; no failure was erased, prior file recorded only passes) and apra-fleet-eft.17 (long-pole performance: 35/200 files exceed the 300s single-file budget, elapsedWall=3498s/~58min vs the ~7min documented target -- correctness unaffected by this issue itself, NEEDS-DEEPER-DESIGN per prior triage). Part 1 slow lane: 1 pass (dispatch-watchdog-timer-ref.test.mjs) / 1 fail (mock-sprint-planner-dispatch-stalled-session.test.mjs, watchdog_timeout + bd-replay recording drift after 450310ms) -- updated apra-fleet-x1c, identical to prior cycles. Part 2: Setup completed cleanly at branch HEAD (fresh install, server up on port 18700 at uptime 8s, toy-repo clone, git identity seed, sandbox beads seed, both sandbox-isolation guards OK), and the canary gh-toy-4ef was confirmed open. Test scenario step 3a's credential-provisioning command, copied verbatim from the playbook, was denied by the Claude Code auto-mode classifier -- a runner-environment/permissions precondition, not product breakage -- updated apra-fleet-j48h (recurring). Separately, REAL_HOME/.claude/.credentials.json also has an empty accessToken (the apra-fleet-xuo.13 condition, also updated), so both blockers co-occurred. Steps 1 and 3b-6 never executed; Teardown ran regardless and was verified (server stopped, sandbox directory removed). This result is informational only: it does not gate this sprint's PASS/FAIL verdict, and all findings above carry over to a future sprint via the pre-existing parent-less carry-over beads updated in this run (no new beads created).
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
