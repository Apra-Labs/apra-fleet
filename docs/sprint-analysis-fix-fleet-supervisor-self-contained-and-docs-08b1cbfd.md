# Sprint Analysis: fix/fleet-supervisor-self-contained-and-docs

Scope issue id(s): apra-fleet-qqof, apra-fleet-pvw9, apra-fleet-p3ur, apra-fleet-n4lu.
Base branch: fix/runner-unattended-auto-and-dispatch-timeout.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [4, 4, 8, 8, 12].
High-water-mark closed count this sprint: 16.
Final closed count: 12.
Final open-at-goal-priority count: 0.

## Deploy/Integration outcomes

Deploy failures (4): C1: Deploy stopped at the second command in the ## Deploy section: `npm ci` failed (exit code 1). Prior step `node scripts/preflight-clear-build-locks.mjs` succeeded ('no stale processes found holding node_modules open'). npm ci fails compiling the native better-sqlite3 dependency during node-gyp rebuild -- Xcode Command Line Tools' libc++ headers (C++20 concepts: `output_iterator_tag`, `concept`, `requires`) are incompatible with the node-gyp toolchain being invoked against Node v24.11.0 (via nvm) / node-gyp v12.2.0 on macOS (Darwin 24.6.0). Representative error: '/Users/akhil/Library/Caches/node-gyp/24.11.0/include/node/v8-memory-span.h:168:35: error: no type named contiguous_iterator_tag in namespace std' and 'cppgc/macros.h:51: error: unknown type name concept', ultimately 'make: *** [Release/obj.target/better_sqlite3/src/better_sqlite3.o] Error 1' / 'gyp ERR! build error ... not ok'. Full npm debug log at /Users/akhil/.npm/_logs/2026-08-19T03_34_59_234Z-debug-0.log. Did not run npm run build, npm run build:binary, the installer, or the smoke test -- per the runbook, a failing Deploy command stops execution immediately. This is a build-toolchain/environment issue (node-gyp/Xcode CLT vs Node 24 native module compat), not something to work around; deploy.md was not modified. No fleet server was stopped or restarted. | C2: Stopped at Step 0 (permission check) before running any deploy.md commands. Missing permission in the CLI allowlist (.claude/settings.json / .claude/settings.local.json): Bash(*apra-fleet* run *) -- required for the Deploy section's launch step (`"$HOME/.apra-fleet/bin/apra-fleet" run --transport http ...`). The allowlist only has Bash(*apra-fleet* start), which is a different subcommand than what deploy.md's Deploy section actually invokes. All other required prefixes (*apra-fleet-installer-* install *, *apra-fleet* --version, npm ci, npm run build, npm run build:binary, dist/apra-fleet-installer-* install *, curl * localhost:8787/api/sprints*, and node scripts/preflight-clear-build-locks.mjs via the broader node:* allow) are present. Add Bash(*apra-fleet* run *) to the permissions allowlist and re-trigger the deploy. | C3: Stopped at Step 0 (permission check) before running any deploy commands. deploy.md's ## Permissions section requires 9 command prefixes; checked both .claude/settings.json (no permissions.allow block at all) and .claude/settings.local.json (has permissions.allow). All required prefixes are covered by settings.local.json EXCEPT one: `Bash(*apra-fleet* run *)` is missing -- needed for the post-install launch step `"$HOME/.apra-fleet/bin/apra-fleet" run --transport http` in the Deploy section. (Note: settings.local.json has `Bash(*apra-fleet* start)`, which does not cover `run`.) Add `Bash(*apra-fleet* run *)` to the permissions allowlist and re-trigger the deploy. No deploy or smoke-test commands were executed. | C4: Stopped at Step 0: .claude/settings.json has no permissions.allow section at all, so every required command prefix from deploy.md's Permissions section is missing. Missing permissions in the CLI permission settings (.claude/settings.json): Bash(*apra-fleet-installer-* install *), Bash(*apra-fleet* --version), Bash(*apra-fleet* run *), Bash(node scripts/preflight-clear-build-locks.mjs), Bash(npm ci), Bash(npm run build), Bash(npm run build:binary), Bash(dist/apra-fleet-installer-* install *), Bash(curl * localhost:8787/api/sprints*). Add these to the permissions allowlist and re-trigger the sprint. No deploy commands were executed.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

1 newTask(s) rejected before reaching bd create: C5: title fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/+[\]-]+$/ (or is empty): "Deploy blocked: add Bash(*apra-fleet* run *) to the CLI permission allowlist"

## Final verdict

PASS -- Reviewed net diff fix/runner-unattended-auto-and-dispatch-timeout..fix/fleet-supervisor-self-contained-and-docs (8 files, +1359/-2). All four scoped beads met:

- apra-fleet-n4lu (P1): backlog.mjs and scope-overlap.mjs now import execBdAsync from './lib/exec-bd.mjs' instead of the repo-root '../../../../scripts/lib/exec-bd.mjs' escape. src/supervisor/lib/exec-bd.mjs is a verbatim copy of scripts/lib/exec-bd.mjs (diff shows only an added vendoring-rationale comment block; code body identical). E2E test n4lu2-packaged-supervisor-boot.test.mjs builds a REAL installed tree (buildDevManifest + extractWorkflowSubsystemAssets), asserts the old escaping path is absent, and boots node bin/serve.mjs from an arbitrary cwd -- GET/POST /api/sprints answered, exit 0 on /api/shutdown, no ERR_MODULE_NOT_FOUND. PASS.
- apra-fleet-qqof (P1): audit doc packages/apra-fleet-se/docs/supervisor-selfcontainment-audit.md enumerates all 41 supervisor-reachable modules as shipping in-tree; qqof-supervisor-selfcontained-audit.test.mjs walks the full static import graph from the installed serve.mjs and asserts zero out-of-tree / unresolved specifiers (guards ANY future escape, not just the two n4lu files), plus qqof2 boots the installed supervisor and confirms GET /api/sprints AND GET /api/members respond 200. Criterion 3 (n4lu linked as subset) satisfied. PASS.
- apra-fleet-pvw9 (P2): SKILL.md 'Stop the supervisor' (graceful POST /api/shutdown + PID-based hard-stop), cross-platform PID/port lookup for unresponsive API (lsof/pgrep, Get-NetTCPConnection/netstat), and a discrete 'Restart the supervisor' stop-then-start section. PASS.
- apra-fleet-p3ur (P2): SKILL.md 'Auto-start on login/boot' with copy-pasteable Windows (schtasks + NSSM), macOS (launchd LaunchAgent), Linux (systemd --user) steps, each covering register + de-register and linking back to the section-0 smoke test. qqof.4 launchd PATH caveat (absolute /usr/local/bin/node + which node note) present. PASS.

Gates: npm run build exit 0; full test suite exit 0 (SUMMARY pass=2249 fail=0, 2 skipped); git status --porcelain clean. No 'lint' script configured. No src/tools/* changes, so no apra-fleet-client sync required. File hygiene clean -- every changed file maps to a scoped bead; no temp/config slippage. ASCII-only docs.

Deploy failures (4 cycles) are environmental, NOT code defects: C1 npm ci failed compiling better-sqlite3 via node-gyp (Xcode CLT libc++ C++20 headers vs Node v24.11.0) -- deploy.md unmodified; C2/C3/C4 stopped at the permission pre-check because the CLI allowlist lacks Bash(*apra-fleet* run *) (has only *apra-fleet* start). The integ-test-runner independently verified the deployed supervisor boots and serves /api/sprints + /api/members. No bead should be reopened.

Secondary findings filed as newTasks: (1) no automated drift guard keeps the vendored src/supervisor/lib/exec-bd.mjs in sync with the canonical scripts/lib/exec-bd.mjs -- currently comment-enforced by hand only; (2) recurring deploy blocker: permission allowlist missing Bash(*apra-fleet* run *); (3) node-gyp/better-sqlite3 build failure on Node 24 + Xcode CLT blocking npm ci in deploy; (4) minor: SKILL.md start/auto-start examples reference the repo-checkout path packages/apra-fleet-se/bin/serve.mjs rather than the installed ~/.apra-fleet path, mildly at odds with the no-git-clone goal.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-qk6q, apra-fleet-j48h.
Summary: Ran the full regression pass per regression-test-playbook.md. Part 1 (real-bd suite, npm run test:integration): 196/196 files completed, 4 failed (f34-concurrent-launch-engagement-integration.test.mjs, mock-sprint-kb-remote-scope.test.mjs, mock-sprint-member-vcs-provider-threading.test.mjs, mock-sprint-publish-push-failure.test.mjs; elapsedWall=4414s), plus the slow lane (npm run test:slow) where mock-sprint-planner-dispatch-stalled-session.test.mjs failed again (dispatch-watchdog-timer-ref.test.mjs passed) -- so suitePassed=false. Three of these failures and the slow-lane failure exactly reproduced pre-existing tracked carry-over beads (apra-fleet-mmxx, apra-fleet-u87n, apra-fleet-5i08, apra-fleet-x1c), which were updated with reconfirmation notes rather than duplicated; one failure (mock-sprint-kb-remote-scope.test.mjs) was new and filed as apra-fleet-qk6q. The known real-bd long-pole performance issue (48/196 files over the 300s single-file budget) was also reconfirmed on the existing tracking bead apra-fleet-eft.17. Part 2 (sandbox smoke test): Setup completed successfully at branch HEAD (fresh install v0.4.1_ac9ee1, server verified up on port 18700, toy repo cloned, git identity seeded, sandbox beads DB seeded and isolation-verified) after first cleaning up unrelated stale leftover state from a much older abandoned session (orphaned server on 18700, stray toy dev-server on 3001, matching known beads apra-fleet-yru/apra-fleet-cgc5, both reconfirmed). However, Test scenario step 3a (credential provisioning) was denied twice in a row by the Claude Code auto-mode classifier despite full static permission coverage; per this repo's CLAUDE.md policy against routing around permission blocks, no workaround was attempted, so steps 4-6 (member provisioning, real Planner dispatch against the canary, closure/version assertion) never ran -- filed as new standalone bead apra-fleet-j48h. Teardown (stop server, delete sandbox) was run and verified clean regardless. This entire result is informational: it does not gate the current sprint's PASS/FAIL verdict, and both newly filed bugs (plus the reconfirmed pre-existing ones) carry over to a future sprint to be picked up and fixed there.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
