# Sprint Analysis: fix/fleet-sprint-stabilization

Scope issue id(s): apra-fleet-xuo.
Base branch: chore/integration-binary-fixes-and-auth-selfheal.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [19, 26, 34, 39, 41].
High-water-mark closed count this sprint: 41.
Final closed count: 41.
Final open-at-goal-priority count: 21.

## Deploy/Integration outcomes

Deploy failures (1): C1: Step 0 permission check failed: .claude/settings.json has no permissions.allow list at all (content is {"attribution":{"commit":"","pr":""}}). Missing permissions required by deploy.md's ## Permissions section: Bash(gh run *), Bash(gh release *), Bash(mkdir *), Bash(rm -rf /tmp/fleet-deploy*), Bash(chmod +x /tmp/fleet-deploy*), Bash(*apra-fleet-installer-* install *), Bash(*apra-fleet* --version), Bash(npm ci), Bash(npm run build), Bash(npm run build:binary), Bash(dist/apra-fleet-installer-* install *). No deploy commands were executed. Add these to the permissions allowlist and re-trigger the sprint.
Integration test failures (4): C2: No open type=feature beads were in scope this cycle (featuresClosed: 0, as expected for an empty scoped list). Ran integ-test-playbook.md's two-part pass. Part 1 (real functional suite, packages/apra-fleet-se against real bd via scripts/run-integ-suites.mjs): resumed a crashed/stale run from 2026-07-30 (14 of 142 files were pending), so only those 14 were freshly measured this cycle; the other 128 recorded results (and most of the 35 files flagged by check-integ-suite-budget.mjs as over the 300s budget) are carried over, not fresh. Final line: 'pass COMPLETE: every discovered file has a recorded result (2 failure(s))', elapsedWall=480s, cumFileTime=23355s, exit 1. Both failures are freshly-observed regressions and were filed: apra-fleet-xuo.6 (ou7.3-sprint-log-integration.test.mjs -- a nonzero-exit child no longer leaves a traceable log/ledger entry) and apra-fleet-xuo.7 (f34-concurrent-launch-engagement-integration.test.mjs -- concurrent real-supervisor topology sees 0 reviewer-newTask child beads and an HTTP-mutex subtest fails). The pre-existing budget bug apra-fleet-eft.17 was updated (not recreated) with this cycle's re-confirmation, noting the staleness caveat. Part 2 (smoke test): Setup ran and was verified against the deploy-verified SHA 4f3e45457c54f3700dbb0843caf660e78aefe973 (install banner printed 'v0.4.0_4f3e45' after a fresh npm run build), server booted, toy repo cloned, and the sandbox-seed-beads.mjs seed step completed cleanly (no ENOENT), with its own path guards printing 'all paths inside the sandbox root' -- positive evidence of sandbox isolation. However, the playbook's dedicated isolation-verification script (scripts/check-sandbox-sync-remote.mjs) was found to silently no-op on this Windows host regardless of arguments (its entrypoint guard still uses the pre-fix 'file://${process.argv[1]}' raw-string comparison, which never matches the real import.meta.url on Windows), so that verification step did not actually execute -- filed as apra-fleet-xuo.8. The smoke scenario could not proceed past Test-scenario step 3a: the runner's ambient Claude credential has an empty claudeAiOauth.accessToken and CLAUDE_CODE_OAUTH_TOKEN is unset, so no token could be resolved for member provisioning; per the playbook this is the designed fail-loud outcome, not a defect, and no bug was filed for it. Steps 4-5 (real Planner dispatch against the canary, closure/version-flag assertion) therefore never ran this cycle -- part 2 is INCONCLUSIVE, not a pass; remediation is to run /login or export CLAUDE_CODE_OAUTH_TOKEN in a live session and re-run. Teardown was run regardless (server stopped, sandbox deleted) and confirmed. Separately, per the dispatch prompt's verification-closure request: apra-fleet-2cc (Windows execFileSync bd-shim bug, all task children closed) was checked against three call sites. Site 1 (scripts/sandbox-seed-beads.mjs) and Site 3 (scripts/check-toy-doer-credentials.mjs's entrypoint guard) were confirmed fixed via live runs this cycle plus tests/2cc-win-bd-invocation-integ.test.ts (5/5 passing, run this cycle). Site 2 (scripts/check-sandbox-sync-remote.mjs) could not be verified precisely because of the xuo.8 guard defect described above -- main() never runs there, so the underlying execBdSync fix inside checkDoltRemoteAbsent() is unreachable/unverifiable from the CLI, and the class of defect (a Windows verification script silently not doing its job) still reproduces. apra-fleet-2cc was therefore left open (not closed) with detailed notes; a bd close/reopen slip was made and corrected mid-session, with the correction logged on the bead. (bugs filed: apra-fleet-xuo.6, apra-fleet-xuo.7, apra-fleet-xuo.8) | C3: No open features were in scope this cycle (empty list per dispatch), so only the standing playbook pass and the three named awaiting-verification bugs were worked. Part 1 (real-bd apra-fleet-se suite via scripts/run-integ-suites.mjs, joined mid-run and polled to completion): 142/142 files PASS, 0 failures, elapsedWall=3847s, cumFileTime=25854s. 37 files still exceed the 300s single-file budget (worst f34-concurrent-launch-engagement-integration.test.mjs at 1669s) -- pre-existing, already tracked as apra-fleet-eft.17, updated with this cycle's numbers rather than duplicated. Verification of the three awaiting-closure bugs, all closed with named evidence: apra-fleet-xuo.7 (f34-concurrent-launch-engagement now passes, 2+ reviewer-newTask child beads and HTTP mutex subtest both green; fix commits predate the run), apra-fleet-xuo.6 (ou7.3-sprint-log-integration now passes, ledger/history CHILD_EXITED ordering fixed; fix commit d5d9381 predates the run), apra-fleet-2cc (Windows execFileSync/entrypoint-guard fix verified two ways: tests/2cc-win-bd-invocation-integ.test.ts 5/5 pass on this Windows runner, and independently confirmed live in the real playbook's Reset path -- sandbox-seed-beads.mjs completed with zero ENOENT and check-sandbox-sync-remote.mjs's main() actually executed rather than silently exiting). Along the way, a separate still-open Windows path bug (apra-fleet-xuo.9, resolvesInsideSandbox() mis-resolving file:// URLs with drive letters) reproduced again during the Reset's verify step; left open (already tracked, not a 2cc regression) after manually confirming the sandbox isolation invariant it checks (sync.remote and git origin) genuinely held. Part 2 (smoke test): repo HEAD was verified to equal the deployed SHA d5d93815cc3787e3cc6c07cde2c7a0d6c66f1ee9 before starting; ran Reset (sandbox pre-existed) successfully, confirmed the server up on port 18700 reporting version v0.4.0_d5d938, but ABORTED at Test-scenario step 3a: no live ambient Claude credential was available to provision the toy-doer member (CLAUDE_CODE_OAUTH_TOKEN unset, and the runner's own $REAL_HOME/.claude/.credentials.json has an empty accessToken/refreshToken) -- an unprovisioned runner session, not a product defect, so per the playbook's own fail-loud step this was not worked around and no bug was filed for it. Teardown was run regardless (server stopped, sandbox removed). Because Part 2 did not complete a fresh smoke-test pass against the deployed SHA this cycle, the overall pass is reported as not passed; remediation is for an operator to run '/login' or export CLAUDE_CODE_OAUTH_TOKEN before Part 2 can be re-run. (bugs filed: none) | C4: INCONCLUSIVE (part-2/smoke evidence not verified fresh -- the report carries no deployedSha (nor legacy PART2_SHA marker)): No open type=feature beads were in scope this cycle. For the two awaiting-verification bugs, I re-ran integ-test-playbook.md's Setup-through-seed steps fresh at deploy-verified SHA b1a4762db804c429e5d44fdacc9ad09dc8538bb0 (HEAD matched exactly) and confirmed scripts/check-sandbox-sync-remote.mjs now prints all checks OK and exits 0 on Windows -- both apra-fleet-xuo.8 (entrypoint guard never ran main()) and apra-fleet-xuo.9 (file:// drive-letter/MSYS resolution false-FAIL) no longer reproduce; both were already closed with matching evidence and were left as-is. Part 1 (real-bd apra-fleet-se suite) was polled to completion: 142/142 files passed with 0 failures; 35 files exceed the 5-minute single-file budget, which duplicates the already-open apra-fleet-eft.17, so no new bug was filed. Part 2's smoke-test scenario (steps 1-6: member registration through canary sprint) could not be run this cycle -- the ambient Claude credential file has an empty accessToken/refreshToken and CLAUDE_CODE_OAUTH_TOKEN is unset, tripping the playbook's own fail-loud credential guard before registration. This is an environment/credential gap, not a product defect at this SHA, so no bug was filed; Setup was torn down cleanly. Because steps 1-6 did not execute against the deployed SHA, deployedSha is omitted so this is read as inconclusive for the smoke-test portion, not a pass. (bugs filed: none) | C5: No open type=feature beads were in scope this cycle. Part 1 (real functional suite, unmocked bd, node scripts/run-integ-suites.mjs) COMPLETE with 0 failures across all 142 discovered files (elapsedWall=3796s, cumFileTime=25454s), resuming and finishing a run already live at session start. check-integ-suite-budget.mjs still FAILs (35/142 files exceed the 300s single-file budget, worst f34-concurrent-launch-engagement-integration.test.mjs at 1486s) -- this duplicates the existing open P3 apra-fleet-eft.17, which was updated with this cycle's fresh numbers rather than filing a new bead. Part 2 (smoke test) is INCONCLUSIVE, not a pass: the stale sandbox from an earlier interrupted attempt was torn down and Setup was re-run cleanly against deploy-verified SHA b3cfbfa9af799a5f6dcbb7158af7169bc4c87b79 (install printed v0.4.0_b3cfbf matching git rev-parse HEAD), server started on port 18700, toy repo cloned, sandbox beads DB seeded, and sandbox-isolation guard passed -- but Test scenario step 3a (credential provisioning) hard-stopped: no ambient Claude credential is available (CLAUDE_CODE_OAUTH_TOKEN unset; $REAL_HOME/.claude/.credentials.json has empty accessToken and expiresAt:0). Per the playbook's own fail-loud instruction this requires a real '/login' or an exported CLAUDE_CODE_OAUTH_TOKEN before re-running -- not something to synthesize or route around. The canary sprint (steps 4-5) was never dispatched. Teardown was run unconditionally and verified (sandbox directory removed, port 18700 refusing connections). No bug bead was filed for the credential gap since it is an environment precondition, not a product defect. Remediation: provision a live Claude credential for the runner's real session, then re-dispatch for a fresh part 2 run. (bugs filed: none)

## Reviewer-proposed newTask rejections

1 newTask(s) rejected before reaching bd create: C3: title fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/-]+$/ (or is empty): "Fix remaining broken file://${process.argv[1]} entrypoint guard in scripts/check-watchdog-isolation.mjs"

## Final verdict

FAIL -- Local quality gates all PASS: git status --porcelain empty; npm run build exit 0; npm test exit 0 (200 files passed / 7 skipped, 2815 tests passed / 36 skipped, 166s). Diff is 46 commits, 70 files, +8146/-144 against origin/chore/integration-binary-fixes-and-auth-selfheal (note: the base branch exists only as a remote ref; the local name does not resolve). Code quality on the closed work is good: closed children of k7b/f34/ou7/19o/2cc/xuo.* each ship paired [impl]+[test] commits, and file hygiene is clean -- no temp files, stray scripts, or slipped tool config. Two spot-checks cleared: (1) scripts/check-watchdog-isolation.mjs:161 now correctly reads `process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href`, so the C3 REJECTED newTask about that guard is ALREADY SATISFIED by commits 4f2fe43/c06c087/b3cfbfa (xuo.10/xuo.11 closed) and must NOT be re-created; (2) repo-mandated apra-fleet-client parity holds -- packages/apra-fleet-client/src/client/api.mjs:343 doltPushMutex() and :357 childIdAllocator() wire both new tools (src/tools/dolt-push-mutex.ts, src/tools/child-id-allocator.ts). FAIL rests on three sprint-exit failures, not on the merged code. (A) DEPLOY NEVER EXECUTED. C1 failed the Step 0 permission check and the blocker is still present in the tree: .claude/settings.json is literally {"attribution":{"commit":"","pr":""}} with no permissions.allow list at all. No deploy command ran in any cycle; nothing was verified as deployable. (B) INTEGRATION PART 2 (SMOKE) COMPLETED IN 0 OF 4 CYCLES. C2/C3/C4/C5 each aborted at playbook Test-scenario step 3a because the runner has no live Claude credential (CLAUDE_CODE_OAUTH_TOKEN unset; $REAL_HOME/.claude/.credentials.json has empty accessToken, expiresAt:0). C4 omits deployedSha entirely. Steps 4-6 (Planner dispatch against the canary, closure/version-flag assertion) have never run, so there is no end-to-end evidence at any deployed SHA. Part 1 is genuinely green (142/142 files, 0 failures in C3/C4/C5), which is why this is an unverified-release failure rather than a functional regression. (C) SCOPE NOT COMPLETE: 21 beads remain open at/above goal priority P1/P2. Four P1 trees are entirely unimplemented -- apra-fleet-gey (.1-.3 launch-failure fast path/relaunch gate), apra-fleet-vkc (.1-.2 dolt-recovery ladder wire-or-decommission), apra-fleet-n74 (.1-.3 route persona bead creation through the id-allocator), apra-fleet-2mi (.1-.3 dedup evidence before create); apra-fleet-3pb (P2) is also open, and apra-fleet-xuo.1 is in progress. The P0 parents apra-fleet-k7b and apra-fleet-f34 have every child closed and appear to need only parent-closure verification, not further development. Not filed as newTasks (already tracked, must not be duplicated): the 300s single-file budget failure (35/142 files, worst f34-concurrent-launch-engagement at 1486-1669s) is open as apra-fleet-eft.17 and was correctly updated rather than re-filed in C3/C4/C5; all gey/vkc/n74/2mi/3pb work already exists as beads.
