# Sprint Analysis: auto-sprint/gfx2-demo

Scope issue id(s): apra-fleet-9te, apra-fleet-20i.
Base branch: auto-sprint/eft-service.
Cycles run: 2.

## Progress

Closed-bead count history (per cycle evaluation): [7, 13].
High-water-mark closed count this sprint: 13.
Final closed count: 13.
Final open-at-goal-priority count: 1.

## Deploy/Integration outcomes

No deploy failures recorded this sprint.
Integration test failures (2): C1: Full integration pass against deploy-verified SHA 61c4c32c6e4ff387eefdb07849e320c4b20fc7e3 (PART2_SHA: 61c4c32c6e4ff387eefdb07849e320c4b20fc7e3). Permissions check passed (all playbook-required Bash prefixes present in .claude/settings.json). Part 1 (real functional suite via scripts/run-integ-suites.mjs) could not run: --status exited 2 with a stale-status-file condition (leftover integ-suite-status.json from 2026-07-22 referencing two now-removed test files), which per INTEG-SUITE.md step 1's decision tree is the 'corrupt/stale, fail loud' branch rather than a resumable crash, so no real-bd suite files executed this cycle -- filed apra-fleet-9te.2 (P2). Part 2 (smoke test) ran fresh this cycle: Setup succeeded (install, server start on 18700, toy-repo clone + sandbox-local git/Dolt remote isolation verified, member registration, credential provisioning both verified green), and step 2's canary gh-toy-4ef was confirmed open, but step 4 (`workflow auto-sprint`) failed immediately with 'workflow "auto-sprint" not found' -- root-caused by inspection to two stale `vendor/apra-pm` path references left over in src/cli/install.ts's buildDevManifest() from the apra-pm submodule-to-package-local-copy reorg (every other apra-pm reference in that file was updated to packages/apra-fleet-se/apra-pm; these two were missed), so agentSchemas never resolves and the whole workflow-runtime install step silently skips. Filed apra-fleet-9te.3 (P1). Teardown ran regardless and the sandbox was fully removed. Independent of the smoke test, both scoped features' own dedicated test tasks were run directly and passed: apra-fleet-20i.1's workflow-log-label.test.mjs (2/2) and apra-fleet-9te.1's tests/readme-routing.test.ts (5/5) -- both features closed clean. Overall passed=false because part 1 recorded a failure and a part-2 bug was filed, even though both in-scope features individually verified green. (bugs filed: apra-fleet-9te.2, apra-fleet-9te.3) | C2: INCONCLUSIVE (part-2/smoke evidence not verified fresh -- the report carries no deployedSha (nor legacy PART2_SHA marker)): Continued and completed this cycle's integ-test-playbook.md pass (deploy-verified SHA daa4faa1206ce692e47cb0b93c83ddf5517ff110), resuming from an earlier turn in the same session rather than rebuilding the sandbox. No open type=feature beads were in scope this cycle, so no features were tested/closed. Part 1 (real-bd suite, packages/apra-fleet-se, 106 files) had already completed earlier this cycle with exit code 1: exactly one failure, mock-sprint-planner-auth-failure-no-retry.test.mjs, which is a pre-existing tracked bug (apra-fleet-eft.54, out of this sprint's scope) already annotated this cycle confirming it still reproduces -- no new bug needed there. Part 2 (smoke test) was (re)run fresh against the deployed SHA this cycle as required: the sandbox (already up from earlier in this session, same SHA) was not rebuilt, but the Test scenario was driven to full completion -- member registration/credentials had already been provisioned, and I ran `workflow auto-sprint` to finish the toy canary sprint (gh-toy-4ef), which reached a PASS verdict end-to-end (Plan -> Develop -> Review -> Deploy -> Integ Test -> Final Review -> Harvest -> Publish), closing the canary and its subtasks, independently verified by running the built CLI's `--version` flag (printed 'fleet-e2e-toy v1.0.0', exit 0). Along the way this run surfaced and evidenced a real, reproducible P1 defect in the auto-sprint engine's own 'Ensure Sprint Branch' phase: when a sprint branch already exists locally (from an earlier interrupted cycle) but hasn't been pushed to origin, the engine resets it to base, silently discarding already-closed work (confirmed via git reflog: 'Reset to origin/main' immediately following the discarded impl commit; the doer's own recovery notes referenced multiple other commit hashes across prior canary runs, indicating this has recurred repeatedly) -- filed as apra-fleet-9te.4 (P1), parented under apra-fleet-9te. Both named verification-closure bugs were resolved: apra-fleet-9te.2 was already closed in an earlier turn this cycle (confirmed, no action needed); apra-fleet-9te.3 (workflow subsystem install/resolution failure) was independently reverified not to reproduce at this SHA -- clean install with no 'no workflow-subsystem assets' warning, workflows populated, and `workflow auto-sprint` resolved and ran to completion -- and closed with evidence. Teardown ran cleanly (server stopped, sandbox deleted) after all testing concluded. `passed` is false because a new bug (apra-fleet-9te.4) was filed this cycle. (bugs filed: apra-fleet-9te.4)

## Reviewer-proposed newTask rejections

None.

## Final verdict

FAIL -- In-scope feature work is complete and clean: apra-fleet-20i.1 (FleetWorkflow optional logPrefix + '=== END scenario ===' markers in packages/apra-fleet-workflow/src/workflow/index.mjs, default '' preserves byte-identical output; test workflow-log-label.test.mjs 2/2 pass), apra-fleet-9te.1 (README /auto-sprint vs /pm routing paragraph; tests/readme-routing.test.ts 5/5 pass), apra-fleet-9te.2 (scripts/run-integ-suites.mjs checkStale() now distinguishes a completed pass with a changed file set -> auto-recover as fresh, vs a live/crashed run -> fail loud; regression-guarded), and apra-fleet-9te.3 (src/cli/install.ts: all stale vendor/apra-pm paths, including the previously-missed agentSchemasDir at former line 291, now point at packages/apra-fleet-se/apra-pm; empty-submodule guard removed appropriately). Build passes; full unit suite 191 passed / 5 skipped / exit 0. FAIL is driven by scope status, not by the delivered code: apra-fleet-9te.4 (P1, child of in-scope parent apra-fleet-9te) remains OPEN -- a reproducible auto-sprint-engine defect where the 'Ensure Sprint Branch' phase, after a failed `git fetch origin <branch>` for a local-only unpushed sprint branch, resets that branch to origin/main and silently discards already-committed, already-closed work (confirmed via reflog 'Reset to origin/main' following the discarded impl commit; doer recovery notes reference multiple lost commit hashes across prior canary runs, indicating recurrence). Integration tests failed in both cycles (C1 filed 9te.2+9te.3, since fixed and closed; C2 filed 9te.4, still open). Per contract, cannot PASS with an open P1 goal-priority bead plus integration failures. The blocker is already tracked as apra-fleet-9te.4, so it is not duplicated as a new task -- it must be resolved (or explicitly de-scoped by the orchestrator) before this scope can pass. Minor hygiene (non-blocking, not in committed branch tree): reviewer working tree carries untracked scratch files (.gemini/, remote-planner*.jsonl) that are not part of any sprint commit.
