# Sprint Analysis: feat/supervisor-beads-sort-created-at

Scope issue id(s): apra-fleet-qoxd.
Base branch: main.
Cycles run: 1.

## Progress

Closed-bead count history (per cycle evaluation): [4].
High-water-mark closed count this sprint: 5.
Final closed count: 4.
Final open-at-goal-priority count: 0.

## Deploy/Integration outcomes

No deploy failures recorded this sprint.
Integration test failures (1): C1: No open type=feature beads were in scope this cycle (explicit empty list -- nothing to test/close via the normal per-feature procedure). One verify-set bead was named for verification-closure: apra-fleet-qoxd (fleet-supervisor Backlog view: sort by created_at), whose 3 children (2 impl + 1 test task) were already closed. Verified against the sprint branch working tree (feat/supervisor-beads-sort-created-at, HEAD f68ef533): ran the feature's own test file (node --test test/supervisor-backlog.test.mjs in packages/apra-fleet-se) -- 42/42 pass, covering every acceptance-criteria case named in apra-fleet-qoxd.3 (asc/desc created_at ordering, missing/invalid timestamps sort last with no throw, omitting sort preserves prior behavior/contract, buildBacklogTasks route wiring, and the rendered header's Created-at sort control with the header splicing exactly once). Also ran the full packages/apra-fleet-se package suite per the qoxd.1/qoxd.2 'npm test stays green' AC: 2044 tests, 2041 pass, 1 fail -- the failure (contracts-schema-dist-staleness-guard.test.mjs) is a pre-existing, unrelated dist/agents/schemas drift (integ-test-runner-output.json and regression-test-runner-output.json out of sync with their apra-pm source, last touched in an older commit 917ab8f2) with no connection to the backlog sort feature, so it did not block closure -- filed as apra-fleet-qoxd.4 [integ] (P3) under --parent apra-fleet-qoxd. The feature itself behaves exactly as specified, so apra-fleet-qoxd was closed with a note citing the commands run and observed output. A broader, non-required full-monorepo npm test run (started as extra diligence) also surfaced a transient-looking failure in tests/knowledge/kb-remote-anchor-freshness.test.ts; re-run in isolation it passed cleanly (4/4), confirming it was a concurrency-related flake under the monorepo's parallel run rather than a real regression, so no bug was filed for it. This repo's integ-test-playbook.md is a repo-local, per-feature/verify-set procedure only (no Setup/Reset/Teardown/smoke-test section -- that lives in regression-test-playbook.md run once per sprint by a different role), so no sandbox lifecycle was performed here. passed is reported false because one bug (apra-fleet-qoxd.4) was filed during this run, even though it is unrelated to the verify-set target, which itself verified clean and was closed. (bugs filed: apra-fleet-qoxd.4)

## Reviewer-proposed newTask rejections

1 newTask(s) rejected before reaching bd create: C1: title fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/+[\]-]+$/ (or is empty): "Backlog header: 7 <th> sort cell has no matching data-row column (alignment)"

## Final verdict

PASS -- Reviewed the net diff for the qoxd verify-set (fleet-supervisor Backlog: sort by created_at). The sprint's own commits (df0cc484, 9d2b9959, f68ef533) are cleanly scoped to exactly two files -- packages/apra-fleet-se/src/supervisor/backlog.mjs (+99) and its test (+107); the other 250+ files in main..branch are pre-existing merged history (gemini removal, register-member, stall-detector, workflow pause-resume, etc.), not artifacts of this sprint. All three closed children verify against their acceptance criteria: (qoxd.1) applyBeadFilters() now reads filters.sort/dir, sorts via a decorate-sort-undecorate over the original index so it is stable and never mutates order for equal timestamps; parseCreatedAt() defensively handles missing/non-string/unparseable created_at (returns null -> sorts last both directions, never throws); route wiring in registerBacklogRoutes threads sort/dir from url.searchParams into buildBacklogTasks; omitting sort leaves the {tasks,total,filterOptions} contract untouched. (qoxd.2) buildFilterHeaderRowHtml adds a 7th <th> with data-sort-field='created_at' offering Unsorted/Created(newest)=desc/Created(oldest)=asc, pre-selected from currentFilters; wireHeaderControls sets/clears both sort+dir off one select and calls applyFilters(); applyFilters() emits sort/dir in the URLSearchParams generically; clear-filters resets currentFilters={}; injectFilterHeader's {6}-<th> regex still matches the ORIGINAL renderBeadsHtml header so it splices exactly once even though the replacement carries 7 cells. (qoxd.3) supervisor-backlog.test.mjs adds well-targeted cases for asc/desc ordering, missing+invalid-sort-last for both directions with doesNotThrow, contract-intact when sort omitted, and end-to-end buildBacklogTasks route wiring via an injected listAllBeads fixture. Verification run locally: node --test supervisor-backlog.test.mjs = 42/42 pass; npm run build (contract + tsc) clean; full package npm test = 2044 tests, 2041 pass, 1 fail. The single failure is contracts-schema-dist-staleness-guard.test.mjs (DRIFTED: integ-test-runner-output.json, regression-test-runner-output.json), a pre-existing dist/agents/schemas drift from commit 917ab8f2 that predates and is untouched by the qoxd commits (confirmed via git diff 33aae6ca..f68ef533 -- dist/ = empty). It is already tracked as the open P3 bead apra-fleet-qoxd.4 (fix: npm run dist-pm then commit refreshed dist/) and is below the P1/P2 sprint goal, so it does not block this epic. No qoxd child was closed on insufficient evidence, so no reopens. Two minor secondary findings filed as newTasks below. Note: working tree has an untracked local backup dir .beads.bak-preclone-20260822/ -- a local DB backup, not a committed sprint artifact.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-5ey2, apra-fleet-5jlr.
Summary: Ran both parts of regression-test-playbook.md at branch HEAD (f68ef53). Part 1: the real-bd suite (scripts/run-integ-suites.mjs, 201 files) completed with 4 recorded failures (f34-concurrent-launch-engagement-integration.test.mjs, mock-sprint-kb-remote-scope.test.mjs, mock-sprint-member-vcs-provider-threading.test.mjs, mock-sprint-publish-push-failure.test.mjs) plus the long-standing single-file budget violation (48/201 files over 300s, tracked in apra-fleet-eft.17); the slow lane (npm run test:slow) also failed 1 of 2 tests (mock-sprint-planner-dispatch-stalled-session.test.mjs -- the watchdog itself fired correctly but the test then failed on stale bd-replay fixture recordings). Three of the real-bd failures matched existing closed carry-over beads (apra-fleet-mmxx, apra-fleet-qk6q, apra-fleet-5i08, all previously triaged as suite-maintenance noise per the once-per-sprint cadence decision apra-fleet-040) and were updated with fresh evidence rather than duplicated; two genuinely new standalone parent-less carry-over beads were filed (apra-fleet-5ey2 for the previously-untracked vcs-provider-threading timeout, apra-fleet-5jlr for the slow-lane fixture-drift failure). Part 2: Setup completed cleanly (install, server start/verify on port 18700, toy-repo clone, git identity seed, sandbox beads seed, and sandbox-isolation guard all passed) and the canary gh-toy-4ef was confirmed open, but the Test scenario's step 3a (credential provisioning) was denied by the Claude Code auto-mode classifier -- an exact, ongoing reproduction of the already-tracked apra-fleet-j48h (updated with today's evidence) -- so steps 1/3b/4/5 never ran and the toy sprint itself was never exercised this pass. Teardown ran and was verified afterward regardless. This result is informational only and does not gate the current sprint's verdict; all filed/updated bugs carry over to a future sprint.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
