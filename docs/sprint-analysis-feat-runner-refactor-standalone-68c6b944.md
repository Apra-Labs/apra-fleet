# Sprint Analysis: feat/runner-refactor-standalone

Scope issue id(s): apra-fleet-3swo.
Base branch: main.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [36, 41, 45, 46, 46].
High-water-mark closed count this sprint: 50.
Final closed count: 46.
Final open-at-goal-priority count: 14.

## Deploy/Integration outcomes

Deploy failures (5): C1: Deploy stopped at `install --force`. Preceding steps all succeeded: preflight-clear-build-locks.mjs found nothing locked; `npm ci` installed 243 packages cleanly; `npm run build` and `npm run build:binary` both completed, producing dist/apra-fleet-installer-darwin-x64. Active-sprints gate passed: `curl http://localhost:8787/api/sprints` showed exactly one live reservation, sprintId=apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6, which is an exact match for this sprint's own dispatch-supplied sprintId; `node scripts/check-foreign-sprints.mjs --self-sprint-id apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6` printed 'own reservation (not foreign)' and 'proceed: 1 own reservation(s), 0 foreign' and exited 0, so proceeding was correct. Failing command: `dist/apra-fleet-installer-darwin-x64 install --force` (exit 1). Output: 'Error: could not stop the running apra-fleet server (it is still running after SIGTERM and a SIGKILL escalation). Stop it manually before installing: pkill -x apra-fleet'. Diagnosis: `ps aux` shows PID 22897 `/Users/akhil/.apra-fleet/bin/apra-fleet --transport http` (started ~3:36AM, state S, not a zombie/uninterruptible process) still alive after the installer's own SIGTERM+SIGKILL escalation -- unusual since SIGKILL normally always succeeds against a live, non-D-state process; worth investigating why the installer's kill logic didn't take effect. deploy.md's Deploy section does not document a manual pkill step as part of the runbook, so per 'Do NOT attempt to fix or work around failures' I did not run `pkill -x apra-fleet` myself and did not retry install. No smoke test was run. Suggested next step for the operator/orchestrator: manually verify/stop PID 22897 (or investigate why SIGKILL didn't terminate it) and re-trigger the deploy; deploy.md's own permissions list already covers pkill (`Bash(pkill:*)`) if the runbook is updated to document this as an explicit recovery step. No KB capture tools were available this session (no ToolSearch tool in this dispatch's toolset), so this finding is reported here in notes instead. | C2: Deploy stopped at the install step. Ran preflight-clear-build-locks.mjs (no locks found), npm ci (success, 243 packages), npm run build (success), npm run build:binary (success, produced dist/apra-fleet-installer-darwin-x64). Active-sprints gate passed cleanly: GET /api/sprints showed exactly one live reservation with sprintId 'apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6', which exact-matches this sprint's own supervisor reservation id given in the dispatch prompt; scripts/check-foreign-sprints.mjs --self-sprint-id confirmed 'proceed: 1 own reservation(s), 0 foreign' with exit 0. Then ran: dist/apra-fleet-installer-darwin-x64 install --force -- this failed with exit code 1 and output: 'Error: could not stop the running apra-fleet server (it is still running after SIGTERM and a SIGKILL escalation). Stop it manually before installing: pkill -x apra-fleet'. This recovery step (pkill -x apra-fleet) is not part of deploy.md's ## Deploy section, so per the 'do not attempt to fix or work around failures' rule I did not run it myself and stopped here instead of proceeding to install/launch/smoke-test. Smoke test was not run. Non-obvious gotcha worth capturing in KB/runbook: `install --force`'s own SIGTERM+SIGKILL stop sequence for the singleton apra-fleet server can still fail to actually kill the process, requiring a manual `pkill -x apra-fleet` before retrying install -- deploy.md does not currently document this failure mode or fallback. Recommend the orchestrator/operator run `pkill -x apra-fleet` (or investigate why the process resists SIGKILL) and then re-trigger this deploy sprint. | C3: Deploy stopped at the install step. Active-sprints gate passed cleanly first: GET http://localhost:8787/api/sprints showed exactly one reservation (sprintId apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6, childPid 85697), which matches this sprint's own dispatched sprintId exactly; node scripts/check-foreign-sprints.mjs --self-sprint-id "apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6" confirmed 'proceed: 1 own reservation(s), 0 foreign' (exit 0). Preceding build steps all succeeded: node scripts/preflight-clear-build-locks.mjs (no locks held), npm ci, npm run build, npm run build:binary all completed cleanly, producing dist/apra-fleet-installer-darwin-x64. Failing command: dist/apra-fleet-installer-darwin-x64 install --force. Exit code: 1. Output: 'Error: could not stop the running apra-fleet server (it is still running after SIGTERM and a SIGKILL escalation). Stop it manually before installing: pkill -x apra-fleet'. Per deploy protocol I did not run pkill or any other workaround -- I am stopping and reporting so the orchestrator/operator can decide how to handle the stuck apra-fleet process (note this may be the currently-running fleet server backing this very session, so killing it has broader consequences than a routine failure). No install, launch, or smoke test was performed. | C4: Deploy stopped at `dist/apra-fleet-installer-darwin-x64 install --force` (exit 1). Build steps all succeeded (preflight-clear-build-locks.mjs exit 0/no locks; npm ci; npm run build; npm run build:binary -> produced dist/apra-fleet-installer-darwin-x64). Active-sprints gate cleared: GET http://localhost:8787/api/sprints showed only one live reservation, sprintId=apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6, exactly matching my own dispatching sprint id; node scripts/check-foreign-sprints.mjs --self-sprint-id "apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6" exited 0 ('proceed: 1 own reservation(s), 0 foreign.'), so no foreign sprint was blocking. The installer itself then failed: 'Error: could not stop the running apra-fleet server (it is still running after SIGTERM and a SIGKILL escalation). Stop it manually before installing: pkill -x apra-fleet'. Confirmed via ps aux that PID 1159 (/Users/akhil/.apra-fleet/bin/apra-fleet --transport http, started 8:13AM) is still running and resisted the installer's own kill escalation. Per deploy protocol I did not run pkill or any other workaround -- that step is not part of deploy.md's Deploy section -- and did not proceed to launching the server or the smoke test. Operator action needed: manually stop the stuck apra-fleet process (PID 1159, or `pkill -x apra-fleet` as the installer suggests) and re-run `install --force`, then continue with the launch + smoke-test steps of deploy.md. | C5: Build succeeded (preflight-clear-build-locks, npm ci, npm run build, npm run build:binary all exit 0; dist/apra-fleet-installer-darwin-x64 produced). Active-sprints gate passed: GET http://localhost:8787/api/sprints showed only my own reservation (sprintId apra-fleet-3swo-3f4e996d-d5ec-4990-8a6d-42248d9a9ca6, matches dispatch prompt exactly); node scripts/check-foreign-sprints.mjs --self-sprint-id confirmed '1 own reservation(s), 0 foreign', exit 0. Deploy failed at the install step: `dist/apra-fleet-installer-darwin-x64 install --force` exited 1 with 'Error: could not stop the running apra-fleet server (it is still running after SIGTERM and a SIGKILL escalation). Stop it manually before installing: pkill -x apra-fleet'. Per deploy operating rules I did not run the suggested pkill workaround (not part of the documented ## Deploy steps) and stopped immediately without running the Smoke test. This looks like a stuck/unkillable apra-fleet server process (separate from the supervisor sprint's own childPid 85697) blocking install --force; an operator should investigate and manually stop the process (or confirm it's safe to pkill) before this sprint retries the deploy. KB note worth capturing: install --force can fail even after gate passes when the running fleet server (port 7523) does not die on SIGKILL escalation -- deploy.md's remediation text (pkill -x apra-fleet) is emitted by the installer itself but is not part of the runbook's own Deploy steps, so a deployer agent following 'never work around failures' will correctly stop here rather than pkill.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

None.

## Final verdict

FAIL -- SCOPE REALITY. apra-fleet-3swo is an epic its own description scopes at 5-6 sprints; this sprint closed 9/16 children. Verified against the tree, not bead counts: Phase T (.1), Phase 0 (.2), Phase 1 (.3) and Phase 2 (.4) have all leaves closed and are genuinely reflected in the diff, but Phase 3 (.5) is only 3/8 done (.5.3/.5.4/.5.5/.5.7 open, .5.8 in_progress even though its commit 63e0132d is on the branch), and Phase 4 (.6, 9 leaves) and Phase 5 (.7, 10 of 14 leaves) are largely untouched. dispatchRole does not exist anywhere in fleet-sprint/ -- role-policies.mjs is consumed only by tests and inline-ladder-guard.mjs, and that guard is inert by construction today because migratedRoleNames() returns []. That is a legitimate staged step, not a completed Phase 3.

DEPLOY NEVER PASSED, SO NOTHING WAS VERIFIED. All 5 cycles stopped at `dist/apra-fleet-installer-darwin-x64 install --force`, so no smoke test and no IntegTest ran, and the four verify-routed parents (.1-.4) were never confirmed against a deployed build. I traced the root cause rather than accepting the deployers' 'SIGKILL did not work' diagnosis, and they were misreading it: ~/Library/LaunchAgents/com.apra-fleet.server.plist carries KeepAlive/SuccessfulExit=false, so launchd relaunches the server the instant it is SIGKILLed. ~/.apra-fleet/data/fleet.log shows successive `startup ... transport=http port=7523` lines with NEW pids 22897, 95298, 63496, 1159, 27537 -- exactly the pids the five deploy reports quoted as 'still alive'. src/cli/install.ts:1023-1035 calls killApraFleet()/waitForApraFleetToStop(), which only run `pkill -x` / `pkill -9 -x` (install.ts:717-726) and never `launchctl bootout` via getServiceManager().stop(), so isApraFleetRunning() sees the relaunched process and the install exits 1. install.ts is untouched by this branch, so this is pre-existing, but it is the direct cause of the sprint's verification gap. Filed as a P1 task.

CODE REVIEW OF THE NET DIFF (main..feat/runner-refactor-standalone, 54 commits, 113 files). Quality is high. runner.js drops 11678 -> 7772 lines (467,064 bytes) with 59 documented facade re-export blocks; every extracted module has a facade-completeness or extraction-behaviour test (phase0-seams-facade, phase1-leaf-facade-completeness, vcs-auth-extraction-facade, coordination-extraction, beads-scope-extraction, beads-transitions-extraction, kb-extraction-facade). git-sync.mjs's withOpenSyncBracket decrements in `finally` on every exit path and its per-key LIFO stack is falsifiably tested (nesting, overlap, different-key, pushCode:true vs false). beads-transitions.mjs's normalizeGoalMax correctly coerces 'P2' -> 2 and degrades an unparseable ceiling to the historical never-skip (fail-open), preserving the documented fail-open-on-allowlist-throw contract. The golden transcripts are NOT a discipline breach: mock-sprint-3bead.jsonl changes only seq numbers and mock-sprint-happy-path.jsonl's only content delta is `bd config get sync.remote --json` dropping 24 -> 12 occurrences, which is exactly the merged #468 probe memo. File hygiene is clean: 46 added files, all justified, no temp/scaffold/tool config, no deletions.

DEFECT IN CLOSED WORK (reopening .7.2). src/services/cloud/lifecycle.ts:44 and :63 now read result.structuredContent.ok, but tests/cloud-lifecycle-unit.test.ts:23 and :100-101 still stub provisionAuth/provisionVcsAuth with the OLD string return ('auth provisioned'). Dereferencing .ok on undefined throws, the throw is swallowed by the surrounding best-effort catch, and the suite run I did emits the proof: '[cloud] provision_llm_auth failed for test-agent: Cannot read properties of undefined (reading ok)' -- twice -- while tests/cloud-lifecycle-unit.test.ts still reports 9 passed. So the new structured branch in the ONLY server-side consumer of these tools has zero real coverage, and unlike fleet-sprint/vcs-auth.mjs:163-165 (which guards `result && result.structuredContent`) the server-side consumer is non-defensive.

CONVENTIONS. No bead id appears in any LLM-facing string: every apra-fleet-XXXX occurrence in src/, fleet-sprint/ and prompts.mjs is inside a comment; the only string-literal hits are test describe() titles and docs. packages/apra-fleet-client was updated in the same change for all three new/changed tools (api.mjs memberReservation:599, vcsCredentialExec:650, plus docs/api-reference.md and docs/mcp-tools.md), satisfying the client-parity rule. vcs_credential_exec is sound: it refuses any command lacking the {{vcs_token}} placeholder (so it cannot degrade into a second unguarded execute_command), escapes per isPosixShell(agentOs, agentShell) rather than assuming POSIX, logs only the pre-substitution form, and redacts the token from stdout/stderr plus the thrown-error message. No secrets in code, no injection path found.

GATES. git status clean; npm run build exit 0; no lint script configured; npm test exit 0 (vitest 316 files passed / 6 skipped in 294s; apra-fleet-se 2771 tests, 2767 pass, 4 skipped, 0 fail; SUMMARY pass=3194 fail=0).

KB. Promoted 4 entries I verified myself this review (details in kb_promotions). Several INFERRED entries now CONTRADICT the tree and were deliberately left unpromoted -- the code wins: 65e6e503 (provision failures detected by retired emoji) is fixed by 9d5b686e, vcs-auth.mjs now branches on structuredContent.ok/.reason; 7dd3a95e (kb_query isError is silent) is fixed by 99dbb879, kb.mjs:284 logs it; ce1db0b4 (kb log lines unasserted) is fixed, runner-kb-priming.test.mjs:443/569/601 now assert the exact log strings; 3d6d4eb4 (no guard scans for a bare push call) is superseded by unbracketed-push-guard.mjs, which is registered on the shared list with git-sync.mjs exempted; 5f366924 (pins hard-code RUNNER_PATH) is superseded by the module-aware moduleSetSource scanner; 8fd19801 (runner.js above the 512KB GitNexus threshold) no longer holds at 467,064 bytes.

VERDICT RATIONALE. The work that landed is well-tested and I can point at the lines that implement each closed bead, but the sprint goal is unmet: P1 Phase 3 leaves plus all of Phase 4 remain open, the four verify-routed phase parents were never validated against a build, and the deploy gate failed in all five cycles. PASS is not available on that evidence.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-3yuu, apra-fleet-jz2m.
Summary: Ran the full regression pass at branch HEAD c6b37e45ef5fb2e0b58b138d8c117fa8ea4a25b3. Part 1 (real-bd suite via scripts/run-integ-suites.mjs): 242/242 files completed, 1 failure (phase1-leaf-facade-completeness.test.mjs, nested spawnSync ETIMEDOUT against golden-transcript/mock-sprint subprocess suites -- new, filed as apra-fleet-3yuu); check-integ-suite-budget.mjs also failed (22/242 files over the 300s budget, same long-standing performance issue -- updated existing apra-fleet-eft.17, no new duplicate). Part 1's slow lane (npm run test:slow, 2 files): dispatch-watchdog-timer-ref.test.mjs passed; mock-sprint-planner-dispatch-stalled-session.test.mjs failed with the same recurring bd-replay recording-drift signature after its watchdog correctly fired at ~450s -- updated existing apra-fleet-5jlr, no new duplicate. Part 2 (sandbox smoke test): Setup completed cleanly (install, server on 18700, toy-repo clone+seed, isolation guard OK, supervisor on 18701). Test scenario got further than most recent prior runs -- steps 1/2/3a/3b (member registration, canary confirmed open, credential provisioning) all succeeded this cycle (the classifier block that stopped step 3a in many prior cycles, apra-fleet-j48h, did not reproduce this run) -- but step 4's real sprint dispatch failed fast with 'Authentication failed on toy-doer', traced to the operator's ambient Claude OAuth token being ~44h expired (present but stale, not empty) both on the real host and in the copy step 3a seeded into the sandbox. This is the same known environment-credential-preflight-gap class already tracked by apra-fleet-xuo.13 (updated with this cycle's fresh evidence, no new duplicate). Step 5's assertions failed as a direct consequence: canary gh-toy-4ef remains open, toy repo HEAD unchanged at 608c28e1bc878c542d8e2764cfb06fa905204ac1, no version-flag commit landed. Teardown ran to completion and fully cleaned up (supervisor stopped at 295s uptime, lock released, server stopped, sandbox rm -rf'd, no leaked processes or ports) -- but its dolt-sql-server reap step (scripts/reap-sandbox-dolt.mjs) itself hard-failed on this macOS host with 'ps: etimes: keyword not found' (a GNU/Linux-only ps keyword with no BSD/macOS equivalent), a real cross-platform bug in the Teardown safety net -- filed as apra-fleet-jz2m (new). This result is entirely informational: it does not gate the current sprint's PASS/FAIL verdict, and every filed/updated bug carries over as pre-existing breakage for a future sprint to address.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
