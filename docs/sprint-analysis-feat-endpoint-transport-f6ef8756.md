# Sprint Analysis: feat/endpoint-transport

Scope issue id(s): apra-fleet-5se.
Base branch: fix/kb-secure-token-trips-prompt-guard.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [12, 15, 20, 20, 21].
High-water-mark closed count this sprint: 25.
Final closed count: 21.
Final open-at-goal-priority count: 0.

## Deploy/Integration outcomes

Deploy failures (5): C1: Permissions check passed (all required prefixes covered by the merged .claude/settings.json + .claude/settings.local.json allowlist). Deploy section executed through the build steps successfully: node scripts/preflight-clear-build-locks.mjs (no stale locks), npm ci (292 packages installed), npm run build (tsc succeeded), npm run build:binary (SEA binary produced at dist/apra-fleet-installer-linux-x64, version v0.4.2_6245d9). Stopped at the runbook's required pre-install active-sprints check: `curl -s http://localhost:8787/api/sprints` returned a NON-EMPTY sprints array -- one active sprint apra-fleet-5se-2c0e9a66-78b3-4435-9725-a1252cbeb8d0 (member local-apra-fleet, issue apra-fleet-5se, viewer-port 8082) with a live, non-stale child process (PID 1620924, elapsed 01:18:03). deploy.md's own Caution states `install --force` stops the shared singleton fleet server on :7523 and can collaterally kill other supervisors' running sprint child processes. Since this reservation is actively running (not stale) and killing it is not my call to make unilaterally, I did not run the installer, did not restart the fleet server, and did not run the smoke test. Deploy is otherwise ready to complete: just needs someone to either wait for apra-fleet-5se to finish or explicitly authorize force-releasing it, then re-run from the '$INSTALLER install --force' step through the smoke test ('$HOME/.apra-fleet/bin/apra-fleet' --version). | C2: Deploy halted before install/restart. Completed successfully: preflight-clear-build-locks.mjs (no stale locks), npm ci, npm run build, npm run build:binary (produced dist/apra-fleet-installer-linux-x64). Per deploy.md's explicit Caution/STOP instruction, ran the active-sprints check `curl -s http://localhost:8787/api/sprints` before `install --force` and it returned a NON-EMPTY sprints array: {"sprints":[{"sprintId":"apra-fleet-5se-2c0e9a66-78b3-4435-9725-a1252cbeb8d0","members":["local-apra-fleet"],"issueRoots":["apra-fleet-5se"],"childPid":1620924,"port":8082}]}. `install --force` stops the running singleton fleet MCP server (port 7523) that this live sprint's dispatches depend on; restarting it risks collaterally killing that sprint's child processes. I have no authority/tooling to force-release another sprint's reservation, so per the runbook I stopped rather than proceeding with install/launch/smoke-test. No source files were modified (git status clean aside from pre-existing untracked log files). To finish this deploy: wait for sprint apra-fleet-5se to finish, then re-run from the `install --force` step onward (installer already built at dist/apra-fleet-installer-linux-x64), or have someone with authority force-release its stale reservation first. | C3: Build steps (preflight-clear-build-locks.mjs, npm ci, npm run build, npm run build:binary) all succeeded (exit 0) and produced dist/apra-fleet-installer-linux-x64. Halted at the runbook's mandatory pre-install-force check: `curl -s http://localhost:8787/api/sprints` returned a non-empty array -- active sprint apra-fleet-5se-2c0e9a66-78b3-4435-9725-a1252cbeb8d0 (member local-apra-fleet, childPid 1620924, port 8082, branch feat/endpoint-transport) confirmed still running via `ps` (~2h58m elapsed). deploy.md's Caution/Deploy sections explicitly instruct: STOP if sprints is non-empty -- wait for them to finish or be ready to force-release their stale reservations before running `install --force`, since that restarts the shared singleton MCP server (port 7523) this sprint's dispatches depend on and would collaterally kill its child processes. Did not run the installer, did not launch `apra-fleet run --transport http`, and did not run the smoke test. Awaiting operator decision: wait for apra-fleet-5se to finish, or explicitly authorize force-releasing its reservation before re-running deploy. | C4: Deploy stopped at the active-sprints pre-flight check in deploy.md, before running install --force. Steps 1-4 of Deploy all succeeded: preflight-clear-build-locks.mjs (no stale locks), npm ci (exit 0), npm run build (exit 0), npm run build:binary (exit 0, produced dist/apra-fleet-installer-linux-x64 at commit 841b5e0, bundle version v0.4.2_841b5e). Step 5, `curl -s http://localhost:8787/api/sprints`, returned a non-empty sprints array: {"sprints":[{"sprintId":"apra-fleet-5se-2c0e9a66-78b3-4435-9725-a1252cbeb8d0","members":["local-apra-fleet"],"issueRoots":["apra-fleet-5se"],"childPid":1620924,"port":8082}],"scopeFreshness":{"lastSyncedAt":null,"ageSeconds":"never-synced"}}. Verified childPid 1620924 is a live fleet-sprint-cli process (--issue apra-fleet-5se --members local-apra-fleet --service-url http://localhost:8787) and port 7523 (the singleton MCP server install --force would restart) is currently up and answering. Per deploy.md's Caution, install --force stops the running fleet server and can collaterally kill a live sprint's child processes; the runbook says STOP unless ready to wait or force-release the other sprint's stale reservation, and I have no instruction/authority to force-release apra-fleet-5se's reservation. install --force, the detached `run --transport http` launch, and the Smoke test were NOT executed. No commands failed; this is a deliberate stop per the runbook's documented caution, awaiting orchestrator decision on whether to wait for apra-fleet-5se to finish or authorize force-releasing its reservation. | C5: Build steps completed successfully (preflight-clear-build-locks.mjs, npm ci, npm run build, npm run build:binary all exited 0; dist/apra-fleet-installer-linux-x64 was produced). Stopped at the runbook's explicit gate before `install --force`: `curl -s http://localhost:8787/api/sprints` returned a non-empty sprints array -- {"sprints":[{"sprintId":"apra-fleet-5se-2c0e9a66-78b3-4435-9725-a1252cbeb8d0","members":["local-apra-fleet"],"issueRoots":["apra-fleet-5se"],"childPid":1620924,"port":8082}],"scopeFreshness":{"lastSyncedAt":null,"ageSeconds":"never-synced"}}. deploy.md's Deploy section explicitly instructs to STOP in this case ('either wait for them to finish, or be ready to force-release their stale reservations and relaunch afterward') because `install --force` restarts the shared singleton MCP server on localhost:7523 that this active sprint's dispatches depend on. I have no explicit authorization to force-release that sprint's reservations, so I did not run `install --force`, the launch step, or the smoke test. Please wait for sprint apra-fleet-5se to finish, or explicitly authorize force-releasing its reservation, then re-trigger deploy.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

None.

## Final verdict

PASS -- VERIFIED INDEPENDENTLY, NOT FROM BEAD COUNTS. I reviewed the net diff (21 files, +3017/-171, all confined to packages/apra-fleet-client and packages/apra-fleet-workflow) and ran every suite myself.

TEST EVIDENCE (all green): npm run build exit 0; root vitest 300 files / 4083 passed / 48 skipped / 0 failed; apra-fleet-se 2372 passed / 0 failed; TEST_EXIT=0. Critically, the root `npm test` (scripts/run-all-tests.mjs) runs ONLY vitest + the apra-fleet-se workspace -- it does NOT run the packages where all this sprint's code lives, so I ran those separately as CI does (.github/workflows/ci.yml:143 `npm test --workspaces --if-present`): apra-fleet-client 108 passed / 0 failed, apra-fleet-workflow 307 passed / 0 failed. I confirmed by name that all eight new endpoint test files actually executed (endpoint-core/http/shapes/memberless/factory/transport-cancellation/engine-e2e), not silently skipped.

THE FOUR OPEN VERIFY-ROUTED BEADS -- each traced to specific lines in THIS diff:
- 5se.7 (core): core.mjs normalizeUsage() returns null rather than a zero-filled object when the provider reported nothing (lines 105-120), so a paid call is never reported as free; classifyEndpointFailure() maps aborted->CancelledError, timeout->FleetTransportError, malformed/invalid_input->AgentOutputError, http->AgentDispatchError as distinct types (lines 231-285). Confirmed core.mjs imports only ../errors/workflow-errors.mjs -- zero fetch/header code, so the 'stays shape-agnostic' criterion holds literally.
- 5se.8 (both shapes): openai-shape.mjs posts /chat/completions (messages array) or /completions (prompt string) chosen by explicit config, with an unrecognized pattern a construction-time TypeError (lines 73-79) -- never sniffed from the model name; anthropic-shape.mjs posts /v1/messages with x-api-key + anthropic-version and a required max_tokens. endpoint-shapes.test.mjs proves envelope convergence between the two shapes and honours a non-default base URL (openrouter.ai) verbatim.
- 5se.9 (memberless): executeCommand always throws CommandError from the engine's OWN taxonomy (memberless.mjs:76-85) -- never resolves. I verified the unpriced signal against the real consumer rather than taking the claim on trust: _getMemberPricing at index.mjs:858-866 tests `parsed.error || !parsed.pricing`, which is exactly the `{error: ...}`-with-no-`pricing` payload memberless.mjs:89-92 returns.
- 5se.10 (packaging/e2e): package.json gains './endpoint' (and './errors'); endpoint-engine-e2e.test.mjs imports by package name and drives a real `new FleetWorkflow(makeEndpointApi(...))` through runWithContext() with schema enforcement, the repair loop (asserts exactly 2 provider requests) and the pricing path, across both shapes, with globalThis.fetch replaced by a thrower and the provider key env vars deleted. Docs updated in all three files.

CROSS-PACKAGE MOVE CHECKED: workflow/errors.mjs is now a pure re-export from '@apralabs/apra-fleet-client/errors'. This is the load-bearing detail -- same class objects, so `instanceof WorkflowError` still holds for an error raised inside the client package. The dependency is properly declared (apra-fleet-workflow depends on @apralabs/apra-fleet-client: '*') and 307 workflow tests confirm no identity breakage.

FAILURE PATHS TRACED, NOT PATTERN-MATCHED: classifyBySignalState() (http.mjs:177-181) is applied at all three points -- pre-flight, fetch rejection, and body read -- and classifies by the signal's own fired state rather than the rejection's name. That is what makes requestStop()'s CancelledError (name 'CancelledError', which isAbortError() does NOT match) still classify as a cancellation. withDeadline() deliberately uses a ref'd setTimeout with a finally-clearDeadline, not AbortSignal.timeout().

DEPLOY: failed in all 5 cycles, but every cycle got through preflight/npm ci/npm run build/npm run build:binary successfully and stopped at deploy.md's own active-sprints gate. That gate saw sprintId apra-fleet-5se-... -- THIS sprint -- as the blocking reservation. It is a correct runbook refusal, not a code defect: the sprint structurally cannot deploy itself. Filed as a newTask, along with the deferred install/smoke-test.

KB PROMOTIONS: promoted only e6b384a3, which I verified with a standalone repro (AbortSignal.timeout(3000) let the process exit at 0ms with aborted=false; a plain ref'd setTimeout fired at 302ms and held the loop open) -- this is exactly the rationale withDeadline() documents. I deliberately did NOT promote five candidates whose claims this sprint has since INVALIDATED: 45223ccc (prototype-chain provider) is fixed by the hasOwnProperty guard at factory.mjs:54; 937931e3 and f10f7d14 (abort misclassification on fetch and on body read) are fixed by classifyBySignalState; 35558f89 (leaked activity records) is fixed by the activityEndEmitted flag; and 72f7bc35 is now factually wrong -- timeoutMs and timeout_s ARE honoured via resolveRequestTimeoutMs(). I also refuted b06081e7 ("No test in this repo loads tool-registry.ts"): tests/code-intelligence-registry-wiring.test.ts:40,61 imports and invokes registerAllTools(). These six should be retired or rewritten rather than promoted.

SECONDARY FINDINGS (verified by repro, none blocking this epic's AC) are filed as newTasks below.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: none.
Summary: Stopped at Step 0 permissions check before running anything: regression-test-playbook.md's Permissions section requires Bash(bd *) coverage (for bd search/bd create in Reporting failures, and bd show/bd dolt in Setup and Test scenario), but neither .claude/settings.json (no permissions key at all) nor .claude/settings.local.json's permissions.allow list contains any entry covering bd -- every other required prefix (mkdir, rm, node dist/index.js, git clone, git -C, node scripts/run-integ-suites.mjs, npm run test:slow) is covered by an existing broader entry, only bd is missing. Per role instructions this is a hard stop: Part 1 (real-bd suite) and Part 2 (smoke test) were not run, no sandbox was brought up so no Teardown was needed, and no regression bugs were filed. The orchestrator/operator needs to run compose_permissions to grant Bash(bd:*) (or an equivalent covering prefix) in .claude/settings.local.json before this pass can proceed; this result is informational only and does not gate the current sprint's verdict.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
