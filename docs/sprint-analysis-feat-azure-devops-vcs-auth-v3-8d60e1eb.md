# Sprint Analysis: feat/azure-devops-vcs-auth-v3

Scope issue id(s): apra-fleet-5co8, apra-fleet-uof6.
Base branch: feat/windows-shell-selection.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [81, 83, 85, 85, 85].
High-water-mark closed count this sprint: 92.
Final closed count: 85.
Final open-at-goal-priority count: 2.

## Deploy/Integration outcomes

Deploy failures (5): C1: Stopped at the Active-sprints gate before running install --force. Build steps completed successfully: preflight-clear-build-locks.mjs killed 4 stale lock holders (PIDs 155076, 35600, 155656, 171032; none was the foreign sprint's childPid), npm ci (added 243 packages), npm run build, and npm run build:binary all exited 0, producing dist/apra-fleet-installer-win-x64.exe (version v0.4.2_54b44b). GET http://localhost:8787/api/sprints then showed one live reservation. My dispatch prompt (.fleet-task.md) did not state a dispatching sprint's own sprintId, so per deploy.md I have no self identity and must treat every live reservation as foreign. node scripts/check-foreign-sprints.mjs --self-sprint-id '' confirmed this: FOREIGN reservation sprintId='apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa' (childPid=6824, members=fleet-win-dev1), exit 3. Per the runbook I did not run install --force and did not run the smoke test (the previously-installed binary would exit 0 regardless of the new build, so that result would be meaningless -- not run rather than reported as a false pass). I also did not attempt a force-release: the runbook's force-release route is only for a stale SELF reservation, and this one has a live childPid and belongs to another sprint. Next step: wait for sprint apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa (childPid 6824) to finish, or have the operator confirm it is actually dead and force-release it via POST http://localhost:8787/api/reservations/apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa/force-release, then relaunch and retry deploy. The already-built binary in dist/ can be reused for the retry; no rebuild needed unless the tree changes. | C2: Stopped at the Active-sprints gate in deploy.md before running install --force. Permissions check passed (all required prefixes covered by the merged .claude/settings.json + .claude/settings.local.json allowlist). GET http://localhost:8787/api/sprints showed one live reservation: sprintId=apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa, childPid=6824, members=[fleet-win-dev1]. My dispatch prompt did not include a 'Your dispatching sprint's own supervisor reservation id (sprintId): <id>' line, so per the runbook I have no self identity and must treat every live reservation as foreign. Confirmed via node scripts/check-foreign-sprints.mjs (no --self-sprint-id) which exited 3 and reported that same reservation as FOREIGN. Per the runbook this means STOP -- do not run install --force. Note this may actually be the sprint that dispatched this deploy; if so, the fix is to re-dispatch with the sprintId explicitly stated, after which the gate should exit 0 and the deploy can proceed. Deploy was halted ahead of the preflight-clear-build-locks / npm ci / npm run build / build:binary steps, since the gate blocks install --force regardless of build outcome and npm ci deletes node_modules before reinstalling -- no reason to churn the tree for a deploy that can't complete. No build, install, or smoke test was performed. No force-release action taken (that is documented as an orchestrator/operator action for a stale SELF reservation, which this is not, since I have no self identity to compare against). | C3: Deploy stopped at the active-sprints gate per deploy.md; install --force was NOT run. Build steps all succeeded: preflight-clear-build-locks.mjs (killed 6 lock holders on this checkout -- esbuild PIDs 91544/129112/134012 and system node.exe PIDs 102324/150368/168208 that had loaded this checkout's rollup addon; none of these was the foreign sprint's child), npm ci, npm run build, npm run build:binary all exited 0. GET /api/sprints then showed one live reservation: sprintId 'apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa' (members: fleet-win-dev1, childPid 6824, port 8081) -- and tasklist confirms PID 6824 is a live Console-session node.exe (843MB), not stale. My dispatch prompt (.fleet-task.md) contained no 'Your dispatching sprint's own supervisor reservation id (sprintId): <id>' line, so per the runbook I have no self identity and must treat every live reservation as foreign. node scripts/check-foreign-sprints.mjs (run without --self-sprint-id, as required when no identity is given) confirmed this and exited 3: STOP, foreign sprint live, do not run install --force. Smoke test was intentionally NOT run -- it would only re-check the previously-installed binary and could give a false-positive deployed signal despite install --force never running. Likely resolution: if this deploy was in fact meant to be dispatched by sprint apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa, re-dispatch with the sprintId explicitly stated so the gate recognizes it as self and proceeds; otherwise wait for that foreign sprint to finish before retrying deploy. | C4: Stopped at the Active-sprints gate in deploy.md before any build/install step ran; working tree is unmodified. Step 0a passed: all runbook Permissions-section prefixes are covered by the merged allow list from .claude/settings.json + .claude/settings.local.json (checked via the mechanical node union script). Step 0b: KB entries were pre-injected into the dispatch prompt (per its own note 'You do not need to call any kb_* tool to read these'), so no kb_session_prime call was needed; nothing new worth kb_capture'ing surfaced. My dispatch prompt did not include a 'Your dispatching sprint's own supervisor reservation id (sprintId): <id>' line, so I have no self identity, and per deploy.md that means every live reservation must be treated as foreign. I ran the gate early (as a cheap read-only probe, before npm ci/build/build:binary) to avoid burning a build that would have to be discarded: `curl -s http://localhost:8787/api/sprints` returned one live reservation: sprintId='apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa', childPid=6824, members=['fleet-win-dev1'], issueRoots=['apra-fleet-5co8','apra-fleet-uof6']. `node scripts/check-foreign-sprints.mjs` (no --self-sprint-id, since none was supplied) classified it as FOREIGN and exited 3: 'STOP: 1 foreign sprint(s) live -- do not run install --force.' Per deploy.md's Active-sprints gate this is a hard stop -- I did not run preflight-clear-build-locks.mjs, npm ci, npm run build, npm run build:binary, the installer, or the smoke test, and I did not call the force-release endpoint (with no self identity I cannot classify this reservation as a stale self-reservation, so releasing it is not mine to do). Note this dispatch is tagged [rbv2t] and the live reservation's issueRoots (apra-fleet-5co8/apra-fleet-uof6) don't obviously match that tag, but it's worth checking whether this deploy was actually dispatched by that same sprint and the prompt simply omitted the sprintId line -- if so, re-dispatching with the sprintId included would let it proceed as a self-reservation. Otherwise: wait for sprintId 'apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa' (childPid 6824, member fleet-win-dev1) to finish, or have the operator confirm it's genuinely stale and force-release via POST http://localhost:8787/api/reservations/<sprintId>/force-release, then relaunch and redeploy. | C5: STOPPED at the Active-sprints gate per deploy.md; build steps completed successfully but install --force was NOT run. Build/test steps: Step 0a permissions check passed (all deploy.md permission prefixes covered by merged .claude/settings.json + settings.local.json allowlist). node scripts/preflight-clear-build-locks.mjs exited 0 (no locked files). npm ci completed (243 packages installed, no errors). npm run build completed (tsc + contract build, no errors). npm run build:binary completed -- dist/apra-fleet-installer-win-x64.exe (v0.4.2_7bfde0) built successfully. Installed/running state is unchanged (no partial install, no rollback needed). Active-sprints gate: GET http://localhost:8787/api/sprints returned one live reservation: sprintId=apra-fleet-5co8,apra-fleet-uof6-24a7b408-ab09-4be2-8a08-a94167b2d9fa childPid=6824 members=fleet-win-dev1. My dispatch prompt (.fleet-task.md) did NOT include a 'Your dispatching sprint's own supervisor reservation id (sprintId)' line, so per deploy.md I have no self identity and must treat every live reservation as foreign. Ran node scripts/check-foreign-sprints.mjs with no --self-sprint-id (correct per runbook when no identity was given); it exited 3 and reported that reservation as FOREIGN. Per deploy.md this means STOP -- do not run install --force. Smoke test was intentionally not run since the deploy did not proceed. If that reservation is actually this dispatching sprint's own, the fix is to re-dispatch with the sprintId supplied explicitly in the prompt, not to force-release the reservation -- it looks like a live, running child (pid 6824), not a stale self-reservation, and force-release should be left to the operator's judgment.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

None.

## Final verdict

FAIL -- FAIL. Root scope beads are both still OPEN (apra-fleet-5co8 P0, apra-fleet-uof6 P1), Deploy never completed in any of the 5 cycles, and 7 verify-routed beads were never confirmed against a deployed build. Build and tests are green (npm run build exit 0; root vitest 311 files passed / 8 skipped / 0 failed; apra-fleet-se node:test SUMMARY pass=2651 fail=0, incl. test/slow-lane-log-persistence.test.mjs). No lint script is configured in package.json, so build+test are the only gates. Working tree is NOT clean: untracked .fleet-out/plan-review-verdict.json is not covered by .gitignore (11604ac0 scoped only .fleet/planner-output.json, .review-tmp/, /open.json). KEY FINDING - the deploy self-identity fix is provably never executed: 5207658b threads run_id (bin/cli.mjs buildRunnerArgs, effectiveRunId) into fleet-sprint/runner.js validateArgs and runSprintCycle (sprintSelfId = validated.runId || validated.branch) and into the deployer's dispatch prompt. git merge-base --is-ancestor 5207658b 54b44b77 and ...7bfde0d7 both pass, so the C1 and C5 build trees (v0.4.2_54b44b, v0.4.2_7bfde0) already contained the fix, yet all five deployers still reported that no sprintId line was present in their prompt. The dispatching runner was therefore a stale copy (launched before the commit / running from the previously installed build), so the active-sprints gate blocked identically in all five cycles and will block again unless a sprint is relaunched from a tree/build containing 5207658b. Substantive ADO work IS present in the diff and looks sound: azure-devops.mjs REST builders + capabilitiesForHost true (lzfv), shared vcs-providers/shell-helpers.mjs shQuote with gitbash-vs-PowerShell dialect selection, machine-detectable skipped:true on the ADO connectivity check, provision_vcs_auth ADO repo-ref derivation, and the opt-in real ADO E2E harness (5co8.6); the stale capabilities pin in packages/apra-fleet-se/test/vcs-capabilities-table.test.mjs IS fixed in this diff (now asserts canOpenPullRequest true for dev.azure.com). But none of 5co8.14, 5co8.6 or lzfv was exercised against a deployed build this sprint - exactly the evidence gap 5co8.14 was reopened for in C5. Sandbox-lifecycle work (uof6) is likewise real: scripts/sandbox-lock.mjs, kill-port.mjs, reap-sandbox-dolt.mjs, check-foreign-sprints.mjs plus tests; the playbook's remaining lsof uses (regression-test-playbook.md:725) are now guarded by 'command -v lsof' and documented as best-effort, so apra-fleet-5co8.25 looks correctly closed. uof6 itself stays open on children egc/cgc5/f28t/yru. Blast-radius concern: scripts/preflight-clear-build-locks.mjs kills every non-self, non-ancestor process holding this checkout's node_modules open (selection ~lines 330-342, killPid ~296-298, taskkill /F /T on win32); the deploy logs show it killing 4-6 processes per run including system node.exe PIDs that had merely loaded this checkout's rollup addon, and the deployers had to hand-check 'none was the foreign sprint's childPid' - that exclusion is not in the script. reopenIds is empty deliberately: I could not audit all 85 closed beads and have no per-bead evidence of a bad close (the two specific candidates I checked, 5co8.14 and 5co8.25, are already open and already fixed-in-diff respectively), and a blanket reopen would violate the one-specific-reason rule. KB: promoted 919a3f50 only; rejected 42cbf8d9 and 8ac63638 (both describe defects this branch FIXED at HEAD - 7b737b16 made the cleanup assertions unconditional and failable despite its misleading 'unfailable' commit subject, and git ls-tree shows only one slow-lane-log-persistence.test.mjs remains) and 135e64ee (lsof guards are now portable).

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: none.
Summary: Ran both parts of regression-test-playbook.md at branch HEAD 46f92220. Part 1: the real-bd suite's status file was resumed from an earlier-in-session run (219/221 results, including both recorded failures, were carried over unchanged rather than freshly executed) -- only 2 pending files ran fresh (13 tests, all PASS). The slow lane DID run fully fresh today (npm run test:slow, pass=1 fail=1, duration_ms=450381), and that failure (mock-sprint-planner-dispatch-stalled-session.test.mjs, bd-replay recording drift after the watchdog correctly fires) is confirmed at HEAD and matches existing bead apra-fleet-5jlr, updated with today's evidence. Of the two carried-over suite failures, f34-concurrent-launch-engagement-integration.test.mjs (apra-fleet-4rjy/g541) was not independently re-run this pass (flagged as such, not re-confirmed); vcs-capabilities-table.test.mjs (apra-fleet-wor5) WAS re-run standalone at the same HEAD and now passes 10/10 -- the test file itself was rewritten to match current product behavior, so this bug does not currently reproduce (flagged for owner re-triage, not closed by this role). The stale-status-file mechanism that caused the misattribution was itself noted against existing bead apra-fleet-jl71. check-integ-suite-budget.mjs still fails (17/221 files over the 300s budget), tracked in existing bead apra-fleet-eft.17, updated with the same carried-over-data caveat. Part 2's Setup (install, server start+port verify, toy-repo clone, sandbox-local git/beads isolation, supervisor boot with identity-checked readiness) completed cleanly, and Test-scenario step 2 confirmed the canary gh-toy-4ef is open. Step 3a (credential provisioning) was denied by the Claude Code auto-mode classifier, matching the long-running known blocker apra-fleet-j48h (updated); this session additionally, independently confirmed both root causes present simultaneously -- the classifier block, and the fact that CLAUDE_CODE_OAUTH_TOKEN is unset and $REAL_HOME/.claude/.credentials.json does not exist at all (cross-posted to apra-fleet-xuo.13, the credential-preflight bead). Per this repo's CLAUDE.md, no workaround was attempted. Teardown ran afterward (supervisor stopped, lock released, server stopped, no orphaned dolt processes found, sandbox directory confirmed removed). No new bugs were created this run; six existing [regression][carry-over]/[integ] beads were updated with fresh evidence instead. This entire result is informational and does not gate the current sprint's PASS/FAIL verdict; all noted failures/blockers are pre-existing and carry over to a future sprint.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
