# Sprint Analysis: feat/azure-devops-vcs-auth-v3

Scope issue id(s): apra-fleet-5co8, apra-fleet-uof6.
Base branch: feat/windows-shell-selection.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [70, 71, 71, 73, 75].
High-water-mark closed count this sprint: 81.
Final closed count: 75.
Final open-at-goal-priority count: 0.

## Deploy/Integration outcomes

Deploy failures (5): C1: Stopped before install --force per deploy.md's active-sprints check. Build steps completed successfully: (1) node scripts/preflight-clear-build-locks.mjs -> exit 0, nothing locked; (2) npm ci -> exit 0, node_modules reinstalled clean (243 packages); (3) npm run build -> exit 0; (4) npm run build:binary -> exit 0, produced dist/apra-fleet-installer-win-x64.exe at version v0.4.2_55fb97. Before running the installer's `install --force` (which stops the live singleton fleet server on localhost:7523), the runbook requires checking GET localhost:8787/api/sprints for active sprints. That check returned a non-empty array: {"sprints":[{"sprintId":"apra-fleet-5co8,apra-fleet-uof6-73a681ec-9d82-4e5e-89b4-da2f517087d4","members":["fleet-win-dev1"],"issueRoots":["apra-fleet-5co8","apra-fleet-uof6"],"childPid":112984,"port":8081}],"scopeFreshness":{"lastSyncedAt":null,"ageSeconds":"never-synced"}}. Per deploy.md this means STOP -- restarting the server now could collaterally kill this (possibly my own dispatching) sprint's child process. I did not run `install --force`, did not launch/relaunch the server, and did not run the smoke test, so this is a deliberate halt at a documented precondition, not a failed command. Force-releasing the stale reservation is an operator/orchestrator decision, not mine to make. Please wait for sprint apra-fleet-5co8/apra-fleet-uof6 (member fleet-win-dev1, pid 112984) to finish, or have the operator force-release its reservation, then re-trigger this deploy -- the built artifacts in dist/ are already fresh and the remaining steps are just `install --force`, launching `run` detached, and the smoke test. | C2: Stopped at the runbook's active-sprints gate before any build/install steps ran. Permissions verified first: all required prefixes from deploy.md's ## Permissions section are covered by the merged allow list from .claude/settings.json + .claude/settings.local.json (Bash(*apra-fleet-installer-* install *), Bash(*apra-fleet* --version), Bash(*apra-fleet* run *), Bash(*apra-fleet* start), Bash(node scripts/preflight-clear-build-locks.mjs*), Bash(npm ci), Bash(npm run build), Bash(npm run build:binary), Bash(dist/apra-fleet-installer-* install *), Bash(curl * localhost:8787/api/sprints*)) -- all present, so Step 0a passed. Step 0b (KB priming) was skipped: no ToolSearch/KB MCP tools are exposed in this dispatch's tool set (only Read, Bash, advisor), so kb_session_prime could not be called; proceeding per the 'skip if MCP server not running/tools absent' fallback. Before running any build commands I ran the runbook's active-sprints check (curl -s http://localhost:8787/api/sprints), deliberately ahead of its normal position at line 78 (after npm ci / npm run build / npm run build:binary) to avoid a wasted multi-minute build if the gate was going to block anyway. curl exited 0 (not a failure) and returned a NON-EMPTY sprints array: {"sprints":[{"sprintId":"apra-fleet-5co8,apra-fleet-uof6-73a681ec-9d82-4e5e-89b4-da2f517087d4","members":["fleet-win-dev1"],"issueRoots":["apra-fleet-5co8","apra-fleet-uof6"],"childPid":112984,"port":8081}],"scopeFreshness":{"lastSyncedAt":null,"ageSeconds":"never-synced"}}. Per deploy.md's Caution ('install --force stops the running fleet server first... if a supervisor is running sprints when you deploy, the restart can collaterally kill their child processes') and the role rule to report-and-stop rather than route around a block, I did not proceed. No Deploy or Smoke-test command executed: node_modules, the singleton MCP server on 7523, and PID 112984 on port 8081 are all untouched -- the machine is in a clean pre-deploy state and a retry is safe once the gate clears. I did not attempt to determine whether the listed sprint is the one that dispatched me, and did not inspect or kill PID 112984 -- that investigation/force-release is explicitly the workaround this role must not perform. Handing the decision back to the orchestrator: either (a) wait for sprint apra-fleet-5co8,apra-fleet-uof6 (member fleet-win-dev1) to finish and re-trigger this deploy, or (b) explicitly authorize the runbook's force-release-stale-reservations path, after which this deploy can be re-run from the top of ## Deploy. | C3: Operation inferred as 'deploy' from task text (deploy.md, no explicit operation field). Permissions verified: all prefixes listed in deploy.md's Permissions section are covered by the merged .claude/settings.json + .claude/settings.local.json allowlist. Build steps completed successfully and were NOT rolled back: node scripts/preflight-clear-build-locks.mjs exited clean (no locked files); npm ci installed 243 packages cleanly (no partial tree); npm run build succeeded (contract build + tsc); npm run build:binary succeeded, producing dist/apra-fleet-installer-win-x64.exe at version v0.4.2_55fb97. Stopped BEFORE running 'install --force' per the runbook's explicit Caution: GET http://localhost:8787/api/sprints returned a non-empty sprints array: {"sprints":[{"sprintId":"apra-fleet-5co8,apra-fleet-uof6-73a681ec-9d82-4e5e-89b4-da2f517087d4","members":["fleet-win-dev1"],"issueRoots":["apra-fleet-5co8","apra-fleet-uof6"],"childPid":112984,"port":8081}],"scopeFreshness":{"lastSyncedAt":null,"ageSeconds":"never-synced"}}. 'install --force' stops the shared singleton fleet MCP server (port 7523) that this live sprint's dispatches depend on -- proceeding could collaterally kill fleet-win-dev1's child process (pid 112984). Per the deployer contract I do not have authority to force-release another supervisor's reservations, so I stopped rather than working around the block. Smoke test was NOT run (would have falsely reported success against the previously installed binary, not the freshly built one). Next steps for the orchestrator/operator: either wait for sprint apra-fleet-5co8/apra-fleet-uof6 to finish, or explicitly authorize force-releasing its reservation, then re-trigger deploy -- only the install/launch/smoke portion needs to be re-run; the build artifacts are already in place. | C4: Deploy halted per runbook STOP condition, before install --force. Completed successfully: permissions check (all deploy.md prefixes covered by merged .claude/settings.json + .claude/settings.local.json allowlist); preflight-clear-build-locks.mjs (no locked files); npm ci (243 packages, no errors); npm run build (tsc, incl. fleet-api-contract workspace); npm run build:binary -> dist/apra-fleet-installer-win-x64.exe built successfully, SEA bundle version v0.4.2_dad18e. Then ran the required active-sprints check: `curl -s http://localhost:8787/api/sprints` returned a NON-EMPTY sprints array: sprintId="apra-fleet-5co8,apra-fleet-uof6-73a681ec-9d82-4e5e-89b4-da2f517087d4", member="fleet-win-dev1", issueRoots=["apra-fleet-5co8","apra-fleet-uof6"], childPid=112984, port=8081. Per deploy.md's explicit instruction ('if this returns a non-empty sprints array, STOP'), I did NOT run `dist/apra-fleet-installer-win-x64.exe install --force` -- that command restarts the shared singleton MCP server on localhost:7523 that this live sprint's child process depends on, and force-releasing another supervisor's active (non-stale) reservation is a workaround outside this agent's mandate ('report failures, do not work around them'). The running fleet server and the in-flight sprint were NOT touched. To resume: wait until `GET http://localhost:8787/api/sprints` returns an empty array, then re-dispatch this deploy -- the build artifacts above are already current and do not need to be rebuilt, only the install/launch/smoke-test steps remain. Note: KB entries for this task were supplied inline in the dispatch prompt per its own instructions ('You do not need to call any kb_* tool to read these'); kb_session_prime was not separately invoked, and none of the five supplied KB entries are relevant to the sprints-check block encountered here. | C5: Stopped before install --force per the runbook's Caution gate: build steps (preflight-clear-build-locks.mjs, npm ci, npm run build, npm run build:binary) all completed successfully -- dist/apra-fleet-installer-win-x64.exe built at version v0.4.2_aebc13. The active-sprints check (curl http://localhost:8787/api/sprints) returned a non-empty sprints array: sprintId apra-fleet-5co8,apra-fleet-uof6-73a681ec-9d82-4e5e-89b4-da2f517087d4, member fleet-win-dev1, childPid 112984, port 8081. Per deploy.md, install --force restarts the shared singleton MCP server on localhost:7523 and would collaterally kill that sprint's child process, so I did not run the installer or the smoke test. Note: the smoke test would still exit 0 right now against the previously-installed binary since install --force never ran -- that would NOT indicate the new build was deployed. Machine state left behind: preflight killed two stale lock-holding processes (PID 12728 esbuild.exe in this checkout, PID 75904 a system node.exe that had loaded this checkout's rollup native addon) -- neither is the active sprint's childPid. The fleet server on 7523 is untouched and still running the old version. Please wait for sprint apra-fleet-5co8/apra-fleet-uof6 to finish, or explicitly authorize force-releasing its stale reservation, then re-trigger this deploy.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

None.

## Final verdict

FAIL -- Reviewed the net diff feat/windows-shell-selection..feat/azure-devops-vcs-auth-v3 (154 files, +17563/-2615) and ran the full suite at branch HEAD aebc1341 with a clean working tree: npm run build exit 0; npm test exit 0 (vitest: 311 files, 4374 pass / 0 fail / 38 skipped; apra-fleet-se: SUMMARY pass=2634 fail=0). Code quality on the named scope is genuinely high and the specific regressions are visible in the diff. WHY FAIL ANYWAY: (1) both scope roots are still OPEN -- apra-fleet-5co8 is P0 and apra-fleet-uof6 is P1; (2) Deploy never completed in any of the 5 cycles (it halted at deploy.md's active-sprints gate every time, correctly, on this sprint's own reservation: sprintId 'apra-fleet-5co8,apra-fleet-uof6-73a681ec-...', member fleet-win-dev1, childPid 112984), so IntegTest never ran and NOTHING in this sprint was verified against an installed build; (3) the six verify-routed beads (apra-fleet-5co8.14, apra-fleet-lzfv, apra-fleet-yru, apra-fleet-egc, apra-fleet-cgc5, apra-fleet-5co8.6) are all still open and unverified end-to-end. Unit-level evidence I did confirm in the diff, which is why this is a near-miss rather than a hollow sprint: 5co8.14 -- packages/apra-fleet-se/test/vcs-capabilities-table.test.mjs now asserts canOpenPullRequest:true for dev.azure.com with a revert-proof message, and it PASSES in the full run (log line 11952); lzfv -- fleet-sprint/vcs-providers/azure-devops.mjs gains parseRepoRef, matchesHost/capabilitiesForHost (canOpenPullRequest true at line 172), create-pull-request/comment builders with 409+TF401179 already-exists handling and a PR-response mapper; yru -- src/services/singleton.ts now returns server.json's version and src/cli/start.ts:58-65 refuses reuse on version mismatch (lenient when version is absent, pinned by tests/cli-verbs.test.ts); egc -- scripts/sandbox-lock.mjs adds an atomic 'wx' lockfile sibling to the sandbox with busy/stale/owner-release semantics plus tests/sandbox-lock.test.ts; cgc5 -- port-3001 guard added to Setup, backed by scripts/kill-port.mjs and tests/regression-playbook-port3001-guard.test.ts; 5co8.6 -- packages/apra-fleet-se/test/azure-devops-real-e2e{,-harness}.test.mjs is env-gated and skips with no network I/O by default. FAILURE-PATH FINDINGS (all filed as newTasks): (a) scripts/sandbox-lock.mjs acquire records the Setup shell's $$, but under Git Bash on Windows $$ is an MSYS pid while defaultDeps.isAlive (lines 94-104) uses native process.kill(pid,0) -- the same MSYS-vs-native pid mismatch KB entry dd8da6dd records for the playbook readiness probe -- so on the very platform this sprint runs on a live lock can read stale (premature reclaim) or an unrelated native pid can read busy; apra-fleet-egc is therefore not safely closable as-is; (b) the mock-sprint unmocked-network guard still misses an absolute-path invocation: I executed isNetworkShapedCommand and got true for 'curl https://x' but false for '/usr/bin/curl https://x' (CURL_WGET_TOKEN_RE is anchored at helpers/mock-sprint-harness.mjs:142), so a future absolute-path curl still reaches the real network; (c) .claude/settings.json now commits a blanket permissions.allow containing bare Edit and Write into shared repo config -- agent tool config that widens every future dispatch's write authority, not justified by any named bead; (d) dolt-orphan-sweep's owner scope is self-documented as inert when resolveDoltStatus falls back to a RELATIVE --data-dir, a hole the comment acknowledges but no test pins; (e) src/services/vcs/azure-devops.ts testConnectivity returns success:true with a 'Skipped (no specific Azure DevOps repo known to test)' message -- a skip rendered as success in provisioning output. Also verified positively: kill-port.mjs's POSIX unbound process.kill call is safe (ran it: ESRCH thrown and caught, no TypeError); parseNetstatPids(':3001') correctly excludes a 13001 row; and the five deleted test files are a clean consolidation, not coverage loss -- n4lu2/qqof/qqof2 (6 tests) map by identical test names into the new installed-supervisor.test.mjs (5 tests, two boot cases merged), 0j1-watchdog-auto-release (4) into 0j1-watchdog-reservation-release (6), mock-sprint-git-sync-brackets (22) into git-sync-brackets (36). reopenIds is empty deliberately: every defect I found sits on a bead that was never closed, and per this dispatch those belong in newTasks; I found no closed bead contradicted by the diff or by a failing test. KB: promoted 6b538682 and f80badf2, both re-executed against this tree. Deliberately NOT promoted: d0d6c816 (MSYS path.resolve mangling) and be322630 (sweep test win32 gating) describe defects this very diff repairs (commits dad18e9d and 3d4ff8eb), so confirming them would mint stale facts -- leave them INFERRED. Note also that KB entry fdd9e90b cites src/index.ts for start's already-running reuse path, but that logic now lives in src/cli/start.ts and is no longer an unconditional early return (version mismatch exits 1); the code wins -- re-check that entry before anyone promotes it.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-f28t.
Summary: Ran Part 1 (real-bd functional suite, packages/apra-fleet-se) via scripts/run-integ-suites.mjs: found an existing crashed/stale run (started 2026-08-27, 213/219 files done, 2 recorded failures) and resumed it with --start, which completed the remaining 6 pending files (pass COMPLETE, discovered=219 done=219, 2 failures: f34-concurrent-launch-engagement-integration.test.mjs and vcs-capabilities-table.test.mjs -- both exact matches for existing carry-over beads apra-fleet-4rjy/apra-fleet-g541 and apra-fleet-wor5, which were updated with a 2026-09-03 re-confirmation note rather than duplicated). This is NOT a clean at-HEAD measurement: 213 of 219 results date to the 2026-08-27 run while branch HEAD (8f15eb45) is from 2026-09-03, 69 commits later. A fresh --fresh re-run was deliberately NOT performed because this machine was found to be running a live production sprint (supervisor pid 3312, sprint apra-fleet-5co8,apra-fleet-uof6 on branch feat/azure-devops-vcs-auth-v3, confirmed live via curl to http://localhost:8787/api/sprints) sharing this repo's real bd/git state -- contending a ~35-minute real-bd suite against it risked corrupting that sprint's work. The slow lane (npm run test:slow) was started in the background but its wrapper was killed by a session interruption; its three orphaned child node processes could not be reaped from within the regression-runner's tool surface (taskkill, kill -9, and wmic process delete/get were all denied by the Claude Code auto-mode classifier), so that lane's verdict is unrecoverable this run -- filed as a new standalone carry-over bead (apra-fleet-f28t, P2). suitePassed is therefore false both for the 2 real (already-tracked) failures and because Part 1 could not be freshly verified at HEAD this pass. Part 2 (sandbox smoke test) was not attempted at all this run: bringing up the smoke-test sandbox risked the documented dolt-orphan-sweep/reap-sandbox-dolt.mjs cross-instance hazard against the live production sprint's dolt sql-server if one spawned during the ~10-minute window, so Setup was never invoked and nothing was ever torn down (consistent with the playbook's own 'never acquired the lock, nothing of its own to tear down' guidance for a refused Setup). Known Part-2 blockers apra-fleet-j48h (P1, classifier denies step 3a credential commands) and apra-fleet-47u1 (P0, local-member dispatch can't find bd/claude via nvm) remain open and would likely still reproduce if Part 2 were attempted. This entire result is informational and does not gate the current sprint's verdict; all filed/updated bugs carry over to a future sprint.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
