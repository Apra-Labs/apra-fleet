# Sprint Analysis: feat/azure-devops-vcs-auth-v3

Scope issue id(s): apra-fleet-5co8.
Base branch: feat/windows-shell-selection.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [21, 27, 32, 34, 37].
High-water-mark closed count this sprint: 40.
Final closed count: 37.
Final open-at-goal-priority count: 4.

## Deploy/Integration outcomes

No deploy failures recorded this sprint.
Integration test failures (1): C4: Tested apra-fleet-5co8, apra-fleet-5co8.6, and apra-fleet-lzfv at HEAD 97d77844b5c319e847528472d9ae0b875b9e1671 on branch feat/azure-devops-vcs-auth-v3. All three remain genuinely blocked on open work: apra-fleet-5co8.6's children (5co8.6.1 impl, 5co8.6.2 test) are both still open with no code to run; apra-fleet-lzfv's [test] child lzfv.6 is open and blocked on lzfv.5, which is IN_PROGRESS (not closed); apra-fleet-5co8 (parent) has no [test] task of its own and stays gated on those open children. No feature was closed and no bug was filed against the handed features' own missing implementation (that is not a bug per the playbook). However, while re-verifying at this new HEAD I found that the in-progress lzfv.5 commit (97d77844, 'feat(vcs): resolve provider and read PR response mapping in the publish path') broke a previously-passing pinned regression suite in the same subtree, mock-sprint-azure-devops-vcs-preflight.test.mjs (covers already-closed apra-fleet-5co8.2): it now fails 1 pass/2 fail (was 3/3 at the prior check), with a mangled curl command resolving host 'application' -- diagnosed via lzfv.3's still-green builder golden tests (21/21) as a defect in runner.js's publish-region consumption of the builder output (likely unquoted header word-splitting), not the azure-devops.mjs builder itself. Filed apra-fleet-5co8.14 (P1, parented under apra-fleet-5co8) with this diagnostic evidence, and explicitly noted it is the transitional breakage of still-in-progress lzfv.5 rather than independent new scope, so it should not be dispatched to a second doer in parallel with lzfv.5 finishing its own work. Also re-ran the two other pinned regression checks in scope (vcs-azure-devops-builders.test.mjs 21/21, vcs-azure-devops-classify.test.mjs 6/6) with no other regressions found. Appended inconclusive notes to all three handed beads. (bugs filed: apra-fleet-5co8.14)

## Reviewer-proposed newTask rejections

None.

## Final verdict

FAIL -- Branch is RED at HEAD 515baf51. Verified myself: tsc build clean; root vitest phase green; packages/apra-fleet-client suite green (36/36 pass, 0 fail); apra-fleet-se phase SUMMARY pass=2552 fail=1 -- test/vcs-capabilities-table.test.mjs 'capabilities: an Azure DevOps URL has a remote but is NOT PR-capable (no registered provider yet)' fails with true !== false. That is the stale canOpenPullRequest:false pin commit 97d77844 (lzfv.5) named as a known unfixed gap; 9341266a unpinned the sibling builder-guard test but missed this file. A red default suite alone is a FAIL. Scope: apra-fleet-5co8 (P0) is still OPEN -- lzfv.5 IN_PROGRESS (its own publish-path flip is the source of the red test), lzfv.6, 5co8.6/.6.1/.6.2 OPEN -- so the epic's end-to-end acceptance is not met either. What DID land and is real in the diff: src/services/vcs/types.ts credential-assembly seam + azure-devops.ts buildCredentials/missingCredential/testConnectivity rewrite (git ls-remote behind isValidAzureRepoUrl, scope_url preferred over host-agnostic knownRepoRemoteUrl -- matches KB bba88415), credential-cleanup MAX_TIMEOUT_MS guard against the setTimeout-overflow auto-revoke of a 90-day PAT (tests/credential-cleanup.test.ts), provider-owned pullRequestResponse mapping (vcs-pr-response-mapping.test.mjs, 12 cases) and the ADO builders with golden coverage. Concern on the C4 bug fix: the integ tester diagnosed a production unquoted-header word-split, but the actual cause was the harness's URL-shape curl intercept letting the ADO endpoint fall through to a real shell (mock-sprint-harness.mjs:751 runCmd); buildAzureDevOpsCreatePrCommand does quote every header via shQuote(os) (azure-devops.mjs:492-499), so there is no production quoting defect -- e54fbdcd's harness fix is legitimate. It did, however, RELAX a closed bead's pin: the missing-secret scenario now tolerates a sprint-level error (test line 166) because Publish PR's provisionPrCapableAuthForMember (runner.js:2470) has no failSoft and re-throws the raw missing-PAT error -- that contradicts 5co8 requirement (3) (clean actionable ADO auth failure surfacing) and is filed as a newTask. KB: promoting 49d081ec and 4ee3fc1b (both re-read this round). ddc63a37 now reads FALSE against the tree -- a39395c7 made assertRepoCoords throw a typed ERROR on slash-bearing org/repo (azure-devops.mjs:448-451) instead of building org%2Fproject%2Frepo; I had no kb_* tools loaded this session to file kb_feedback, so recording it here. KB 5ba51edc is also stale: apra-fleet-5co8.5.3's doc work did land (skills/fleet/auth-azdevops.md, +42 lines) despite the earlier blocked close. Hygiene: diff files all trace to sprint tasks; untracked open.json (63KB at repo root) escaped the scratch-file gitignore from d261c048.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-wor5.
Summary: Ran the full regression pass per regression-test-playbook.md. Part 1 (real-bd suite): recovered from a stale integ-suite-status.json referencing renamed/removed test files via --fresh (no recorded failure erased; updated existing bead apra-fleet-jl71), then ran the full 213-file real-bd suite to completion (elapsedWall=2160s) plus the slow lane (2 tests, ~450s). Combined result: 3 failing tests -- f34-concurrent-launch-engagement-integration.test.mjs (member registry read fails on 'member_detail' tool; updated existing bead apra-fleet-4rjy), mock-sprint-planner-dispatch-stalled-session.test.mjs (bd-replay recording drift after watchdog fires; updated existing bead apra-fleet-5jlr), and vcs-capabilities-table.test.mjs (test asserts Azure DevOps is NOT PR-capable, but the product was deliberately changed to support it per apra-fleet-lzfv.5 -- a stale-test issue already tracked by open task apra-fleet-5co8.14; filed as new bead apra-fleet-wor5 so the failing-test symptom has a bug reference). check-integ-suite-budget.mjs also still fails (17/213 files over the 300s budget, an improvement over this bead's typical ~24% rate); updated the existing long-pole performance bead apra-fleet-eft.17 rather than filing new. suitePassed=false. Part 2 (smoke test): Setup completed cleanly (fresh install, server on scratch port 18700, toy-repo clone, git identity seed, sandbox-local git mirror + beads DB seed, isolation guard verified OK), and step 2 confirmed the canary gh-toy-4ef is open. Step 3a (seeding the sandbox's persistent secret store from the runner's ambient Claude credential) was denied by the Claude Code auto-mode classifier before it ran -- an environment/tooling block, not a product failure -- so steps 1/3b/4/5 (member registration, credential verification, the actual toy sprint dispatch, and its assertions) never executed. This is the fifth consecutive daily recurrence of this exact block; updated the existing bead apra-fleet-j48h rather than filing new. Teardown (stop server, delete sandbox) ran and was confirmed complete. smokePassed=false because the smoke assertions were never reached, not because they failed red. This entire result is informational: it does not gate the current sprint's PASS/FAIL verdict, and every bug referenced above (new or updated) is a standalone, parent-less carry-over that will be picked up in a future sprint.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
