# Sprint Analysis: feat/windows-shell-selection

Scope issue id(s): apra-fleet-7dir.
Base branch: main.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [0, 1, 1, 10, 21].
High-water-mark closed count this sprint: 22.
Final closed count: 21.
Final open-at-goal-priority count: 4.

## Deploy/Integration outcomes

Deploy failures (2): C2: Deploy stopped at npm ci (Deploy step, before the active-sprints GET localhost:8787/api/sprints check and before install --force -- so no server restart, no partial install, no disruption to any live sprint). Permission check (Step 0a) passed: all 10 command prefixes in deploy.md Permissions section are covered by the merged .claude/settings.json + .claude/settings.local.json allowlist. Sequence: ran node scripts/preflight-clear-build-locks.mjs first, which killed PID 92808 (an orphaned node_modules/@esbuild/win32-x64/esbuild.exe from a prior build holding this checkouts node_modules open). Then npm ci failed with: npm error code EPERM, npm error syscall unlink, npm error path C:/akhil/git/apra-fleet/node_modules/@rollup/rollup-win32-x64-msvc/rollup.win32-x64-msvc.node, npm error errno -4048 (EPERM: operation not permitted, unlink on that file). Reran preflight to see if it would now flag the new holder -- it reported no stale processes found holding node_modules open. Retried npm ci a second time (about 9s later, ruling out a transient AV-scan window) and got the identical EPERM on the same file. Likely root cause: preflight-clear-build-locks.mjs only detects processes whose own executable path/cmdline points inside this checkouts node_modules (per its own doc comment) -- e.g. a standalone binary like esbuild.exe that lives in node_modules. It cannot see a native addon (rollup.win32-x64-msvc.node) loaded into a host process living outside node_modules (e.g. a system node.exe, an editor/language-server process, or a leftover vitest worker). This looks like a structural blind spot in the preflight tool, not a one-off flake, since it reproduced on retry. Per the deployer contract, no workaround was attempted (no manual unlock, no admin re-run, no wrapper script) -- stopping and reporting instead. Note the working tree is now left with node_modules partially removed (npm ci deletes-then-reinstalls and errored mid-unlink), so a bare retry will likely fail again until the lock holder is identified/killed or the tree is cleaned up. Requested next step: an operator identifies and terminates whatever process holds a handle on the rollup-win32-x64-msvc native addon file (e.g. via Get-Process node | Select Id,Path -- not run here since that prefix is not in the current allowlist), or preflight-clear-build-locks.mjs is extended to detect handles on loaded native-addon files held by out-of-tree processes. Re-trigger deploy after either is done. | C4: Deploy stopped mid-Deploy section per runbook: fail-and-report, no workarounds. Sequence executed: (1) `node scripts/preflight-clear-build-locks.mjs` succeeded, killing 2 stale esbuild.exe processes (PIDs 59748, 11732) that had a lock on this checkout's node_modules. (2) `npm ci` then failed with EPERM while removing/reinstalling node_modules, unable to unlink `C:\akhil\git\apra-fleet\node_modules\@rollup\rollup-win32-x64-msvc\rollup.win32-x64-msvc.node`. Both exit signals as observed (not reconciled): shell reported exit code 127; npm itself reported `npm error code EPERM`, `npm error errno -4048`, `[Error: EPERM: operation not permitted, unlink '...rollup.win32-x64-msvc.node'] { errno: -4048, code: 'EPERM', syscall: 'unlink' }`. Full npm log at C:\Users\akhil\AppData\Local\npm-cache\_logs\2026-08-22T12_17_40_956Z-debug-0.log.

No commands after `npm ci` were run: `npm run build`, `npm run build:binary`, the `curl localhost:8787/api/sprints` active-sprints check, and `"$INSTALLER" install --force` were never reached. The shared singleton MCP server on port 7523 was NOT restarted -- no live supervisor sprint's dispatches were collaterally affected by this deploy attempt.

Environment state warning: `npm ci` deletes node_modules before reinstalling and died mid-unlink, so this checkout's node_modules is very likely left in a partially-installed/inconsistent state, not the state it was in before this run. It was NOT cleaned up (no workaround/fix attempted per role rules) -- a plain retry of `npm ci` will not be starting from a clean baseline.

Likely root cause (unverified hypothesis, not confirmed by process inspection): preflight-clear-build-locks.mjs only kills processes whose OWN executable path lives inside this checkout's node_modules (e.g. esbuild.exe). The rollup native addon (`rollup.win32-x64-msvc.node`) is a `.node` binary that gets loaded/mapped INTO some other node.exe process (e.g. a leftover Vite/Rollup watcher or test runner) whose executable lives outside node_modules -- such a holder would not be matched by the current ownership-scoped kill logic, leaving its lock on the rollup binary uncleared. This is an observation + hypothesis only; the holding process was not inspected, per the stop-and-report boundary.

Recommendation for the operator: identify and close whatever process (likely a lingering dev-server/watcher/build process from a prior session) is holding rollup.win32-x64-msvc.node open, verify/restore node_modules to a clean state, then re-run this deploy from the top.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

None.

## Final verdict

FAIL -- Reviewed the net diff main..feat/windows-shell-selection (120 files, +6999/-2007) against epic apra-fleet-7dir. Quality of what LANDED is good: `npm run build` exit 0, full `npm test` (scripts/run-all-tests.mjs, core vitest + apra-fleet-se + workflow lanes) exit 0 with no failures -- including contracts-schema-dist-staleness-guard and golden-transcript, which KB entries 5dabfa36/cbda293c predicted red; they pass on this tree, so those entries are stale here. Implementation is verified real, not bead arithmetic: src/types.ts:26 adds `shell?: 'gitbash'|'pwsh7'|'powershell5'`; src/services/shell-probe.ts probes gitbash>pwsh7>powershell5 with a real smoke command asserting BOTH exit 0 AND a stdout marker (isProvenGitBash/isProvenPwsh7/isProvenPowerShell5), rejects WSL/System32/WindowsApps bash.exe by path AND by uname (isWslLauncherPath + isWindowsPosixUname), and degrades to powershell5 with a warning instead of failing registration -- matching 7dir.1.3's 'never register on path-existence alone' AC; src/os/windows-gitbash.ts extends LinuxCommands and overrides only Windows-native surface; strategy.ts:125 now passes the resolved shell to execSync for killPid (7dir.4); execute-command.ts, execute-prompt.ts, compose-permissions.ts, member-home.ts, orphan-recovery.ts, stall/* are shell-branched with new tests (tests/os-commands-gitbash, register-member-shell-probe, windows-shell-probe, execute-prompt-shell-matrix, shell-matrix-command-builders, compose-permissions-gitbash-delivery, strategy-gitbash-local-exec, provider-install-shell-matrix).

FAIL is driven by the epic being materially incomplete, plus two concrete code-level gaps:

1. CLIENT DRIFT (7dir.1, child 1.2 IN_PROGRESS). `git diff main..branch --stat -- packages/apra-fleet-client` is EMPTY while src/types.ts and src/tools/register-member.ts:73 both gained the `shell` tool input. Repo CLAUDE.md makes mirroring the client non-optional in the same change. Mitigation found by inspection: api.mjs:358/366 forward `options` verbatim to callTool, so there is no runtime break -- the drift is documentation/typedef only (packages/apra-fleet-client/docs/api-reference.md:283-297 RegisterMemberOptions table lists neither `shell` nor the older `code_intel_provider`). Still unfinished per 1.2.

2. FLEET-SPRINT SIDE IS DEAD CODE (7dir.3; 3.1/3.2 CLOSED, 3.3/3.4 OPEN). se-os-commands.mjs, se-posix.mjs, se-windows.mjs, se-windows-gitbash.mjs (~376 lines) landed but nothing imports them outside each other, no test file references them, and runner.js:2481 still calls the ad-hoc wrapPowerShellEncodedForMember -- the exact call site 7dir.3's AC names. So the epic's second half (the .mjs instance of the pattern) delivers no behaviour today.

3. GIT-BASH PATH RESOLUTION INCONSISTENCY (new defect, filed below, not reopened). src/os/windows-gitbash.ts GIT_BASH_CANDIDATES lists only the two Program Files roots and Program Files\Git\usr\bin, then falls back to bare 'bash.exe'. src/services/shell-probe.ts:82 deliberately ALSO probes %LOCALAPPDATA%\Programs\Git\bin\bash.exe (the default user-scope Git for Windows install). A local Windows member registered `gitbash` from a user-scope install therefore spawns with shell='bash.exe' resolved off PATH -- reintroducing exactly the WSL/System32 bash.exe ambiguity the probe exists to eliminate. Bounded severity: cleanExec is LocalStrategy-only, so remote members are unaffected. Not reopened against 7dir.4 because that bead's criteria cover the killPid/execSync shell routing and the clean-env decision (both implemented correctly), not candidate-path resolution.

4. 7dir.5 remains open with test child 5.3 (shell-aware string assertions for execute-command/monitor-task/watch/strategy) unclosed; the audit comments in strategy.ts:63-77 and execute-command.ts:265-277 that justify leaving wrapPowerShellEncoded sites unbranched are sound and were checked (the encoded form is shell-agnostic as a string), but the assertions pinning them are not all in place.

5. DEPLOY NEVER COMPLETED. 2 of 5 cycles died at `npm ci` (EPERM unlink on node_modules/@rollup/rollup-win32-x64-msvc/rollup.win32-x64-msvc.node), reproducibly. npm run build:binary and `install --force` were never exercised on this branch by any cycle. I verified this checkout's node_modules is currently usable (npx vitest 4.0.18 runs) and did NOT re-run npm ci.

Hygiene: working tree is not clean -- untracked tasks.json, .fleet-planner-output.json and .scratch-dolt-settle/ (a bd/planner scratch dump) sit at repo root; the .gitignore change in this diff added /all.json and .fleet-review*.json but missed these. Also noted (non-blocking): isPosixShell is now duplicated four times with three different signatures (utils/agent-helpers.ts export, plus private copies in member-home.ts, orphan-recovery.ts, compose-permissions.ts, and isPosixShellMember in execute-prompt.ts), each with a comment saying the duplication is deliberate.

KB: kb_promotions is empty and deliberately so. I checked two candidates directly and both describe defects ALREADY FIXED on this tree, so promoting them would mislead: 0e3ece89 (fixture leak) -- tests/windows-powershell-error-handling.test.ts:107-146 now pushes to tempDirsToClean and drains via ONE collection-time afterAll, exactly the fix the entry's claim predates; 1288e360 (deepMergeJson Hashtable pollution + Merge-Objects throw) -- src/os/windows.ts:213 starts $current as $null and Merge-Objects calls ConvertTo-HashtableDeep before recursing, and the live-PowerShell tests at tests/windows-powershell-error-handling.test.ts:307-338 (new-file containment, nested {x:1}+{y:2} -> {x:1,y:2}) pass in this run. Both entries should be corrected or retired rather than trusted as current leads.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-wclh.
Summary: Ran regression-test-playbook.md Part 1 (real-bd suite + slow lane) and Part 2 (sandbox smoke test) at branch HEAD 76c90f5, always tearing down the sandbox afterward. Part 1 fast lane: fresh run (--fresh, since the prior status file was 2 days/29 commits stale) of all 200 files finished pass COMPLETE with 5 failures (f34-concurrent-launch-engagement-integration.test.mjs, golden-transcript-3bead.test.mjs, mock-sprint-kb-remote-scope.test.mjs, mock-sprint-publish-push-failure.test.mjs, mock-sprint-reviewer-dispatch-error.test.mjs) plus a known perf/budget violation (34/200 files exceed the 300s single-file budget, worst 1347s). Part 1 slow lane: dispatch-watchdog-timer-ref.test.mjs passed; mock-sprint-planner-dispatch-stalled-session.test.mjs failed with the long-standing bd-replay recording-drift signature after a 450s watchdog-timeout abort. Of these 6 correctness failures plus the budget issue, 6 exactly matched existing open [regression][carry-over]/[integ] beads (apra-fleet-mmxx, apra-fleet-qk6q, apra-fleet-5i08, apra-fleet-2cf, apra-fleet-eft.17, apra-fleet-x1c) and were updated with today's reproduction evidence rather than duplicated. golden-transcript-3bead.test.mjs's failure this run had a materially different signature (TypeError at 4s, not the stall-abort apra-fleet-w7ee describes, and its sibling golden-transcript.test.mjs actually passed this run) so it was filed as a new standalone, parent-less bead apra-fleet-wclh, and apra-fleet-w7ee was annotated to cross-reference the split. Part 2: Setup completed fully and cleanly (install, server up on port 18700, status verify passed, toy-repo clone, sandbox-local git mirror, guarded beads seed, sandbox-isolation check all green) at branch HEAD, but the Test scenario's step 3a (credential-provisioning command, copied verbatim from the playbook) was denied by the Claude Code auto-mode permission classifier -- a known, already-tracked issue (apra-fleet-j48h, updated with today's reproduction) -- so steps 1/2/3b/4/5 (member registration, canary check, real Planner dispatch, closure assertion) never executed this cycle; no workaround was attempted per repo policy. Teardown ran and fully removed the sandbox regardless. This entire result is informational and does not gate the current sprint's PASS/FAIL verdict; all filed/updated bugs are pre-existing breakage that carries over to a future sprint.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
