# Sprint Analysis: fix/runner-dolt-push-reliability

Scope issue id(s): apra-fleet-spp, apra-fleet-vkc.1, apra-fleet-mrj, apra-fleet-417.4, apra-fleet-66u, apra-fleet-647.1.
Base branch: chore/integration-binary-fixes-and-auth-selfheal.
Cycles run: 5.

## Progress

Closed-bead count history (per cycle evaluation): [23, 27, 27, 29, 29].
High-water-mark closed count this sprint: 32.
Final closed count: 29.
Final open-at-goal-priority count: 0.

## Deploy/Integration outcomes

Deploy failures (5): C1: Stopped at Step 0 (permission check) before running any deploy commands. deploy.md's ## Permissions section requires the following command prefixes in .claude/settings.json under permissions.allow, but they are missing:
  Bash(*apra-fleet* start)
  Bash(curl * localhost:8787/api/sprints*)
Present/covered prefixes: Bash(*apra-fleet-installer-* install *), Bash(*apra-fleet* --version), Bash(npm ci), Bash(npm run build), Bash(npm run build:binary), Bash(dist/apra-fleet-installer-* install *). Add the two missing entries to the permissions allowlist and re-trigger the deploy. No build, install, or smoke-test commands were executed. | C2: Step 0 permission check failed: deploy.md's Permissions section requires these command prefixes, but they are missing from .claude/settings.json permissions.allow: 'Bash(*apra-fleet* start)' and 'Bash(curl * localhost:8787/api/sprints*)'. Add these to the allowlist and re-trigger deployment. Did not execute any Deploy or Smoke test steps. | C3: Stopped at Step 0 (permissions check) -- deploy.md's Permissions section requires command prefixes not present in .claude/settings.json's permissions.allow: Bash(*apra-fleet* start) and Bash(curl * localhost:8787/api/sprints*). All other required prefixes (Bash(*apra-fleet-installer-* install *), Bash(*apra-fleet* --version), Bash(npm ci), Bash(npm run build), Bash(npm run build:binary), Bash(dist/apra-fleet-installer-* install *)) are already present. No deploy commands were executed. Add the two missing entries to the allowlist and re-trigger the sprint. | C4: Stopped at Step 0 permission check before running any deploy commands. deploy.md's Permissions section requires 'Bash(*apra-fleet* start)' and 'Bash(curl * localhost:8787/api/sprints*)', neither of which is present in .claude/settings.json permissions.allow (only 'Bash(*apra-fleet* --version)' and installer-related prefixes are allowed, plus unrelated curl-less entries). Add these two entries to the allowlist and re-trigger the deploy; per policy I will not add them myself or work around the block. | C5: Stopped at Step 0 (permission check) before running any deploy commands. deploy.md's ## Permissions section requires these command prefixes in .claude/settings.json under permissions.allow, but they are missing: Bash(*apra-fleet* start) and Bash(curl * localhost:8787/api/sprints*). Present prefixes (*apra-fleet-installer-* install *, *apra-fleet* --version, npm ci, npm run build, npm run build:binary, dist/apra-fleet-installer-* install *) are all satisfied. Add the two missing entries to the permissions allowlist and re-trigger the deploy.
No integration test failures recorded this sprint.

## Reviewer-proposed newTask rejections

2 newTask(s) rejected before reaching bd create: C5: title fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/\[\]-]+$/ (or is empty): "Unblock Deploy: add supervisor start + sprints-API smoke-test prefixes to the deploy permissions allowlist" | C5: title fails safe-character allowlist /^[A-Za-z0-9 .,:;!?()'_/\[\]-]+$/ (or is empty): "Re-run Deploy + IntegTest to verify apra-fleet-spp, apra-fleet-mrj, apra-fleet-66u against a deployed build"

## Final verdict

FAIL -- Code review of the net diff for all six named beads is clean and their acceptance criteria are met at the source level: apra-fleet-spp -- dolt-sync.mjs classifies push.kind==='auth' as a distinct DoltSyncError at both the first-push (L646-655) and post-reconcile re-push (L702-710) terminals, reached only after a bounded one-shot onAuthFailure self-heal, no longer folded into DoltDivergedError; apra-fleet-vkc.1 -- recovery ladder wired via buildDoltRecoveryLadder (runner.js L1125) through surfaceDivergence at both divergence terminals with degraded-by-default preserved, covered by dolt-push-recovery-wiring.test.mjs; apra-fleet-mrj -- createChildBeadWithAllocatedId (runner.js L2149-2171) emits --id alone and links the parent via a separate bd update --parent, with newtask-body-file-roundtrip.test.mjs L255/257/114-115/308 pinning the both-flags regression, the separate link, the null-allocator --parent path, and the mismatched-parent guard; apra-fleet-417.4 -- vcs-auth-preflight.test.mjs coverage present; apra-fleet-66u -- stall progress score now derives from a fresh bd list --status=closed read (runner.js L8108-8137) rather than the stale fetchAllBeadsShared snapshot, a genuine root-cause fix; apra-fleet-647.1 -- VCSModule abstraction with extensive passing suites. Build is green, working tree is clean, and the full test suites pass (root vitest 2893 passed / 23 skipped -- skips are Docker/real-CLI integration only; apra-fleet-se 1714 pass / 0 fail, SUMMARY pass=2010 fail=0). FAIL rationale: Deploy failed at Step 0 in ALL 5 cycles, so the three verify-routed P1/P2 parent bugs apra-fleet-spp, apra-fleet-mrj, apra-fleet-66u were never confirmed against a deployed build -- they remain OPEN and unverified end-to-end. Root cause is in-scope: deploy.md (commit 669065a1, see deploy.md L16 and L25) now REQUIRES the command prefixes 'Bash(*apra-fleet* start)' and 'Bash(curl * localhost:8787/api/sprints*)' for the supervisor start + smoke-test steps added this sprint, but those prefixes are absent from the (gitignored, per-machine) .claude/settings.json permissions.allow, so the deploy runbook cannot execute as written. The deployer correctly surfaced the block rather than routing around it. Per the dispatch's explicit instruction not to PASS on deploy/integration failure and to treat spp/mrj/66u as open unverified targets, this is FAIL despite the code itself being correct and unit-verified. No already-closed bead was closed on insufficient evidence (vkc.1 and 417.4 are impl/test tasks fully covered by the passing suites; 647.1's close cites verification against a real deployed build), so reopenIds is empty; spp/mrj/66u are still OPEN and therefore belong in newTasks/normal re-routing, not reopenIds.

## Regression pass (once per sprint, informational)

Regression pass: FAILED (real-bd suite: fail, smoke test: fail).
Carry-over beads filed: apra-fleet-w7ee, apra-fleet-rf4q, apra-fleet-cptc, apra-fleet-w5j, apra-fleet-p8o, apra-fleet-x1c, apra-fleet-t91, apra-fleet-cgc5.
Summary: Ran the full regression pass at branch HEAD (fix/runner-dolt-push-reliability @ 349e6743) per regression-test-playbook.md. Part 1 (real-bd suite via scripts/run-integ-suites.mjs, 178/178 files complete): 173 pass, 5 fail -- f34-concurrent-launch-engagement-integration.test.mjs (VCSModule resolveProvider vs MCP adapter's missing 'member_detail' tool), golden-transcript.test.mjs and golden-transcript-3bead.test.mjs (4 subtests, false stall-abort despite a verify-routed bead closing), mock-sprint-member-vcs-provider-threading.test.mjs and mock-sprint-publish-push-failure.test.mjs (timeouts at 180000ms); also 46 files exceed the 300s single-file budget (long pole), and the slow lane's mock-sprint-planner-dispatch-stalled-session.test.mjs still fails on SPRINT_LOCK_HELD/bd-replay drift. All of these already had matching parent-less carry-over bugs on file (apra-fleet-w7ee, rf4q, cptc, w5j, p8o, x1c, t91) and were updated in place with today's reconfirmation rather than duplicated. Part 2 (smoke test, fresh sandbox at ~/temp/.apra-fleet-tests, scratch port 18700): Setup and the toy sprint (gh-toy-4ef, max-cycles 1) ran to completion; the --version feature itself was implemented correctly and verified working directly (node dist/index.js --version -> 'fleet-e2e-toy v1.0.0', exit 0), but the sprint's own Deploy phase failed with EADDRINUSE on port 3001 (a stale toy-app dev server left bound by an earlier interrupted attempt on the fixed sandbox path, which ## Setup does not defend against the way ## Reset does), Integration Test was skipped that cycle, and the Develop/Review round cap (3) was reached before the test child gh-toy-4ef.2 could close -- so the canary epic gh-toy-4ef was left OPEN and the toy sprint's own verdict was FAIL. Filed a new parent-less carry-over bug (apra-fleet-cgc5, P2) for this smoke-harness port-cleanup gap. Teardown ran (server stopped, sandbox removed, port 3001 confirmed free) regardless of the failures, per protocol. This result is informational and does not gate the current sprint's verdict; all filed/updated bugs are parent-less carry-over items for a future sprint to pick up.
Informational only -- this pass ran after the final verdict and did not gate it; any bead above is parent-less by design and carries over to a future sprint.
