{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apra-pm/harvester-output@2",
  "version": 2,
  "title": "harvester output (deliberately mismatched major version, test fixture only)",
  "description": "apra-fleet-unw.22 test fixture: simulates a vendor/apra-pm submodule bump that changed the harvester output contract's major version, to prove contracts.mjs's version-pin check throws loudly instead of silently accepting a drifted schema.",
  "type": "object",
  "required": ["status", "notes", "someNewRequiredFieldFromV2"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["OK", "FAILED"]
    },
    "notes": {
      "type": "string"
    },
    "someNewRequiredFieldFromV2": {
      "type": "string"
    }
  }
}
