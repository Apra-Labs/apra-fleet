Read GEMINI.md, PLAN.md, and progress.json. You are on branch sprint/oauth-providers. Phases 1-3 are complete.

Execute Phase 4 — the final phase.

**Task 4.1** — Update auth mode display in `src/tools/member-detail.ts` and `src/tools/list-members.ts`:

Read both files first. Find where `auth` mode is currently displayed. Update the logic to:

```
If OAuth credential files exist on remote AND no API key env var set:
  → auth = "oauth"

If API key env var is set (check provider.authEnvVar in the remote environment):
  → auth = "api-key"

If both OAuth files present AND API key env var set:
  → auth = "api-key (WARNING: OAuth also present — API key takes precedence)"

If neither:
  → auth = "none" (or existing fallback)
```

To check if OAuth files exist on remote: check if `provider.oauthCredentialFiles()` returns non-null AND the first file exists remotely (use `cmds.credentialFileCheck(file.remotePath)` or equivalent).

To check API key env var: check if `provider.authEnvVar` is set in the remote shell environment.

**Task 4.2 (VERIFY — FINAL)**:
1. Run `npm run build` — zero TypeScript errors
2. Run `npm test` — 613 pass, 3 skipped (same baseline)
3. Fix any failures
4. Update progress.json — mark all Phase 4 tasks completed, sprint complete
5. Commit all changes with message: "feat(#62): complete DRY OAuth support for all providers"
6. `git push origin sprint/oauth-providers`
7. **STOP. Report build output and test results. This is the final checkpoint.**