Scope reviewed: apra-fleet-iri and its full subtree (.1/.1.1/.1.2, .2/.2.1/.2.2/.2.3, .3/.3.1/.3.2/.3.3/.3.4, .4/.4.1/.4.2 -- 16 issues total, 11 leaf `task` items plus 5 grouping nodes: root apra-fleet-iri [issue_type=task but a 4-child container with no model metadata, treated as a grouping node like the 4 `feature` siblings] and features .1/.2/.3/.4).

Criteria 1,2,3,6,7,8,9,10 PASS:
- Coverage (1): the single sprint goal (merge origin/main, preserve+port KB features, repoint install paths, prove green build) is fully decomposed across features .1-.4.
- Test tasks (2): every feature has exactly one [test] task (.1.2, .2.3, .3.4, .4.2), each correctly downstream (`blocks`) of its feature's last impl task -- criterion 5 (dependency wiring) also passes on this point.
- Acceptance criteria (3): every leaf task has a concrete, checkable acceptance-criteria list (greps, exit codes, file existence, byte-offset ordering, etc).
- No scope creep (6) / no duplicates (7): all 11 leaf tasks map 1:1 to distinct, in-scope pieces of the merge/port goal; no overlap found.
- Feasibility (8): dependency chain (snapshot -> merge -> port -> repoint) correctly gates each stage on the prior stage's output existing.
- Ready-work check (9): `bd list --parent apra-fleet-iri --ready --json` is non-empty (apra-fleet-iri.1, apra-fleet-iri.1.1), and `bd blocked --parent apra-fleet-iri` / `bd dep cycles` show a clean, single linear chain with no deadlock. Root apra-fleet-iri is issue_type=task, not epic, so `bd epic status` does not apply; manual inspection of dependent_count/children was used instead, per runbook.
- Model metadata (10): all 11 leaf tasks carry a `model` metadata key (standard/premium/cheap as appropriate); the 5 grouping nodes correctly carry none, consistent with their non-dispatched container role.

Criterion 11 FAILS (lane cohesion):
- Presence and cohesion pass: every leaf task has `streak`+`streakOrder`, and each streak groups tasks touching the same files/component (kb-snapshot: 1.1+1.2; merge-resolve: 2.1+2.2+2.3; kb-port-workflow: 3.1+3.2; kb-port-agents: 3.3+3.4; post-merge-verify: 4.1+4.2). Mutex resources are correctly co-laned: 2.1+2.2 explicitly document sharing one in-flight merge/.gitmodules and "must run back to back ... must not be split apart"; 3.1+3.2 explicitly document sharing one file (auto-sprint.js) and "must not be split."  Effort per lane is well under the 200 threshold (merge-resolve is highest at 8 pts x premium(20) = 160), so no over-threshold split exception is in play.
- VIOLATION: apra-fleet-iri.3.3 (streak `kb-port-agents`) carries a `blocks` dependency on apra-fleet-iri.3.2 (streak `kb-port-workflow`) -- a cross-lane blocks edge between two lanes of the same feature (apra-fleet-iri.3) that touch entirely disjoint files (auto-sprint.js for 3.1/3.2 vs. the 4 agent .md files for 3.3/3.4) with no mutex or logical coupling documented anywhere (unlike the explicit mutex notes on 2.1/2.2 and 3.1/3.2). This unnecessarily forces kb-port-agents to wait for kb-port-workflow to fully finish even though the two are independent, and it is exactly the "raw dependency edge spanning two streaks" the lane-cohesion rule prohibits -- ordering across these two lanes should instead come from both independently keying off the same upstream feature gate. Concretely, apra-fleet-iri.3.1 is already (correctly) gated on apra-fleet-iri.2.3; apra-fleet-iri.3.3 should be gated the same way instead of on apra-fleet-iri.3.2. Fix:
  1. `bd dep remove apra-fleet-iri.3.3 apra-fleet-iri.3.2`
  2. `bd dep add apra-fleet-iri.3.3 apra-fleet-iri.2.3`  (parallels how apra-fleet-iri.3.1 is gated, letting both lanes of feature .3 start together once the merge-integrity test closes)
  3. `bd dep add apra-fleet-iri.4.1 apra-fleet-iri.3.2`  (preserves the "feature .3 fully done before feature .4 starts" guarantee -- both lanes -- without serializing kb-port-agents behind kb-port-workflow; apra-fleet-iri.4.1 already depends on apra-fleet-iri.3.4, so this just adds the second lane's completion as a second gate)
  Note: the other three cross-feature blocks edges in this DAG (apra-fleet-iri.2.1 <- apra-fleet-iri.1.2; apra-fleet-iri.3.1 <- apra-fleet-iri.2.3; apra-fleet-iri.4.1 <- apra-fleet-iri.3.4) are NOT flagged -- each sits at a genuine single-lane-per-feature transition boundary (features .1, .2, .4 each contain only one lane), so a task-level blocks edge is the only mechanism available to make bd's readiness computation honor the feature-level sequencing (parent-child does not propagate blocking). apra-fleet-iri.3 is the only feature with two parallel lanes, and that is where the erroneous coupling was introduced.

Additional (non-blocking) observations under criterion 4 (task size, "flag larger ones"):
- apra-fleet-iri.2.1 lists ~15 likely-conflicted files (install.ts, index.ts, types.ts, 4 tool files, install.test.ts, package.json/-lock, ci.yml, README/AGENTS/CHANGELOG, llms-full.txt, .beads/issues.jsonl, calibration.json) -- well over the ~3-file guideline. Flagging per criterion 4, but not requiring a split: the task itself documents this is one atomic in-flight git merge that must not be split across sessions (mutex with 2.2), so decomposing further is infeasible. This is bucketed L / premium accordingly.
- apra-fleet-iri.3.3 states "Files changed: 4 markdown files" -- also over the ~3-file guideline, but unlike 2.1/2.2 and 3.1/3.2 there is no documented mutex/session reason the 4 file edits must stay together (they are independent per-agent template insertions). This could reasonably have been split into smaller per-file tasks; recommend doing so if lane effort ever grows, though it does not block approval at current lane-effort levels (kb-port-agents lane effort = 60, well under threshold).

taskAssignments below cover the 11 dispatchable leaf tasks (grouping nodes apra-fleet-iri, .1, .2, .3, .4 excluded per graph-semantics -- they are parent containers, not doer-dispatched work, and correctly carry no model/streak metadata).
