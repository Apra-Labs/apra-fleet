Scope reviewed: apra-fleet-iri subtree only (17 issues: root task + features .1-.4 + their 12 leaf tasks). bd list --parent apra-fleet-iri --ready --json returned a non-empty set (apra-fleet-iri.1, apra-fleet-iri.1.1) and bd dep cycles found nothing, so criterion 9 (ready-work) passes and there is no dead-lock. Criteria 1 (coverage), 2 (test tasks present per feature: .1.2, .2.3, .3.4, .4.2), 3 (concrete acceptance criteria in every task body), 6 (no scope creep -- everything is merge/KB-port work), 7 (no duplicates), 8 (feasibility -- network reachability to apra-pm remote was pre-verified, DATA_DIR fallback etc. called out), and 10 (every task has model metadata: 1.1/1.2/2.2/2.3/3.2/3.3/3.4/3.5/4.2=standard, 2.1/3.1=premium, 4.1=cheap) all pass.

CHANGES_NEEDED on two points:

(A) Criterion 11, "no cross-lane edges among open members" bullet -- violated systemically. Every feature-to-feature handoff is enforced by a raw `blocks` edge between two leaf tasks that sit in *different* `streak` lanes, even though the feature-level blocks chain (apra-fleet-iri.2 blocked-by apra-fleet-iri.1; .3 blocked-by .2; .4 blocked-by .3) already encodes that same ordering at the lane-group level. The redundant task-level edges are:
  - apra-fleet-iri.2.1 (streak=merge-resolve) blocked-by apra-fleet-iri.1.2 (streak=kb-snapshot) -> fix: `bd dep remove apra-fleet-iri.2.1 apra-fleet-iri.1.2`
  - apra-fleet-iri.3.1 (streak=kb-port-workflow) blocked-by apra-fleet-iri.2.3 (streak=merge-resolve) -> fix: `bd dep remove apra-fleet-iri.3.1 apra-fleet-iri.2.3`
  - apra-fleet-iri.3.3 (streak=kb-port-agents) blocked-by apra-fleet-iri.2.3 (streak=merge-resolve) -> fix: `bd dep remove apra-fleet-iri.3.3 apra-fleet-iri.2.3`
  - apra-fleet-iri.3.5 (streak=kb-port-agents) blocked-by apra-fleet-iri.2.3 (streak=merge-resolve) -> fix: `bd dep remove apra-fleet-iri.3.5 apra-fleet-iri.2.3`
  - apra-fleet-iri.4.1 (streak=post-merge-verify) blocked-by apra-fleet-iri.3.4 (streak=kb-port-agents) -> fix: `bd dep remove apra-fleet-iri.4.1 apra-fleet-iri.3.4`
  - apra-fleet-iri.4.2 (streak=post-merge-verify) blocked-by apra-fleet-iri.3.2 (streak=kb-port-workflow) -> fix: `bd dep remove apra-fleet-iri.4.2 apra-fleet-iri.3.2`

In each case the existing feature-level blocks edge already provides lane sequencing, so these task-level edges are the redundant "raw dependency edge spanning two streaks" the rubric prohibits and should be removed. (Note: within-lane blocks edges -- e.g. iri.1.2<-iri.1.1, iri.2.2<-iri.2.1, iri.2.3<-iri.2.2, iri.3.2<-iri.3.1, iri.3.4<-iri.3.3, iri.3.4<-iri.3.5, iri.4.2<-iri.4.1 -- are correct same-lane sequencing and should stay.) Lane cohesion (bullet 1), mutex co-lane placement (bullet 3, e.g. iri.2.1/iri.2.2 explicitly noted as sharing an in-flight merge and correctly co-laned in merge-resolve; iri.3.1/iri.3.2 correctly co-laned on the same auto-sprint.js file) and effort-under-threshold (bullet 4: merge-resolve lane = (L4+M2+M2)=8 x premium20 = 160 <=200; kb-port-workflow = (L4+M2)=6 x premium20=120<=200; all other lanes well under 200) all pass.

(B) Criterion 4 (task size) -- apra-fleet-iri.2.1 ("[impl] Run git merge origin/main and resolve the source-code conflicts") lists ~14 conflicted paths (src/cli/install.ts, src/index.ts, src/types.ts, 4 files under src/tools/, tests/install.test.ts, package.json, package-lock.json, .github/workflows/ci.yml, README.md, AGENTS.md, CHANGELOG.md, llms-full.txt, .beads/issues.jsonl, sprint-logs/calibration.json) to hand-resolve in one task, far beyond the ~3-file guideline. This is flagged per the runbook's "flag larger ones" instruction; it may be acceptable given it's an atomic in-flight-merge mutex that cannot safely be split mid-merge (the sibling apra-fleet-iri.2.2 task already carves out the submodule half), but the planner should confirm this split is intentional/irreducible rather than an oversight.

Once the six cross-lane blocks edges in (A) are removed (or the planner otherwise justifies keeping them), and (B) is confirmed intentional, this plan should be approved.
