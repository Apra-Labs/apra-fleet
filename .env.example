# Integration test opt-in (copy to .env and fill in)
# .env is gitignored -- never commit real credentials.
# Secrets go in the fleet credential store, not here.

# Set to 1 to opt into real-send email integration tests
SEND_EMAIL_INTEG=1
